# CLAUDE.md - AI Assistant Guide for Medici Theme

## 🚨 КРИТИЧНО ВАЖЛИВО - ЧИТАТИ ПЕРШИМ!

### ⚠️ ОБОВ'ЯЗКОВА ІНФОРМАЦІЯ ПРО ПРОЄКТ

Цей проєкт використовує **КОМЕРЦІЙНІ (ПЛАТНІ) ВЕРСІЇ**:

- ✅ **GeneratePress Premium** (NOT free version!)
- ✅ **GenerateBlocks Pro 2.0+** (NOT free version!)

**ВСІ ЗМІНИ КОДУ МАЮТЬ ВІДПОВІДАТИ ВИМОГАМ:**

1. 📖 **`CODING-RULES.md`** - Обов'язкові правила кодування (ЧИТАТИ ПЕРЕД КОДОМ!)
2. 📖 **`Skill.md`** - Повна документація GenerateBlocks 2.x (1109 рядків)
3. 📖 **`CLAUDE.md`** - Цей файл (архітектура теми)

**ПЕРЕД НАПИСАННЯМ БУДЬ-ЯКОГО КОДУ:**

- ✅ **Перевір наявність `node_modules/@prettier/` - якщо немає, запусти `npm install`**
- ✅ Прочитай `CODING-RULES.md` - обов'язкові правила
- ✅ Прочитай `Skill.md` - якщо працюєш з GenerateBlocks
- ✅ **Прочитай секцію "CSS ПРАВИЛА" в CLAUDE.md - якщо редагуєш CSS файли (css/)**
- ✅ Перевір UniqueId формат (8 hex chars, lowercase)
- ✅ Екрануй CSS variables (`\\u002d\\u002d` в JSON)
- ✅ Додай responsive breakpoints
- ✅ Використай security functions (esc*\*, sanitize*\*)
- ✅ **Перевір баланс `{` та `}` у CSS файлах після редагування**
- ✅ **ЗАПУСТИ `npm run format` ПЕРЕД КОМІТОМ!**
- ✅ **ПЕРЕВІР `npm run format:check` - має бути "All matched files use Prettier code style!"**

**НЕДОТРИМАННЯ = ЗЛАМАНА ТЕМА!**

---

## 🚨 MANDATORY PRE-COMMIT WORKFLOW (CI/CD)

### ⚠️ ОБОВ'ЯЗКОВО ВИКОНАТИ ПЕРЕД КОЖНИМ КОМІТОМ!

**CI/CD перевіряє 7 типів перевірок. Коміт без цих перевірок = FAIL:**

```bash
# 🔴 ОБОВ'ЯЗКОВИЙ WORKFLOW ПЕРЕД КОМІТОМ:

# 0. КРИТИЧНО! Перевір наявність npm залежностей (ПЕРШИЙ КРОК!)
if [ ! -d "node_modules/@prettier" ]; then
    echo "⚠️ ПОМИЛКА: Відсутні npm залежності! Виконую npm install..."
    rm -rf node_modules package-lock.json
    npm install
fi

# 1. Форматування (Prettier) - КРИТИЧНО! ОБОВ'ЯЗКОВО!
npm run format              # Автоматично виправляє всі файли
npm run format:check        # Перевірка (має бути "All matched files use Prettier code style!")

# ВАЖЛИВО: Якщо npm run format:check FAIL → ЗУПИНИСЬ! НЕ комітити!
# Виконай: npm run format та перевір знову

# 2. CSS Balance Check (якщо редагував CSS)
for f in css/**/*.css; do echo "$f: {=$(grep -c '{' $f) }=$(grep -c '}' $f)"; done

# 3. Тільки після успішних перевірок:
git add -A
git commit -m "..."
git push
```

**🚨 ЗОЛОТЕ ПРАВИЛО:**

```
НЕ КОМІТИТИ → поки npm run format:check не покаже "All matched files use Prettier code style!"
```

**⚠️ КРИТИЧНО ВАЖЛИВО - ЧИТАЙ ЦЕ ЗАВЖДИ:**

**PRETTIER CHECK Є ОБОВ'ЯЗКОВИМ ПЕРЕД КОЖНИМ КОМІТОМ!**

```bash
# ЗАВЖДИ виконуй ці 2 команди перед git commit:
npm run format              # Відформатувати всі файли
npm run format:check        # Перевірити що все OK

# Має вивести: "All matched files use Prettier code style!"
# Якщо бачиш [warn] → ЗУПИНИСЬ! НЕ комітити!
```

**Prettier перевіряє:**

- ✅ CSS файли (`css/**/*.css`)
- ✅ JavaScript файли (`js/**/*.js`)
- ✅ PHP файли (`**/*.php`) - ПОТРЕБУЄ `@prettier/plugin-php`!
- ✅ JSON файли (`**/*.json`)
- ✅ Markdown файли (`**/*.md`)

**ЗАБОРОНЕНО комітити якщо:**

- ❌ `npm run format:check` показує `[warn]` або помилки
- ❌ Відсутній `node_modules/@prettier/plugin-php`
- ❌ CSS файли мають незбалансовані дужки `{` ≠ `}`
- ❌ ESLint або StyleLint показують помилки
- ❌ НЕ виконав `npm run format` перед комітом

### 📋 CI/CD Checks (всі мають пройти):

| Check           | Команда локально       | Що перевіряє                       |
| --------------- | ---------------------- | ---------------------------------- |
| **Prettier**    | `npm run format:check` | Форматування CSS/JS/JSON/MD/PHP    |
| **ESLint**      | `npm run lint:js`      | JavaScript syntax + best practices |
| **StyleLint**   | `npm run lint:css`     | CSS syntax + BEM validation        |
| **CSS Balance** | `grep -c "{" / "}"`    | Баланс фігурних дужок              |
| **PHPStan**     | `composer phpstan`     | PHP static analysis (Level 5)      |
| **PHPCS**       | `composer phpcs`       | WordPress Coding Standards         |
| **PHP Compat**  | —                      | PHP 8.1+ compatibility             |

### 🔴 ТИПОВІ ПОМИЛКИ ТА ЯК ЇХ УНИКНУТИ:

**1. Prettier Formatting Error:**

```
[warn] CHANGELOG.md
[warn] Code style issues found. Run Prettier with --write to fix.
```

**Рішення:** `npm run format` ПЕРЕД комітом

**2. CSS Balance Error:**

```
❌ CSS Error in css/file.css: { = 42, } = 41
```

**Рішення:** Знайти незакриту `}` в CSS файлі

**3. ESLint Error:**

```
error  'variable' is not defined  no-undef
```

**Рішення:** `npm run lint:js -- --fix` або вручну виправити

**4. PHPStan Error:**

```
Parameter #1 $post_id of function esc_html expects string, int given
```

**Рішення:** Cast: `esc_html((string) $post_id)`

**5. npm ci / package-lock.json Error:**

```
npm error Invalid: lock file's keyv@4.5.4 does not satisfy keyv@5.5.5
npm error Missing: typescript@5.9.3 from lock file
```

**Рішення:**

```bash
rm -rf node_modules package-lock.json && npm install
```

**6. Node Version Mismatch (EBADENGINE):**

```
npm warn EBADENGINE required: { node: '>=20' }
npm warn EBADENGINE current: { node: 'v18.20.8' }
```

**Рішення:** CI та локально мають використовувати Node 20+. Перевір `.github/workflows/*.yml`

**7. Missing @prettier/plugin-php Error (КРИТИЧНО!):**

```
[error] Cannot find package '@prettier/plugin-php' imported from /home/user/Medici_/noop.js
npm run format
> prettier --write "**/*.{css,js,php,json,md}"
```

**Причина:**

- Відсутній пакет `@prettier/plugin-php` у `node_modules/`
- Пошкоджені або неповні npm залежності
- `package-lock.json` не синхронізований з `package.json`

**Рішення (ОБОВ'ЯЗКОВЕ!):**

```bash
# 1. Видалити node_modules та package-lock.json
rm -rf node_modules package-lock.json

# 2. Чиста інсталяція всіх залежностей
npm install

# 3. Перевірка що @prettier/plugin-php встановлений
ls -la node_modules/@prettier/plugin-php || echo "ПОМИЛКА: пакет не встановлений!"

# 4. Тепер запусти форматування
npm run format

# 5. Перевір що всі файли відформатовані
npm run format:check
```

**Коли виникає:**

- Після `git clone` без `npm install`
- Після оновлення `package.json` без `npm install`
- При пошкоджених `node_modules`
- При конфліктах версій в `package-lock.json`

**КРИТИЧНО:** ЦЕ ЛАМАЄ CI/CD! Prettier НЕ зможе форматувати PHP файли → CI/CD FAIL!

**Профілактика:**
✅ Завжди запускай `npm install` після `git pull`
✅ Перевіряй наявність `node_modules/@prettier/` перед форматуванням
✅ НЕ комітити без перевірки `npm run format:check`

### ⚠️ NODE.JS ВИМОГИ:

| Компонент   | Мінімальна версія |
| ----------- | ----------------- |
| **Node.js** | >= 20.0.0         |
| **npm**     | >= 10.0.0         |

**Перевірка:** `node --version && npm --version`

### ⚡ QUICK COMMANDS:

```bash
# Виправити ВСЕ одразу:
npm run format && npm run lint:js -- --fix && npm run lint:css -- --fix

# Повна перевірка перед комітом:
npm run format:check && npm run lint:js && npm run lint:css && composer phpstan
```

### 🚫 ЗАБОРОНЕНО:

- ❌ **Комітити без `npm run format` (КРИТИЧНО!)**
- ❌ **Комітити якщо `npm run format:check` показує помилки**
- ❌ **Комітити без перевірки наявності `node_modules/@prettier/`**
- ❌ Ігнорувати CI/CD помилки
- ❌ Використовувати `--no-verify` для git hooks
- ❌ Force push без виправлення CI помилок
- ❌ Змінювати Node версію в CI без оновлення package-lock.json
- ❌ Використовувати Node < 20 (залежності вимагають >= 20)
- ❌ Комітити після `git pull` без `npm install`

---

## 🚨 WORDPRESS HTML САНІТИЗАЦІЯ - КРИТИЧНА ПРОБЛЕМА!

### ⚠️ ЗАБОРОНЕНО використовувати HTML block для форм і інтерактивних елементів!

**ПРОБЛЕМА з HTML block (WordPress Gutenberg):**

- ❌ **WordPress редактор "санітайзить" HTML при збереженні**
- ❌ **Видаляє `<form>`, `<input>`, `<svg>`, `<button>` елементи**
- ❌ **JS handler не знаходить `.js-exit-intent-form` → форма не працює**
- ❌ **CSS селектори не матчаться → стилі "не працюють"**
- ❌ **Кнопка закриття порожня (SVG видалено)**
- ❌ **Інтерактивні компоненти стають нефункціональними**

**ЩО ВІДБУВАЄТЬСЯ:**

1. Створюєш HTML block з формою → зберігаєш
2. WordPress редактор автоматично "чистить" HTML:
   - `<form>` → видаляється
   - `<input>` → видаляються
   - `<svg>` → видаляються
   - Залишаються тільки `<label>`, `<div>`, текст
3. JavaScript не знаходить форму (селектор `.js-exit-intent-form` не матчиться)
4. CSS стилі не застосовуються (немає елементів для селекторів)
5. Компонент виглядає "голим" та не працює

**РІШЕННЯ - Використовуй SHORTCODE block замість HTML block:**

✅ **Shortcode block** гарантує збереження всієї розмітки
✅ WordPress **НЕ санітайзить вміст шорткода**
✅ Форма рендериться на **backend через PHP** (не редактор)
✅ Всі класи/атрибути **зберігаються** (`.js-exit-intent-form`, `[data-gb-close-panel]`)
✅ **Параметризація** через атрибути шорткода

**ПРИКЛАД (Exit-Intent Popup):**

```php
// Замість HTML block:
<!-- ❌ НЕ ПРАЦЮЄ - WordPress видалить <form>, <input> -->

// Використовуй Shortcode block:
[medici_exit_intent_popup]

// Або з параметрами:
[medici_exit_intent_popup
  title="Заголовок"
  subtitle="<strong>Підзаголовок</strong>"
  button_text="Текст кнопки"]
```

**ТЕХНІЧНА РЕАЛІЗАЦІЯ шорткодів:**

```php
// 1. Створи метод рендеру в класі (наприклад Exit_Intent_Public)
public function render_popup_shortcode( array $atts = array() ): string {
    $atts = shortcode_atts( /* defaults */, $atts, 'shortcode_name' );

    // Санітизація
    $title = sanitize_text_field( $atts['title'] );
    $html = wp_kses( $atts['html'], /* allowed tags */ );

    ob_start();
    ?>
    <div class="component">
        <!-- Повна HTML розмітка з формою -->
    </div>
    <?php
    return (string) ob_get_clean();
}

// 2. Зареєструй шорткод через init hook (Loader pattern)
$this->loader->add_action( 'init', $this->public, 'register_shortcodes', 10 );

public function register_shortcodes(): void {
    add_shortcode( 'shortcode_name', array( $this, 'render_popup_shortcode' ) );
}
```

**КОЛИ ВИКОРИСТОВУВАТИ SHORTCODE замість HTML:**

- ✅ Будь-які `<form>` елементи
- ✅ `<input>`, `<textarea>`, `<select>`, `<button>`
- ✅ `<svg>` іконки та графіка
- ✅ Інтерактивні компоненти з JavaScript обробниками
- ✅ GenerateBlocks Overlay Panel з формами
- ✅ Динамічний контент (PHP-генерований)

**ФАЙЛИ-ПРИКЛАДИ:**

- `inc/exit-intent/class-exit-intent-public.php:174-256` - реалізація шорткода
- `gutenberg/EXIT-INTENT-POPUP.html` - документація використання

**ПРАВИЛО:**

```
HTML block → тільки для статичного контенту (текст, зображення, розмітка)
Shortcode block → для форм, інпутів, SVG, інтерактивних елементів
```

---

## 🎯 GENERATEBLOCKS OVERLAY PANEL + EXIT-INTENT - ТЕХНІЧНІ ПРАВИЛА

### ⚠️ КРИТИЧНІ МОМЕНТИ ІНТЕГРАЦІЇ

**Для LLM-розробників при роботі з GenerateBlocks Pro Overlay Panel + Exit-Intent**

#### 1. ID Overlay Panel

**Проблема:** У GB Pro Overlay Panel може **НЕ бути** поля "Panel ID".

**Рішення:** Беріть значення з **Overlay Panel Options → Data Attribute**

**Приклад:** `gb-overlay-424`

**Має збігатися в 2 місцях:**

```javascript
// 1. Конфіг (PHP → JavaScript)
window.mediciExitIntentConfig.overlayPanelId = "gb-overlay-424"

// 2. Тригер-кнопка (data-gb-overlay атрибут)
<button data-gb-overlay="gb-overlay-424" ...>
```

**Перевірка збігу:**

```javascript
// Консоль браузера
window.mediciExitIntentConfig.overlayPanelId;
document.querySelector('[data-gb-overlay="gb-overlay-424"]');
```

---

#### 2. Тригер і закриття

**Прихований тригер (ОБОВ'ЯЗКОВИЙ):**

- Має бути у `wp_footer` (GeneratePress Hook Element)
- `exit-intent-overlay.js` відкриває панель через `.click()` по елементу з `data-gb-overlay`

**Приклад:**

```html
<!-- GeneratePress Hook: wp_footer, Priority: 999 -->
<button
	class="exit-intent-trigger"
	data-gb-overlay="gb-overlay-424"
	aria-hidden="true"
	style="display: none !important;"
></button>
```

**Закриття (ОБОВ'ЯЗКОВИЙ атрибут):**

- У контенті popup потрібен елемент з `data-gb-close-panel`
- `closeOverlayPanel()` шукає `.exit-intent-content [data-gb-close-panel]` і клікає його

**Приклад:**

```html
<!-- Кнопка закриття -->
<button data-gb-close-panel>×</button>

<!-- Або лінк -->
<a href="#" data-gb-close-panel>Ні, дякую</a>
```

**Перевірка:**

```javascript
document.querySelector('.exit-intent-content [data-gb-close-panel]');
```

---

#### 3. Конфіг і порядок підключення

**Конфіг має бути ПЕРЕД скриптом:**

```php
// inc/exit-intent/class-exit-intent-public.php
wp_add_inline_script(
	'medici-exit-intent-overlay',
	'window.mediciExitIntentConfig = {...};',
	'before' // КРИТИЧНО!
);
```

**Мінімальний конфіг:**

```javascript
window.mediciExitIntentConfig = {
	overlayPanelId: 'gb-overlay-424', // Data Attribute з Overlay Panel
	cookieExp: 30, // days
	delay: 2, // seconds
	debug: false, // WP_DEBUG value
};
```

**Якщо конфіг ПІСЛЯ скрипта → використовуються дефолтні значення!**

---

#### 4. CSS: критичний кейс `<noscript>`

**ПРОБЛЕМА:**

Оптимізація/тема "defer CSS" переносить `<link rel="stylesheet">` у `<noscript>`:

```html
<!-- ❌ CSS НЕ працює при увімкненому JS -->
<noscript>
	<link rel="stylesheet" href=".../exit-intent-overlay.css" />
</noscript>
```

**Наслідки:**

- `document.styleSheets` НЕ містить файл
- Стилі не застосовуються
- Popup виглядає "голим"

**РІШЕННЯ - Inline CSS:**

```php
// inc/exit-intent/class-exit-intent-assets.php
$handle = 'medici-exit-intent-overlay-inline';

// Register empty handle
wp_register_style($handle, false, [], filemtime($css_path));
wp_enqueue_style($handle);

// Add inline CSS from file
$css = file_get_contents($css_path);
wp_add_inline_style($handle, $css);
```

**Результат:**

```html
<!-- ✅ CSS ЗАВЖДИ працює -->
<style id="medici-exit-intent-overlay-inline-inline-css">
	/* CSS content */
</style>
```

**Переваги:**

- ✅ Inline CSS НЕ може бути переміщений у `<noscript>`
- ✅ Працює з будь-яким оптимізатором
- ✅ Не потрібні excludes

---

#### 5. Швидкі діагностичні команди

**У консолі браузера (F12):**

```javascript
// 1. Перевірка збігу ID
window.mediciExitIntentConfig.overlayPanelId
// → "gb-overlay-424"

document.querySelector('[data-gb-overlay="gb-overlay-424"]')
// → <button> або null (якщо не збігається)

// 2. Перевірка кнопки закриття
document.querySelector('.exit-intent-content [data-gb-close-panel]')
// → <button> або <a> або null

// 3. Перевірка CSS (має бути НЕ transparent)
getComputedStyle(document.querySelector('.exit-intent-content')).backgroundColor
// → "rgb(255, 255, 255)" ✅ або "rgba(0, 0, 0, 0)" ❌

// 4. Перевірка inline CSS
document.querySelector('style#medici-exit-intent-overlay-inline-inline-css')
// → <style> ✅ або null ❌

// 5. Перевірка стилів у document.styleSheets (для external CSS)
[...document.styleSheets].some(s =>
  (s.href || '').includes('exit-intent-overlay.css')
)
// → false (якщо CSS у <noscript>) ❌
```

---

#### 6. Типові помилки

**❌ Помилка #1:** ID не збігається

```javascript
// Конфіг
overlayPanelId: "medici-exit-intent-panel"

// Тригер
data-gb-overlay="gb-overlay-424"

// Результат: popup НЕ відкривається
```

**✅ Рішення:** Синхронізуйте обидва значення

---

**❌ Помилка #2:** Відсутній тригер

```html
<!-- НЕ МАЄ прихованої кнопки з data-gb-overlay -->
```

**✅ Рішення:** Додайте Hook Element у `wp_footer`

---

**❌ Помилка #3:** Відсутній `data-gb-close-panel`

```html
<!-- Кнопка закриття БЕЗ атрибута -->
<button class="exit-intent-close">×</button>
```

**✅ Рішення:** Додайте `data-gb-close-panel`

```html
<button class="exit-intent-close" data-gb-close-panel>×</button>
```

---

**❌ Помилка #4:** CSS у `<noscript>`

```javascript
// Перевірка
getComputedStyle(document.querySelector('.exit-intent-content')).backgroundColor;
// → "rgba(0, 0, 0, 0)" ❌ (transparent = стилі не застосовані)
```

**✅ Рішення:** Використовуйте inline CSS через `wp_add_inline_style()`

---

#### 7. Файли-приклади

**PHP реалізація:**

- `inc/exit-intent/class-exit-intent.php` - головний клас (v1.1.0)
- `inc/exit-intent/class-exit-intent-loader.php` - Loader pattern
- `inc/exit-intent/class-exit-intent-assets.php` - CSS/JS enqueue (v1.1.0)
- `inc/exit-intent/class-exit-intent-public.php` - shortcode + конфіг (v1.1.0)

**JavaScript:**

- `js/exit-intent-overlay.js` - handler (v2.1.3)
- `js/vendor/bioep.min.js` - exit-intent detection

**CSS:**

- `css/components/exit-intent-overlay.css` - стилі (~6.8KB)

**Документація:**

- `gutenberg/EXIT-INTENT-POPUP.html` - інструкції використання

---

## 🔴 КРИТИЧНО! ОБОВ'ЯЗКОВІ ПРАВИЛА ЧИТАННЯ CODING-RULES

### ⚡ НОВА СТРУКТУРА (3 ФАЙЛИ)

**CODING-RULES тепер розділений на 3 спеціалізовані файли для оптимізації:**

1. **CODING-RULES.md** - Master Index (431 рядок) - ЗАВЖДИ читати ПЕРШИМ
2. **docs/coding-rules/CODING-RULES-CORE.md** (~1400 рядків) - Базові правила GenerateBlocks
3. **docs/coding-rules/CODING-RULES-ADVANCED.md** (~1300 рядків) - Продвинуті техніки
4. **docs/coding-rules/CODING-RULES-WORDPRESS.md** (~1600 рядків) - WordPress стандарти

---

### 🚨 ЖОРСТКІ ВИМОГИ ДЛЯ LLM (ОБОВ'ЯЗКОВО!)

**1️⃣ ЗАВЖДИ СПОЧАТКУ ПРОЧИТАЙ MASTER INDEX:**

```
📖 Read: CODING-RULES.md (Master Index)
```

Master Index містить:

- ✅ Структуру документації
- ✅ Таблицю маршрутизації (Який файл читати?)
- ✅ Додаткові правила
- ✅ Checklist

**2️⃣ ВИЗНАЧ ТИП ЗАВДАННЯ ТА ПРОЧИТАЙ ТІЛЬКИ РЕЛЕВАНТНІ ФАЙЛИ:**

**КАТЕГОРИЧНО ЗАБОРОНЕНО:**

- ❌ Читати всі 3 файли одночасно (марнування токенів!)
- ❌ Читати файли "на всяк випадок"
- ❌ Читати файли без перевірки таблиці маршрутизації
- ❌ Пропускати Master Index

**ДОЗВОЛЕНО:**

- ✅ Прочитай Master Index (ЗАВЖДИ!)
- ✅ Визнач тип завдання згідно з таблицею маршрутизації
- ✅ Прочитай ТІЛЬКИ потрібний файл (або 2 файли, якщо таблиця вказує)
- ✅ Якщо сумнів - запитай користувача

---

### 📊 ТАБЛИЦЯ МАРШРУТИЗАЦІЇ (КОПІЯ З MASTER INDEX)

**Використовуй цю таблицю для вибору файлу:**

| Завдання користувача                | Файли для читання                                   |
| ----------------------------------- | --------------------------------------------------- |
| Створення GenerateBlocks patterns   | **CORE**                                            |
| Редагування блоків, JSON            | **CORE**                                            |
| UniqueId, CSS Variables, Responsive | **CORE**                                            |
| Global Styles, Buttons, Cards       | **CORE**                                            |
| **Редагування CSS файлів (css/)**   | **CLAUDE.md (секція CSS ПРАВИЛА)**                  |
| **Виправлення CSS помилок**         | **CLAUDE.md (секція CSS ПРАВИЛА)**                  |
| **Додавання/зміна стилів**          | **CLAUDE.md (секція CSS ПРАВИЛА)**                  |
| Dynamic Content, Query Loops        | **CORE** + **ADVANCED** (секція 34)                 |
| Реєстрація Dynamic Tags API         | **ADVANCED** (секція 21)                            |
| Query Block Pro (ACF)               | **ADVANCED** (секція 22)                            |
| Conditions API                      | **ADVANCED** (секція 23)                            |
| Custom Selectors, Container Queries | **ADVANCED** (секції 24-25)                         |
| WooCommerce інтеграція              | **ADVANCED** (секція 27)                            |
| Performance оптимізація             | **ADVANCED** (секції 28, 30) + **CORE** (секція 17) |
| Блог модуль GeneratePress           | **ADVANCED** (секція 34)                            |
| REST API, Headless                  | **ADVANCED** (секція 33)                            |
| PHP код, WordPress функції          | **WORDPRESS**                                       |
| Типізація, strict_types             | **WORDPRESS**                                       |
| PSR-4, Composer, DI                 | **WORDPRESS**                                       |
| GeneratePress хуки                  | **WORDPRESS** + **CORE** (секція 13)                |
| Безпека, sanitization               | **WORDPRESS** + **CORE** (секція 16)                |
| Checklist, заборонені практики      | **WORDPRESS** + Master Index                        |

---

### 🎯 WORKFLOW ДЛЯ LLM (ОБОВ'ЯЗКОВИЙ АЛГОРИТМ)

**Перед написанням БУДЬ-ЯКОГО коду:**

```
КРОК 1: Прочитай CODING-RULES.md (Master Index)
        ↓
КРОК 2: Визнач тип завдання користувача
        ↓
КРОК 3: Знайди завдання в таблиці маршрутизації
        ↓
КРОК 4: Прочитай ТІЛЬКИ вказані файли (1 або 2)
        ↓
КРОК 5: Виконай завдання згідно з правилами
        ↓
КРОК 6: Перевір Checklist з Master Index
```

**ПРИКЛАД ПРАВИЛЬНОГО WORKFLOW #1:**

```
Користувач: "Створи GenerateBlocks button з hover ефектом"

LLM думає:
1. ✅ Read: CODING-RULES.md (Master Index)
2. ✅ Тип завдання: Створення GenerateBlocks patterns
3. ✅ Таблиця: "Створення GenerateBlocks patterns" → CORE
4. ✅ Read: docs/coding-rules/CODING-RULES-CORE.md (тільки цей файл!)
5. ✅ Знайди секції: UniqueId, CSS Variables, Global Styles, Visual Effects
6. ✅ Створи код згідно з правилами
7. ✅ Checklist з Master Index
```

**ПРИКЛАД ПРАВИЛЬНОГО WORKFLOW #2 (CSS):**

```
Користувач: "Додай стилі для нової картки в css/components/cards.css"

LLM думає:
1. ✅ Read: CLAUDE.md секція "CSS ПРАВИЛА"
2. ✅ Перевіряю баланс дужок ПЕРЕД редагуванням:
   grep -c "{" css/components/cards.css  # 30
   grep -c "}" css/components/cards.css  # 30
3. ✅ Read: css/components/cards.css
4. ✅ Додаю нову картку з коректним закриттям блоку
5. ✅ Перевіряю баланс дужок ПІСЛЯ редагування:
   grep -c "{" css/components/cards.css  # 32
   grep -c "}" css/components/cards.css  # 32
6. ✅ Баланс зберігся → все OK!
7. ✅ Перевіряю hover states, media queries, dark theme
```

**ПРИКЛАД НЕПРАВИЛЬНОГО WORKFLOW (ЗАБОРОНЕНО!):**

```
Користувач: "Створи GenerateBlocks button з hover ефектом"

LLM думає:
1. ❌ Read: docs/coding-rules/CODING-RULES-CORE.md (пропущено Master Index!)
2. ❌ Read: docs/coding-rules/CODING-RULES-ADVANCED.md (непотрібно!)
3. ❌ Read: docs/coding-rules/CODING-RULES-WORDPRESS.md (непотрібно!)
4. ❌ Марнування 3000+ токенів!
```

---

### 💡 МЕХАНІЗМ АВТОМАТИЧНОГО ОНОВЛЕННЯ

**Коли оновлювати файли:**

**CORE файл оновлюється:**

- ✅ При змінах UniqueId, CSS Variables, Responsive
- ✅ При оновленні Global Styles
- ✅ При додаванні нових секцій 1-20

**ADVANCED файл оновлюється:**

- ✅ При додаванні нових продвинутих технік
- ✅ При оновленні секцій 21-35
- ✅ При змінах в Blog Module, Query Block Pro, тощо

**WORDPRESS файл оновлюється:**

- ✅ При змінах WordPress Coding Standards
- ✅ При оновленні типізації, PSR-4
- ✅ При додаванні нових PHP правил

**Master Index оновлюється:**

- ✅ При додаванні нових файлів до структури
- ✅ При змінах в таблиці маршрутизації
- ✅ При оновленні Checklist або заборонених практик
- ✅ При зміні версії (3.0.0+)

**LLM має:**

1. ✅ Визначити, який файл потрібно оновити
2. ✅ Оновити ТІЛЬКИ відповідний файл
3. ✅ Оновити Master Index, якщо змінилась структура
4. ✅ Оновити версію та CHANGELOG в Master Index

**🔄 СИНХРОНІЗАЦІЯ ДОКУМЕНТАЦІЇ (ОБОВ'ЯЗКОВО!):**

При оновленні **CLAUDE.md Change Log** ЗАВЖДИ оновлювати:

1. ✅ **TODO.md** — позначити виконані завдання як `[x]`, додати до секції "✅ ЗАВЕРШЕНО"
2. ✅ **CHANGELOG.md** — додати детальний запис у секцію `[Unreleased]`

**Алгоритм:**

```
CLAUDE.md Change Log оновлено
        ↓
TODO.md: [ ] → [x] для виконаних завдань
        ↓
TODO.md: Додати версію до "✅ ЗАВЕРШЕНО"
        ↓
CHANGELOG.md: Додати запис до [Unreleased]
        ↓
Коміт всіх 3 файлів разом
```

**ЗАБОРОНЕНО:**

- ❌ Оновити CLAUDE.md без оновлення TODO.md
- ❌ Забути CHANGELOG.md при значних змінах
- ❌ Комітити документацію частинами (все разом!)

---

### ⚠️ ПОРУШЕННЯ ЦІЄЇ ВИМОГИ = КРИТИЧНА ПОМИЛКА!

**Якщо LLM:**

- ❌ Пропустив Master Index
- ❌ Прочитав всі файли без необхідності
- ❌ Не використав таблицю маршрутизації
- ❌ Не перевірив Checklist

**Результат:**

- 🔴 Марнування токенів (3000+ токенів замість 500-1000)
- 🔴 Повільний response
- 🔴 Можлива помилка через перевантаження контексту
- 🔴 Неефективна робота LLM

**ПРАВИЛЬНА РОБОТА LLM:**

- ✅ Економія токенів (60-70% менше)
- ✅ Швидший response
- ✅ Точна інформація з потрібного файлу
- ✅ Ефективний workflow

---

## ⚡ ШВИДКІ ДОВІДНИКИ ДЛЯ ULTRA-FAST ДОСТУПУ

### 📝 docs/guides/QUICK-REFERENCE.md (30 СЕКУНД)

**Призначення:** Ультра-короткий довідник для миттєвої перевірки критичних правил.

**Коли використовувати:**

- ✅ Для простих завдань (button, card, section)
- ✅ Коли потрібно швидко перевірити формат (UniqueId, CSS Vars)
- ✅ Перед створенням коду для миттєвого нагадування
- ✅ Замість повного CODING-RULES для тривіальних задач

**Що містить:**

- 🔴 ТОП-10 критичних правил (таблиця)
- 🔧 Генератори коду (UniqueId: JS/Python/Bash)
- 📋 Формати для Copy-Paste (CSS Vars, Hover, Section, PHP)
- 🚫 ТОП-10 найчастіших помилок
- ✅ 5-секундний checklist
- 📖 Маршрутизація файлів (скорочена таблиця)
- ⚡ Ultra-Quick Tips

**Економія токенів:** 80-90% для простих завдань

**Приклад використання:**

```
Користувач: "Створи простий button"

LLM думає:
1. ✅ Read: docs/guides/QUICK-REFERENCE.md (193 рядки, 30 секунд)
2. ✅ Перевіряю ТОП-10 правил
3. ✅ Copy-paste UniqueId generator
4. ✅ Copy-paste Hover Effect format
5. ✅ Створюю код (економія 1200+ токенів!)
```

**ВАЖЛИВО:**

- НЕ використовуй для складних завдань (Query Block Pro, Dynamic Tags API, WooCommerce)
- Для складних завдань завжди читай повний CODING-RULES файл
- docs/guides/QUICK-REFERENCE.md - це доповнення, а не заміна CODING-RULES

---

### ✅ docs/guides/CHECKLIST.md (ДЕТАЛЬНА ПЕРЕВІРКА)

**Призначення:** Повний checklist для всіх етапів розробки з 100+ пунктами перевірки.

**Коли використовувати:**

- ✅ Перед написанням коду (Pre-Code Checklist)
- ✅ Під час розробки (Coding Checklist)
- ✅ Перед коммітом (Pre-Commit Checklist - 40+ пунктів)
- ✅ При debug (Debug Checklist)
- ✅ Для планування workflow (10-step process)

**Що містить:**

1. **⚡ Швидкий Checklist (5 секунд)**
   - UniqueId, CSS Vars, Responsive, Security, Типізація

2. **📝 Pre-Code Checklist**
   - Документація (4 пункти)
   - Структура завдання (4 пункти)
   - Безпека (4 пункти)

3. **🔨 Coding Checklist**
   - GenerateBlocks JSON (40+ пунктів)
     - UniqueId (5 пунктів)
     - CSS Variables (3 пункти)
     - Responsive (4 пункти)
     - Visual Effects (2 пункти)
     - Global Classes (3 пункти)
     - Overlay (3 пункти)
     - Two-Level Section (3 пункти)
     - Attribute Order (4 пункти)
   - PHP Code (20+ пунктів)
   - CSS/Styles (4 пункти)
   - JavaScript (4 пункти)

4. **🚀 Pre-Commit Checklist (КРИТИЧНО!)**
   - GenerateBlocks (10 пунктів)
   - PHP Code (10 пунктів)
   - Performance (8 пунктів)
   - Security (9 пунктів)
   - Documentation (5 пунктів)
   - Testing (7 пунктів)
   - Git (5 пунктів)

5. **🐛 Debug Checklist**
   - GenerateBlocks Issues (4 підсекції)
   - PHP Issues (3 підсекції)
   - Performance Issues (2 підсекції)

6. **📊 Workflow Checklist (10 кроків)**

**Приклад використання:**

```
LLM перед коммітом:
1. ✅ Read: docs/guides/CHECKLIST.md
2. ✅ Виконую Pre-Commit Checklist (40+ пунктів)
3. ✅ Перевіряю UniqueId у hex форматі
4. ✅ Перевіряю CSS Variables екрановані
5. ✅ Перевіряю Security: esc_html, sanitize
6. ✅ Перевіряю Performance: lazy loading, width/height
7. ✅ Всі пункти виконані - готовий до коммі ту
```

**КРИТИЧНО ВАЖЛИВО:**

- ЗАВЖДИ використовуй Pre-Commit Checklist перед коммітом
- НЕ пропускай жодного пункту з Security секції
- Перевіряй UniqueId та CSS Variables в кожному блоці

---

### 🎯 СТРАТЕГІЯ ВИКОРИСТАННЯ ДОВІДНИКІВ

**Для простих завдань (1-3 хвилини):**

```
1. Read: docs/guides/QUICK-REFERENCE.md (30 секунд)
2. Створи код
3. Перевір 5-секундний checklist
```

**Для середніх завдань (5-15 хвилин):**

```
1. Read: CODING-RULES.md (Master Index)
2. Read: Відповідний CODING-RULES файл (CORE/ADVANCED/WORDPRESS)
3. Створи код
4. Read: docs/guides/CHECKLIST.md (Pre-Commit section)
```

**Для складних завдань (15+ хвилин):**

```
1. Read: CODING-RULES.md (Master Index)
2. Read: 1-2 CODING-RULES файли згідно таблиці маршрутизації
3. Створи код
4. Read: docs/guides/CHECKLIST.md (повний Coding Checklist)
5. Read: docs/guides/CHECKLIST.md (Pre-Commit Checklist)
6. Debug якщо потрібно (Debug Checklist)
```

**Метадані (.meta.json файли):**

Для LLM доступні метадані файли:

- `docs/coding-rules/CODING-RULES-CORE.meta.json` - структура CORE файлу
- `docs/coding-rules/CODING-RULES-ADVANCED.meta.json` - структура ADVANCED файлу
- `docs/coding-rules/CODING-RULES-WORDPRESS.meta.json` - структура WORDPRESS файлу

Ці файли містять:

- Розмір файлу (lines)
- Карту секцій з номерами рядків
- Рівень критичності кожної секції
- Ключові слова
- Use cases
- Залежності

**Використання метаданих:**

```
LLM може прочитати .meta.json для:
- Швидкого визначення де знаходиться потрібна секція
- Оцінки складності завдання
- Визначення залежностей між файлами
- Пошуку по ключовим словам
```

---

### 📁 ПОЛІТИКА СТВОРЕННЯ ФАЙЛІВ

**ЗАГАЛЬНЕ ПРАВИЛО ДЛЯ ПРОЄКТУ "MEDICI":**

⚠️ **НЕ СТВОРЮВАТИ НОВІ ФАЙЛИ БЕЗ КРИТИЧНОЇ НЕОБХІДНОСТІ!**

🚫 **ЗАБОРОНЕНО ВИТРАЧАТИ ТОКЕНИ НА НЕПОТРІБНІ ФАЙЛИ!**

**Коли можна створювати нові файли:**

- ✅ Користувач **прямо та явно** попросив створити новий файл
- ✅ **Абсолютно неможливо** виконати завдання без створення нового файлу
- ✅ Створення окремого файлу **критично важливе** для безпеки або продуктивності

**КАТЕГОРИЧНО ЗАБОРОНЕНО:**

- ❌ **НЕ створювати "додаткові" або "допоміжні" файли** навіть якщо здається що це допоможе
- ❌ **НЕ створювати файли "для зручності"** користувача без прямого запиту
- ❌ **НЕ створювати документацію** (.md, .txt, README) без явного запиту
- ❌ **НЕ створювати приклади коду** в окремих файлах
- ❌ **НЕ створювати конфігураційні файли** якщо є існуючі
- ❌ **НЕ витрачати токени** на файли які користувач не просив

**В решті випадків:**

- ❌ НЕ створювати нові PHP модулі - використовувати існуючі файли в `inc/`
- ❌ НЕ створювати нові CSS файли - використовувати `style.css`
- ❌ НЕ створювати нові JS файли - використовувати `js/scripts.js`
- ❌ НЕ створювати нові шаблони - використовувати існуючі template files
- ❌ НЕ створювати нові GeneratePress Elements - використовувати існуючі в `gutenberg/`
- ❌ НЕ створювати markdown документацію (.md) без прямого запиту

**Що робити замість створення:**

- ✅ Додавати код до існуючих файлів (functions.php, style.css, тощо)
- ✅ Об'єднувати функціонал з існуючими модулями
- ✅ Розширювати існуючі класи та функції
- ✅ Використовувати WordPress hooks для додавання функціоналу
- ✅ Додавати CSS правила до style.css з коментарями-секціями
- ✅ Пояснювати код у відповіді замість створення файлів-прикладів

**Мета:**

- Підтримувати проєкт чистим
- Уникати роздування файлової структури
- **Економити токени відповіді**
- Полегшити підтримку коду
- Робити тільки те, що просить користувач

### 📏 ПОЛІТИКА ОНОВЛЕННЯ ДОКУМЕНТАЦІЇ

**🚨 КРИТИЧНО: ЛІМІТ ДОКУМЕНТАЦІЇ В CLAUDE.md**

⚠️ **ЗАБОРОНЕНО створювати 600+ рядків у CLAUDE.md!**

**Максимальний розмір нових секцій:**

- ✅ **40-80 рядків** для звичайних секцій
- ✅ **80-150 рядків** для критично важливих features
- ❌ **600+ рядків** - категорично заборонено!

**Що ОБОВ'ЯЗКОВО включати:**

- ✅ API references (функції з type hints)
- ✅ Function signatures
- ✅ Code examples (minimal, technical)
- ✅ Technical features list
- ✅ Файли змінені та версії

**Що ЗАБОРОНЕНО включати:**

- ❌ User guides (використання для користувачів)
- ❌ Детальні приклади з поясненнями
- ❌ Troubleshooting (debugging steps)
- ❌ Дублювання інформації з інших файлів
- ❌ Теоретичні пояснення та концепції

**Орієнтація документації:**

- 🤖 **Тільки для LLM** (AI assistants)
- 🎯 **Максимум технічної інформації**
- ⚡ **Мінімум слів, максимум коду**

**Мета:** Економити токени, підтримувати документацію компактною та технічною.

---

### 🚫 ЗАБОРОНА ДЕТАЛЬНИХ ІНСТРУКЦІЙ ТА ПОЯСНЕНЬ У ФАЙЛАХ

**🚨 КРИТИЧНО ВАЖЛИВЕ ПРАВИЛО ДЛЯ LLM!**

⚠️ **КАТЕГОРИЧНО ЗАБОРОНЕНО створювати у файлах детальні інструкції, пояснення та надмірну інформацію, яка витрачає час на створення та токени LLM!**

**ЗАБОРОНЕНІ типи контенту:**

- ❌ **Детальні покрокові інструкції** (як зробити щось крок за кроком)
- ❌ **Розгорнуті пояснення** (чому щось працює так, а не інакше)
- ❌ **Tutorials та guides** (навчальні матеріали для користувачів)
- ❌ **Troubleshooting секції** (як виправити проблему А, Б, В...)
- ❌ **Приклади з коментарями** (великі code blocks з детальними поясненнями)
- ❌ **Теоретичні розділи** (про архітектуру, принципи, концепції)
- ❌ **Дублювання інформації** (повтор того що є в інших файлах)
- ❌ **"Як використовувати"** секції (user-facing documentation)

**ДОЗВОЛЕНІ типи контенту:**

- ✅ **API Reference** - тільки сигнатури функцій з type hints
- ✅ **Code snippets** - мінімальні приклади (5-10 рядків максимум)
- ✅ **Technical specs** - версії, залежності, конфігурації
- ✅ **Критичні правила** - що ЗАБОРОНЕНО та що ОБОВ'ЯЗКОВО
- ✅ **File references** - посилання на файли та рядки коду
- ✅ **Commands** - bash/npm команди без пояснень

**ПРИКЛАДИ:**

**❌ НЕПРАВИЛЬНО (ЗАБОРОНЕНО):**

```markdown
## Як використовувати Exit-Intent Popup

### Крок 1: Створення Overlay Panel

1. Відкрийте WordPress admin
2. Перейдіть до GenerateBlocks → Overlay Panels
3. Створіть новий panel з назвою "Exit Intent"
4. У налаштуваннях встановіть:
   - Trigger: None (ми тригеримо програмно)
   - Animation: Scale In
   - Backdrop: Blur 5px
5. Збережіть panel та запам'ятайте Panel ID
6. Додайте HTML контент з форми...

### Крок 2: Налаштування bioEp Library

bioEp library детектує вихід користувача з сайту через mouseout event.
Коли курсор рухається до верхньої частини екрану, library перевіряє cookie
і якщо користувач не бачив popup останні 30 днів, тригериться event...

### Troubleshooting

**Проблема:** Popup не відкривається

**Рішення:**

1. Перевірте що Panel ID співпадає в конфігу та тригері
2. Перевірте browser console на помилки
3. Перевірте що cookie не заблоковані
   ...
```

**✅ ПРАВИЛЬНО (ДОЗВОЛЕНО):**

````markdown
## Exit-Intent Implementation

**Files:**

- `inc/exit-intent/class-exit-intent.php` (v1.1.0) - main bootstrap
- `js/exit-intent-overlay.js` (v2.1.3) - form handler
- `js/vendor/bioep.min.js` - exit-intent detection

**Config:**

```javascript
window.mediciExitIntentConfig = {
	overlayPanelId: 'gb-overlay-424', // Data Attribute з Overlay Panel
	cookieExp: 30, // days
	delay: 2, // seconds
};
```
````

**Critical Rules:**

- ⚠️ Panel ID має збігатися в конфігу та `data-gb-overlay` атрибуті
- ⚠️ Закриття через `data-gb-close-panel` атрибут (ОБОВ'ЯЗКОВИЙ)
- ⚠️ Inline CSS через `wp_add_inline_style()` (bypass optimization plugins)

**Diagnostic (browser console):**

```javascript
window.mediciExitIntentConfig.overlayPanelId; // → "gb-overlay-424"
document.querySelector('[data-gb-overlay="gb-overlay-424"]'); // → <button> або null
```

```

**РІЗНИЦЯ:**

- ❌ Неправильний приклад: **~150 рядків, ~1500 токенів, 80% шум**
- ✅ Правильний приклад: **~30 рядків, ~300 токенів, 100% корисна інформація**

**Економія:** 1200 токенів (80%) + 5 хвилин часу LLM на читання

**ЗОЛОТЕ ПРАВИЛО:**

```

Якщо LLM витрачає > 10 секунд на читання секції
→ секція ЗАНАДТО ДЕТАЛЬНА
→ СКОРОТИ до мінімуму або ВИДАЛИ

````

**ВИКЛЮЧЕННЯ:**

Детальні інструкції ДОЗВОЛЕНІ тільки якщо:

1. ✅ Користувач **явно попросив** створити tutorial/guide
2. ✅ Це **окремий файл** `docs/tutorials/` (НЕ CLAUDE.md, НЕ CHANGELOG.md)
3. ✅ Файл призначений **для людей** (не для LLM)

**ВАЖЛИВО:**

- Файли `CLAUDE.md`, `CODING-RULES*.md`, `CHANGELOG.md` - **тільки для LLM**
- LLM НЕ ПОТРІБНІ детальні пояснення, tutorials, troubleshooting
- LLM потрібні: API, rules, specs, code snippets, file references
- **Кожен токен коштує гроші та час** - використовуй їх ефективно!

**Перевірка перед комітом:**

```bash
# Перевірка файлів перед комітом - використовуй validation script:
./scripts/validate-docs.sh
```

**Мета:** **Максимум корисної інформації, мінімум токенів, мінімум часу LLM.**

---

## 📋 ФАЙЛ-СПЕЦИФІЧНІ ПРАВИЛА ДОКУМЕНТАЦІЇ

### ⚠️ КРИТИЧНІ ФАЙЛИ - СУВОРІ ЗАБОРОНИ

**Ці файли КРИТИЧНО ВАЖЛИВІ для LLM та мають найсуворіші обмеження:**

#### 1. CLAUDE.md (AI Assistant Guide)

**ЗАБОРОНЕНО:**

- ❌ Change logs та version history (використовуй CHANGELOG.md)
- ❌ Детальні implementation tutorials
- ❌ User-facing documentation (як користувачу щось зробити)
- ❌ Розгорнуті приклади з поясненнями > 50 рядків
- ❌ Troubleshooting guides з багатьма кроками

**ДОЗВОЛЕНО:**

- ✅ Архітектурні діаграми та структура проєкту
- ✅ Critical rules та заборони
- ✅ Technical reference (файли, класи, API endpoints)
- ✅ Code snippets (максимум 10-15 рядків)
- ✅ Маршрутизація до детальних файлів

**Ліміт:** 20000 токенів (~2500 рядків)

---

#### 2. CHANGELOG.md (Project History)

**ЗАБОРОНЕНО:**

- ❌ Великі code snippets (> 20 рядків)
- ❌ Детальні таблиці з усіма файлами
- ❌ Покрокові інструкції виправлень
- ❌ Duplicate descriptions (якщо є в commit message)
- ❌ Benchmarks та performance metrics (винятки: критичні оптимізації)

**ДОЗВОЛЕНО:**

- ✅ Короткі описи змін (1-3 речення)
- ✅ Список файлів (без деталей)
- ✅ Commit hashes та branch names
- ✅ Breaking changes warnings
- ✅ Migration guides (короткі, тільки команди)

**Ліміт:** 10000 токенів (~1000 рядків)

---

#### 3. CODING-RULES\*.md (Coding Standards)

**ЗАБОРОНЕНО:**

- ❌ Tutorials "як створити блок з нуля"
- ❌ Теоретичні пояснення "чому BEM краще SMACSS"
- ❌ Multiple examples одного й того ж (1 приклад достатньо)
- ❌ Historical context (чому було прийнято рішення)
- ❌ Comparisons з іншими frameworks

**ДОЗВОЛЕНО:**

- ✅ Rules: "ОБОВ'ЯЗКОВО робити X", "ЗАБОРОНЕНО робити Y"
- ✅ Code examples (1 приклад per rule, < 15 рядків)
- ✅ Validation patterns (regex, functions)
- ✅ Заборонені практики з коротким "чому"
- ✅ File references (де знайти приклад у коді)

**Ліміт:** ~1500 рядків per file

---

#### 4. docs/api/\*.md (API Documentation)

**ЗАБОРОНЕНО:**

- ❌ Installation tutorials (крок 1, крок 2, крок 3...)
- ❌ Usage guides для кінцевих користувачів
- ❌ Zapier/Make integration tutorials
- ❌ Troubleshooting з багатьма кейсами
- ❌ "How it works" теоретичні секції

**ДОЗВОЛЕНО:**

- ✅ Function signatures з type hints
- ✅ Database schema (CREATE TABLE statements)
- ✅ Payload structure (JSON examples)
- ✅ Endpoint URLs та HTTP methods
- ✅ Code examples (мінімальні, working code тільки)

**Ліміт:** ~400 рядків per file

---

#### 5. TODO.md (Project Roadmap)

**ЗАБОРОНЕНО:**

- ❌ Детальні change logs завершених версій
- ❌ Розгорнуті описи кожного завдання (> 5 рядків)
- ❌ Historical context "чому ми це робили"
- ❌ Meeting notes та discussions
- ❌ Застарілі завдання старші 3 місяців (archive або видали)

**ДОЗВОЛЕНО:**

- ✅ Active tasks списки (короткі, actionable)
- ✅ Пріоритети (High/Medium/Low)
- ✅ Технічні довідники (UTM strategy, commands)
- ✅ Roadmap phases (summary only)
- ✅ Links to detailed change log (CHANGELOG.md)

**Ліміт:** ~300 рядків

---

#### 6. docs/\*\*/\*.md (Supporting Documentation)

**ЗАБОРОНЕНО:**

- ❌ Detailed implementation examples (>30 lines)
- ❌ Duplicate content з інших файлів
- ❌ Archive information старша 6 місяців
- ❌ Bot/3rd party documentation (unless critical)
- ❌ Version history файла (git log для цього)

**ДОЗВОЛЕНО:**

- ✅ Technical specifications
- ✅ Quick reference tables
- ✅ File structure maps
- ✅ Validation rules та patterns
- ✅ Critical security notes

**Ліміт:** ~300-500 рядків per file

---

### 🔍 AUTOMATED VALIDATION CHECKLIST

**Pre-commit перевірки (ОБОВ'ЯЗКОВО!):**

```bash
# Automated validation script
./scripts/validate-docs.sh

# Checks:
# - File size limits (CLAUDE.md: 2500, CHANGELOG.md: 1000, TODO.md: 300)
# - Keywords validation (verbose docs, detailed instructions)
# - Changelog duplicates in CLAUDE.md
# - Archive references
```

**Повний код:** `scripts/validate-docs.sh` (119 lines)

---

### 📊 MANUAL REVIEW CHECKLIST

**Перед комітом documentation changes:**

- [ ] Прочитав файл за < 60 секунд (якщо ні → занадто деталь но)
- [ ] Немає tutorials "як зробити X"
- [ ] Немає troubleshooting "проблема A → рішення B"
- [ ] Немає дублікатів з інших файлів
- [ ] Code examples < 15 рядків (винятки: critical complex cases)
- [ ] Списки заборон чіткі та короткі
- [ ] Посилання на детальні файли замість copy-paste
- [ ] Файл не перевищує token limit для свого типу
- [ ] Запустив `npm run format` перед комітом
- [ ] Запустив validation script (якщо є)

---

### 🚨 КРИТИЧНІ ПОМИЛКИ - INSTANT REJECT

**Якщо LLM додав це у commit - НЕГАЙНО REJECT:**

1. ❌ **Tutorial секція з >5 steps** в CLAUDE.md або API docs
2. ❌ **Changelog entries** в CLAUDE.md (belongs to CHANGELOG.md)
3. ❌ **Code snippets >50 рядків** в будь-якому .md файлі
4. ❌ **Troubleshooting guide** з multiple scenarios
5. ❌ **Duplicate content** що вже є в іншому файлі
6. ❌ **Archive information** старша 6 місяців в active docs
7. ❌ **User-facing tutorials** в LLM documentation files
8. ❌ **Theoretical explanations** без actionable rules

**Action:** `git reset HEAD~1` і перепиши commit з правильною документацією.

---

### 💡 BEST PRACTICES

**Замість tutorials - дай:**

- ✅ File path + function name: `inc/events/class-events.php:174-256`
- ✅ Command to run: `wp option update medici_webhook_url 'https://...'`
- ✅ Config example: `{overlayPanelId: 'gb-overlay-424', cookieExp: 30}`
- ✅ Critical rule: "Panel ID MUST match in config and data-gb-overlay attribute"

**Замість troubleshooting - дай:**

- ✅ Diagnostic command: `console.log(window.mediciExitIntentConfig)`
- ✅ Validation check: `document.querySelector('[data-gb-close-panel]') !== null`
- ✅ Error indicator: "If X is null → missing Y attribute"

**Замість duplicate content - дай:**

- ✅ Cross-reference: "See CODING-RULES-CORE.md section 14 for UniqueId format"
- ✅ Link: "Full API reference in docs/api/EVENTS-API.md"
- ✅ Summary: "3 strategies: Source (20pts), Medium (15pts), Service (25pts)"

---

## 🚨 КРИТИЧНІ ПРАВИЛА ДЛЯ CSS ФАЙЛІВ

### ⚠️ ОБОВ'ЯЗКОВІ ВИМОГИ ПРИ РОБОТІ З CSS

**ЗАБОРОНЕНО - ЦЕ ЛАМАЄ ПАРСИНГ CSS!**

❌ **НЕ залишати незакриті фігурні дужки** - це критична помилка!
❌ **НЕ видаляти закриваючі `}` при редагуванні** - завжди перевіряй баланс
❌ **НЕ копіювати CSS без перевірки синтаксису** - перевір кожен блок
❌ **НЕ змішувати стилі з різних компонентів** без закриття попередніх блоків
❌ **НЕ видаляти media queries без закриття всіх вкладених правил**

**ОБОВ'ЯЗКОВО - ЦЕ ГАРАНТУЄ СТАБІЛЬНІСТЬ!**

✅ **ЗАВЖДИ перевіряй баланс `{` та `}` після редагування**
✅ **ЗАВЖДИ закривай media queries коректно**
✅ **ЗАВЖДИ закривай keyframe animations** (`@keyframes`)
✅ **ЗАВЖДИ закривай псевдокласи** (`:hover`, `:focus`, `:before`, `:after`)
✅ **ЗАВЖДИ закривай вкладені селектори** (`[data-theme="dark"]`, `:root`)
✅ **ЗАВЖДИ перевіряй CSS перед комітом** - використовуй валідатор

### 📋 CHECKLIST ПЕРЕД РЕДАГУВАННЯМ CSS

**1. Перед зміною:**

```bash
# Перевір баланс дужок (має бути однаково)
grep -c "{" css/file.css && grep -c "}" css/file.css
```

**2. Під час редагування:**

- ✅ Відкрив `{` → відразу додай закриваючу `}` (навіть якщо вона буде далі)
- ✅ Видаляєш блок → видали ВСЕ (і `{` і `}`)
- ✅ Копіюєш код → перевір чи всі дужки на місці
- ✅ Media query → перевір вкладеність селекторів

**3. Після редагування:**

```bash
# Перевір баланс знову
grep -c "{" css/file.css && grep -c "}" css/file.css

# Перевір CSS валідатором (опціонально)
npx stylelint css/file.css
```

### 🛡️ КРИТИЧНІ ФАЙЛИ CSS (ПІДВИЩЕНА УВАГА!)

**Файли, де помилки НАЙБІЛЬШ критичні:**

1. **`css/core/variables.css`** - CSS змінні (`:root`, `[data-theme="dark"]`)
   - ⚠️ Незакритий `:root` → весь сайт без змінних!
   - ⚠️ Незакритий `[data-theme="dark"]` → темна тема зламана!

2. **`css/components/navigation.css`** - Fixed header
   - ⚠️ Незакриті media queries → навігація зламана на mobile!
   - ⚠️ Незакриті keyframes → анімація не працює!

3. **`css/components/cards.css`** - Картки (7 типів)
   - ⚠️ Незакриті hover states → картки не реагують на hover!

4. **`css/layout/layout.css`** - Grid, Hero, Footer
   - ⚠️ Незакритий footer → весь footer зламаний!
   - ⚠️ Незакриті media queries → responsive не працює!

5. **`css/modules/blog/blog-single.css`** - Single post (832 рядки)
   - ⚠️ Найбільший файл → найбільше ризиків!
   - ⚠️ Багато вкладених блоків → уважно перевіряй!

### 🔍 ТИПОВІ ПОМИЛКИ (УНИКАЙ!)

**Помилка #1: Видалення закриваючої дужки при refactoring**

```css
/* ❌ НЕПРАВИЛЬНО */
.card {
    background: white;
    /* видалив декілька рядків і забув закрити блок */

.another-card {
    /* помилка парсингу! */
}

/* ✅ ПРАВИЛЬНО */
.card {
    background: white;
} /* ЗАВЖДИ закривай блок! */

.another-card {
    background: gray;
}
```

**Помилка #2: Незакритий media query**

```css
/* ❌ НЕПРАВИЛЬНО */
@media (max-width: 767px) {
    .element {
        padding: 1rem;
    }
/* забув закрити media query! */

/* ✅ ПРАВИЛЬНО */
@media (max-width: 767px) {
    .element {
        padding: 1rem;
    }
} /* ЗАВЖДИ закривай media query! */
```

**Помилка #3: Незакритий псевдоклас з вкладеністю**

```css
/* ❌ НЕПРАВИЛЬНО */
[data-theme="dark"] {
    --bg: #0f172a;
    .card {
        background: var(--bg);
    }
/* забув закрити [data-theme="dark"]! */

/* ✅ ПРАВИЛЬНО */
[data-theme="dark"] {
    --bg: #0f172a;
}

[data-theme="dark"] .card {
    background: var(--bg);
}
```

**Помилка #4: Незакритий keyframe animation**

```css
/* ❌ НЕПРАВИЛЬНО */
@keyframes slideDown {
    from {
        opacity: 0;
    }
    to {
        opacity: 1;
    }
/* забув закрити @keyframes! */

/* ✅ ПРАВИЛЬНО */
@keyframes slideDown {
    from {
        opacity: 0;
    }
    to {
        opacity: 1;
    }
} /* ЗАВЖДИ закривай @keyframes! */
```

### 🎯 ЗОЛОТЕ ПРАВИЛО CSS

**Кожна відкриваюча `{` МАЄ мати закриваючу `}`**

```
Відкриваючих { = Закриваючих }
```

**Перевірка балансу:**

```bash
# Має вивести однакові числа!
grep -c "{" css/file.css  # 42
grep -c "}" css/file.css  # 42
```

Якщо числа різні → є помилка!

### 📊 РЕКОМЕНДОВАНІ ІНСТРУМЕНТИ

**1. VS Code Extensions:**

- CSS Peek - швидкий перегляд CSS
- Stylelint - автоматична перевірка синтаксису
- Bracket Pair Colorizer - візуалізація парних дужок

**2. CLI Tools:**

```bash
# Stylelint (встановити глобально)
npm install -g stylelint stylelint-config-standard

# Перевірка файлу
stylelint css/**/*.css
```

**3. Git Pre-commit Hook (опціонально):**

```bash
#!/bin/bash
# Перевірка балансу дужок перед комітом
for file in css/**/*.css; do
    open=$(grep -c "{" "$file")
    close=$(grep -c "}" "$file")
    if [ "$open" != "$close" ]; then
        echo "❌ CSS Error in $file: { = $open, } = $close"
        exit 1
    fi
done
```

### ⚡ ЩО РОБИТИ ЯКЩО CSS ЗЛАМАВСЯ?

**Крок 1: Знайди файл з помилкою**

```bash
# Перевір баланс у всіх CSS файлах
for file in css/**/*.css; do
    echo "=== $file ==="
    grep -c "{" "$file"
    grep -c "}" "$file"
done
```

**Крок 2: Знайди незакриту дужку**

- Відкрий файл у VS Code
- Встанови курсор на `{` і подивись чи є парна `}`
- Використовуй "Go to Bracket" (Ctrl+Shift+\)

**Крок 3: Виправ помилку**

- Додай закриваючу `}` у потрібному місці
- Перевір баланс знову

**Крок 4: Протестуй**

- Перезавантаж сторінку (Ctrl+F5)
- Перевір чи стилі застосувались
- Перевір Dark Theme (якщо є)
- Перевір Responsive (mobile, tablet)

**ВАЖЛИВО:** Після виправлення CSS помилок завжди перевіряй:

- ✅ Головну сторінку
- ✅ Блог сторінку
- ✅ Single post
- ✅ Footer
- ✅ Navigation (mobile menu)
- ✅ Dark theme toggle
- ✅ Всі responsive breakpoints (767px, 1024px)

---

## Project Overview

**Medici Medical Marketing Theme** - WordPress child theme для медичного маркетингу.

### Key Information

- **Theme Type**: WordPress Child Theme
- **Parent Theme**: GeneratePress Premium (commercial)
- **Block Editor**: GenerateBlocks Pro 2.0+ (commercial)
- **Version**: 2.0.0
- **Text Domain**: medici.agency
- **Language**: Ukrainian (primary)
- **Target**: Medical Marketing Agencies
- **Site**: https://www.medici.agency

### AI Language Requirements

🇺🇦 **Ukrainian Language Only** 🇺🇦

All communication MUST be in Ukrainian language.

---

## Architecture & File Structure

**Скорочена версія. Детальніше див. `CLAUDE.md.backup`**

```
medici/
├── functions.php          # Module loader v2.0.0
├── style.css             # Theme metadata
├── theme.json            # WordPress Global Styles
│
├── inc/                   # PHP modules (62 файли)
│   ├── blog/              # Repository + Service Pattern
│   ├── events/            # Event Dispatcher + Observer
│   ├── lead/              # Adapter Pattern
│   └── [core modules]     # 22 core modules
│
├── css/                   # ITCSS Architecture
├── js/                    # Frontend JavaScript
├── gutenberg/             # GeneratePress Elements (10 HTML)
└── assets/                # Fonts, images, twemoji
```

**OOP Architecture (v2.0.0):**

- `inc/blog/` - Repository + Service
- `inc/events/` - Event Dispatcher + 4 observers
- `inc/lead/` - IntegrationManager + 3 adapters
- `inc/exit-intent/` - Plugin Boilerplate

---

## 📚 ДОДАТКОВА ДОКУМЕНТАЦІЯ

**Винесені файли для детальної інформації:**

- **📁 Advanced Architecture:** `docs/architecture/ADVANCED-PATTERNS.md`
  - PSR-11 DI Container, PSR-4 Autoloading, Repository Pattern
  - REST API Endpoints, Static Analysis, Production Performance

- **📁 Modern Frontend:** `docs/architecture/MODERN-FRONTEND.md`
  - Modern ES6+ JavaScript, Gutenberg Block Development
  - CSS Architecture — BEM + ITCSS, Performance Optimization

- **📁 WordPress Security:** `docs/security/WORDPRESS-SECURITY-CVE.md`
  - Ландшафт загроз 2024-2025, Критичні патерни вразливостей
  - Security Audit Checklist, Hardening для теми Medici

- **📁 Shortcodes API:** `docs/api/SHORTCODES-API.md`
  - medici_warning, medici_cta, medici_takeaways, Technical Details

- **📁 DevOps Guide:** `docs/devops/DEVOPS-GUIDE.md`
  - Composer & PHPStan, PHP CodeSniffer, GitHub Actions CI/CD

- **📁 Changelog:** `CHANGELOG.md`
  - Детальна історія змін проєкту

---

## 📝 Recent Updates (2025-12-19)

### ⚡ Git Sparse Checkout (Dev Optimization)

**Додано ефективне рішення для прискорення Dev workflow:**

- ✅ **16x прискорення** - виключення 4,350 файлів з checkout
- ✅ Створено автоматичні скрипти (`./git-dev-mode enable/disable/status`)
- ✅ Детальна документація (600+ рядків) в `docs/devops/GIT-SPARSE-CHECKOUT.md`
- ✅ Шаблони конфігурації в `.git-templates/`
- ✅ Оновлено `docs/devops/DEVOPS-GUIDE.md`

**Результати:**

- Файлів: 4,623 → 273 (**16x менше**)
- `git pull`: ~5 сек → <1 сек (**5x швидше**)
- IDE індексування: ~10 сек → ~1 сек (**10x швидше**)

### 🎨 Prettier Formatting Rules

**ПОСИЛЕНО правила Prettier перед комітом:**

- ⚠️ **КРИТИЧНО:** `npm run format:check` є ОБОВ'ЯЗКОВИМ перед комітом
- ✅ Додано детальний checklist в секцію "MANDATORY PRE-COMMIT WORKFLOW"
- ✅ Prettier перевіряє: CSS, JS, PHP, JSON, MD (5 типів файлів)
- ❌ ЗАБОРОНЕНО комітити якщо `format:check` показує `[warn]`

**Команди:**

```bash
npm run format              # Автоматично виправити
npm run format:check        # Перевірити перед комітом
```

### 🔧 Виправлення

- 🐛 Виправлено підрахунок файлів у `git-dev-mode` скрипті
- 🎨 Prettier formatting для всіх markdown файлів
- 📝 Оновлено sparse-checkout конфігурацію (додано `docs/devops/`)

---

**Last Updated:** 2025-12-19
**Theme Version:** 2.0.0
````
