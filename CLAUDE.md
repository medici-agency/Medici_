# CLAUDE.md - AI Assistant Guide for Medici Theme

## 🚨 КРИТИЧНО ВАЖЛИВО - ЧИТАТИ ПЕРШИМ!

### ⚠️ ОБОВ'ЯЗКОВА ІНФОРМАЦІЯ ПРО ПРОЄКТ

Цей проєкт використовує **КОМЕРЦІЙНІ (ПЛАТНІ) ВЕРСІЇ**:

- ✅ **GeneratePress Premium** (NOT free version!)
- ✅ **GenerateBlocks Pro 2.0+** (NOT free version!)

**ВСІ ЗМІНИ КОДУ МАЮТЬ ВІДПОВІДАТИ ВИМОГАМ:**

1. 📖 **`CODING-RULES.md`** - Обов'язкові правила кодування (ЧИТАТИ ПЕРЕД КОДОМ!)
2. 📖 **`Skill.md`** - Повна документація GenerateBlocks 2.x (1109 рядків)
3. 📖 **`CLAUDE.md`** - Цей файл (архітектура теми)

**ПЕРЕД НАПИСАННЯМ БУДЬ-ЯКОГО КОДУ:**

- ✅ Прочитай `CODING-RULES.md` - обов'язкові правила
- ✅ Прочитай `Skill.md` - якщо працюєш з GenerateBlocks
- ✅ **Прочитай секцію "CSS ПРАВИЛА" в CLAUDE.md - якщо редагуєш CSS файли (css/)**
- ✅ Перевір UniqueId формат (8 hex chars, lowercase)
- ✅ Екрануй CSS variables (`\\u002d\\u002d` в JSON)
- ✅ Додай responsive breakpoints
- ✅ Використай security functions (esc*\*, sanitize*\*)
- ✅ **Перевір баланс `{` та `}` у CSS файлах після редагування**
- ✅ **ЗАПУСТИ `npm run format` ПЕРЕД КОМІТОМ!**

**НЕДОТРИМАННЯ = ЗЛАМАНА ТЕМА!**

---

## 🚨 MANDATORY PRE-COMMIT WORKFLOW (CI/CD)

### ⚠️ ОБОВ'ЯЗКОВО ВИКОНАТИ ПЕРЕД КОЖНИМ КОМІТОМ!

**CI/CD перевіряє 7 типів перевірок. Коміт без цих перевірок = FAIL:**

```bash
# 🔴 ОБОВ'ЯЗКОВИЙ WORKFLOW ПЕРЕД КОМІТОМ:

# 1. Форматування (Prettier) - КРИТИЧНО!
npm run format              # Автоматично виправляє всі файли
npm run format:check        # Перевірка (має бути "All matched files use Prettier code style!")

# 2. CSS Balance Check (якщо редагував CSS)
for f in css/**/*.css; do echo "$f: {=$(grep -c '{' $f) }=$(grep -c '}' $f)"; done

# 3. Тільки після успішних перевірок:
git add -A
git commit -m "..."
git push
```

### 📋 CI/CD Checks (всі мають пройти):

| Check           | Команда локально       | Що перевіряє                       |
| --------------- | ---------------------- | ---------------------------------- |
| **Prettier**    | `npm run format:check` | Форматування CSS/JS/JSON/MD/PHP    |
| **ESLint**      | `npm run lint:js`      | JavaScript syntax + best practices |
| **StyleLint**   | `npm run lint:css`     | CSS syntax + BEM validation        |
| **CSS Balance** | `grep -c "{" / "}"`    | Баланс фігурних дужок              |
| **PHPStan**     | `composer phpstan`     | PHP static analysis (Level 5)      |
| **PHPCS**       | `composer phpcs`       | WordPress Coding Standards         |
| **PHP Compat**  | —                      | PHP 8.1+ compatibility             |

### 🔴 ТИПОВІ ПОМИЛКИ ТА ЯК ЇХ УНИКНУТИ:

**1. Prettier Formatting Error:**

```
[warn] CHANGELOG.md
[warn] Code style issues found. Run Prettier with --write to fix.
```

**Рішення:** `npm run format` ПЕРЕД комітом

**2. CSS Balance Error:**

```
❌ CSS Error in css/file.css: { = 42, } = 41
```

**Рішення:** Знайти незакриту `}` в CSS файлі

**3. ESLint Error:**

```
error  'variable' is not defined  no-undef
```

**Рішення:** `npm run lint:js -- --fix` або вручну виправити

**4. PHPStan Error:**

```
Parameter #1 $post_id of function esc_html expects string, int given
```

**Рішення:** Cast: `esc_html((string) $post_id)`

### ⚡ QUICK COMMANDS:

```bash
# Виправити ВСЕ одразу:
npm run format && npm run lint:js -- --fix && npm run lint:css -- --fix

# Повна перевірка перед комітом:
npm run format:check && npm run lint:js && npm run lint:css && composer phpstan
```

### 🚫 ЗАБОРОНЕНО:

- ❌ Комітити без `npm run format`
- ❌ Ігнорувати CI/CD помилки
- ❌ Використовувати `--no-verify` для git hooks
- ❌ Force push без виправлення CI помилок

---

## 🔴 КРИТИЧНО! ОБОВ'ЯЗКОВІ ПРАВИЛА ЧИТАННЯ CODING-RULES

### ⚡ НОВА СТРУКТУРА (3 ФАЙЛИ)

**CODING-RULES тепер розділений на 3 спеціалізовані файли для оптимізації:**

1. **CODING-RULES.md** - Master Index (431 рядок) - ЗАВЖДИ читати ПЕРШИМ
2. **docs/coding-rules/CODING-RULES-CORE.md** (~1400 рядків) - Базові правила GenerateBlocks
3. **docs/coding-rules/CODING-RULES-ADVANCED.md** (~1300 рядків) - Продвинуті техніки
4. **docs/coding-rules/CODING-RULES-WORDPRESS.md** (~1600 рядків) - WordPress стандарти

---

### 🚨 ЖОРСТКІ ВИМОГИ ДЛЯ LLM (ОБОВ'ЯЗКОВО!)

**1️⃣ ЗАВЖДИ СПОЧАТКУ ПРОЧИТАЙ MASTER INDEX:**

```
📖 Read: CODING-RULES.md (Master Index)
```

Master Index містить:

- ✅ Структуру документації
- ✅ Таблицю маршрутизації (Який файл читати?)
- ✅ Додаткові правила
- ✅ Checklist

**2️⃣ ВИЗНАЧ ТИП ЗАВДАННЯ ТА ПРОЧИТАЙ ТІЛЬКИ РЕЛЕВАНТНІ ФАЙЛИ:**

**КАТЕГОРИЧНО ЗАБОРОНЕНО:**

- ❌ Читати всі 3 файли одночасно (марнування токенів!)
- ❌ Читати файли "на всяк випадок"
- ❌ Читати файли без перевірки таблиці маршрутизації
- ❌ Пропускати Master Index

**ДОЗВОЛЕНО:**

- ✅ Прочитай Master Index (ЗАВЖДИ!)
- ✅ Визнач тип завдання згідно з таблицею маршрутизації
- ✅ Прочитай ТІЛЬКИ потрібний файл (або 2 файли, якщо таблиця вказує)
- ✅ Якщо сумнів - запитай користувача

---

### 📊 ТАБЛИЦЯ МАРШРУТИЗАЦІЇ (КОПІЯ З MASTER INDEX)

**Використовуй цю таблицю для вибору файлу:**

| Завдання користувача                | Файли для читання                                   |
| ----------------------------------- | --------------------------------------------------- |
| Створення GenerateBlocks patterns   | **CORE**                                            |
| Редагування блоків, JSON            | **CORE**                                            |
| UniqueId, CSS Variables, Responsive | **CORE**                                            |
| Global Styles, Buttons, Cards       | **CORE**                                            |
| **Редагування CSS файлів (css/)**   | **CLAUDE.md (секція CSS ПРАВИЛА)**                  |
| **Виправлення CSS помилок**         | **CLAUDE.md (секція CSS ПРАВИЛА)**                  |
| **Додавання/зміна стилів**          | **CLAUDE.md (секція CSS ПРАВИЛА)**                  |
| Dynamic Content, Query Loops        | **CORE** + **ADVANCED** (секція 34)                 |
| Реєстрація Dynamic Tags API         | **ADVANCED** (секція 21)                            |
| Query Block Pro (ACF)               | **ADVANCED** (секція 22)                            |
| Conditions API                      | **ADVANCED** (секція 23)                            |
| Custom Selectors, Container Queries | **ADVANCED** (секції 24-25)                         |
| WooCommerce інтеграція              | **ADVANCED** (секція 27)                            |
| Performance оптимізація             | **ADVANCED** (секції 28, 30) + **CORE** (секція 17) |
| Блог модуль GeneratePress           | **ADVANCED** (секція 34)                            |
| REST API, Headless                  | **ADVANCED** (секція 33)                            |
| PHP код, WordPress функції          | **WORDPRESS**                                       |
| Типізація, strict_types             | **WORDPRESS**                                       |
| PSR-4, Composer, DI                 | **WORDPRESS**                                       |
| GeneratePress хуки                  | **WORDPRESS** + **CORE** (секція 13)                |
| Безпека, sanitization               | **WORDPRESS** + **CORE** (секція 16)                |
| Checklist, заборонені практики      | **WORDPRESS** + Master Index                        |

---

### 🎯 WORKFLOW ДЛЯ LLM (ОБОВ'ЯЗКОВИЙ АЛГОРИТМ)

**Перед написанням БУДЬ-ЯКОГО коду:**

```
КРОК 1: Прочитай CODING-RULES.md (Master Index)
        ↓
КРОК 2: Визнач тип завдання користувача
        ↓
КРОК 3: Знайди завдання в таблиці маршрутизації
        ↓
КРОК 4: Прочитай ТІЛЬКИ вказані файли (1 або 2)
        ↓
КРОК 5: Виконай завдання згідно з правилами
        ↓
КРОК 6: Перевір Checklist з Master Index
```

**ПРИКЛАД ПРАВИЛЬНОГО WORKFLOW #1:**

```
Користувач: "Створи GenerateBlocks button з hover ефектом"

LLM думає:
1. ✅ Read: CODING-RULES.md (Master Index)
2. ✅ Тип завдання: Створення GenerateBlocks patterns
3. ✅ Таблиця: "Створення GenerateBlocks patterns" → CORE
4. ✅ Read: docs/coding-rules/CODING-RULES-CORE.md (тільки цей файл!)
5. ✅ Знайди секції: UniqueId, CSS Variables, Global Styles, Visual Effects
6. ✅ Створи код згідно з правилами
7. ✅ Checklist з Master Index
```

**ПРИКЛАД ПРАВИЛЬНОГО WORKFLOW #2 (CSS):**

```
Користувач: "Додай стилі для нової картки в css/components/cards.css"

LLM думає:
1. ✅ Read: CLAUDE.md секція "CSS ПРАВИЛА"
2. ✅ Перевіряю баланс дужок ПЕРЕД редагуванням:
   grep -c "{" css/components/cards.css  # 30
   grep -c "}" css/components/cards.css  # 30
3. ✅ Read: css/components/cards.css
4. ✅ Додаю нову картку з коректним закриттям блоку
5. ✅ Перевіряю баланс дужок ПІСЛЯ редагування:
   grep -c "{" css/components/cards.css  # 32
   grep -c "}" css/components/cards.css  # 32
6. ✅ Баланс зберігся → все OK!
7. ✅ Перевіряю hover states, media queries, dark theme
```

**ПРИКЛАД НЕПРАВИЛЬНОГО WORKFLOW (ЗАБОРОНЕНО!):**

```
Користувач: "Створи GenerateBlocks button з hover ефектом"

LLM думає:
1. ❌ Read: docs/coding-rules/CODING-RULES-CORE.md (пропущено Master Index!)
2. ❌ Read: docs/coding-rules/CODING-RULES-ADVANCED.md (непотрібно!)
3. ❌ Read: docs/coding-rules/CODING-RULES-WORDPRESS.md (непотрібно!)
4. ❌ Марнування 3000+ токенів!
```

---

### 💡 МЕХАНІЗМ АВТОМАТИЧНОГО ОНОВЛЕННЯ

**Коли оновлювати файли:**

**CORE файл оновлюється:**

- ✅ При змінах UniqueId, CSS Variables, Responsive
- ✅ При оновленні Global Styles
- ✅ При додаванні нових секцій 1-20

**ADVANCED файл оновлюється:**

- ✅ При додаванні нових продвинутих технік
- ✅ При оновленні секцій 21-35
- ✅ При змінах в Blog Module, Query Block Pro, тощо

**WORDPRESS файл оновлюється:**

- ✅ При змінах WordPress Coding Standards
- ✅ При оновленні типізації, PSR-4
- ✅ При додаванні нових PHP правил

**Master Index оновлюється:**

- ✅ При додаванні нових файлів до структури
- ✅ При змінах в таблиці маршрутизації
- ✅ При оновленні Checklist або заборонених практик
- ✅ При зміні версії (3.0.0+)

**LLM має:**

1. ✅ Визначити, який файл потрібно оновити
2. ✅ Оновити ТІЛЬКИ відповідний файл
3. ✅ Оновити Master Index, якщо змінилась структура
4. ✅ Оновити версію та CHANGELOG в Master Index

**🔄 СИНХРОНІЗАЦІЯ ДОКУМЕНТАЦІЇ (ОБОВ'ЯЗКОВО!):**

При оновленні **CLAUDE.md Change Log** ЗАВЖДИ оновлювати:

1. ✅ **TODO.md** — позначити виконані завдання як `[x]`, додати до секції "✅ ЗАВЕРШЕНО"
2. ✅ **CHANGELOG.md** — додати детальний запис у секцію `[Unreleased]`

**Алгоритм:**

```
CLAUDE.md Change Log оновлено
        ↓
TODO.md: [ ] → [x] для виконаних завдань
        ↓
TODO.md: Додати версію до "✅ ЗАВЕРШЕНО"
        ↓
CHANGELOG.md: Додати запис до [Unreleased]
        ↓
Коміт всіх 3 файлів разом
```

**ЗАБОРОНЕНО:**

- ❌ Оновити CLAUDE.md без оновлення TODO.md
- ❌ Забути CHANGELOG.md при значних змінах
- ❌ Комітити документацію частинами (все разом!)

---

### ⚠️ ПОРУШЕННЯ ЦІЄЇ ВИМОГИ = КРИТИЧНА ПОМИЛКА!

**Якщо LLM:**

- ❌ Пропустив Master Index
- ❌ Прочитав всі файли без необхідності
- ❌ Не використав таблицю маршрутизації
- ❌ Не перевірив Checklist

**Результат:**

- 🔴 Марнування токенів (3000+ токенів замість 500-1000)
- 🔴 Повільний response
- 🔴 Можлива помилка через перевантаження контексту
- 🔴 Неефективна робота LLM

**ПРАВИЛЬНА РОБОТА LLM:**

- ✅ Економія токенів (60-70% менше)
- ✅ Швидший response
- ✅ Точна інформація з потрібного файлу
- ✅ Ефективний workflow

---

## ⚡ ШВИДКІ ДОВІДНИКИ ДЛЯ ULTRA-FAST ДОСТУПУ

### 📝 docs/guides/QUICK-REFERENCE.md (30 СЕКУНД)

**Призначення:** Ультра-короткий довідник для миттєвої перевірки критичних правил.

**Коли використовувати:**

- ✅ Для простих завдань (button, card, section)
- ✅ Коли потрібно швидко перевірити формат (UniqueId, CSS Vars)
- ✅ Перед створенням коду для миттєвого нагадування
- ✅ Замість повного CODING-RULES для тривіальних задач

**Що містить:**

- 🔴 ТОП-10 критичних правил (таблиця)
- 🔧 Генератори коду (UniqueId: JS/Python/Bash)
- 📋 Формати для Copy-Paste (CSS Vars, Hover, Section, PHP)
- 🚫 ТОП-10 найчастіших помилок
- ✅ 5-секундний checklist
- 📖 Маршрутизація файлів (скорочена таблиця)
- ⚡ Ultra-Quick Tips

**Економія токенів:** 80-90% для простих завдань

**Приклад використання:**

```
Користувач: "Створи простий button"

LLM думає:
1. ✅ Read: docs/guides/QUICK-REFERENCE.md (193 рядки, 30 секунд)
2. ✅ Перевіряю ТОП-10 правил
3. ✅ Copy-paste UniqueId generator
4. ✅ Copy-paste Hover Effect format
5. ✅ Створюю код (економія 1200+ токенів!)
```

**ВАЖЛИВО:**

- НЕ використовуй для складних завдань (Query Block Pro, Dynamic Tags API, WooCommerce)
- Для складних завдань завжди читай повний CODING-RULES файл
- docs/guides/QUICK-REFERENCE.md - це доповнення, а не заміна CODING-RULES

---

### ✅ docs/guides/CHECKLIST.md (ДЕТАЛЬНА ПЕРЕВІРКА)

**Призначення:** Повний checklist для всіх етапів розробки з 100+ пунктами перевірки.

**Коли використовувати:**

- ✅ Перед написанням коду (Pre-Code Checklist)
- ✅ Під час розробки (Coding Checklist)
- ✅ Перед коммітом (Pre-Commit Checklist - 40+ пунктів)
- ✅ При debug (Debug Checklist)
- ✅ Для планування workflow (10-step process)

**Що містить:**

1. **⚡ Швидкий Checklist (5 секунд)**
   - UniqueId, CSS Vars, Responsive, Security, Типізація

2. **📝 Pre-Code Checklist**
   - Документація (4 пункти)
   - Структура завдання (4 пункти)
   - Безпека (4 пункти)

3. **🔨 Coding Checklist**
   - GenerateBlocks JSON (40+ пунктів)
     - UniqueId (5 пунктів)
     - CSS Variables (3 пункти)
     - Responsive (4 пункти)
     - Visual Effects (2 пункти)
     - Global Classes (3 пункти)
     - Overlay (3 пункти)
     - Two-Level Section (3 пункти)
     - Attribute Order (4 пункти)
   - PHP Code (20+ пунктів)
   - CSS/Styles (4 пункти)
   - JavaScript (4 пункти)

4. **🚀 Pre-Commit Checklist (КРИТИЧНО!)**
   - GenerateBlocks (10 пунктів)
   - PHP Code (10 пунктів)
   - Performance (8 пунктів)
   - Security (9 пунктів)
   - Documentation (5 пунктів)
   - Testing (7 пунктів)
   - Git (5 пунктів)

5. **🐛 Debug Checklist**
   - GenerateBlocks Issues (4 підсекції)
   - PHP Issues (3 підсекції)
   - Performance Issues (2 підсекції)

6. **📊 Workflow Checklist (10 кроків)**

**Приклад використання:**

```
LLM перед коммітом:
1. ✅ Read: docs/guides/CHECKLIST.md
2. ✅ Виконую Pre-Commit Checklist (40+ пунктів)
3. ✅ Перевіряю UniqueId у hex форматі
4. ✅ Перевіряю CSS Variables екрановані
5. ✅ Перевіряю Security: esc_html, sanitize
6. ✅ Перевіряю Performance: lazy loading, width/height
7. ✅ Всі пункти виконані - готовий до коммі ту
```

**КРИТИЧНО ВАЖЛИВО:**

- ЗАВЖДИ використовуй Pre-Commit Checklist перед коммітом
- НЕ пропускай жодного пункту з Security секції
- Перевіряй UniqueId та CSS Variables в кожному блоці

---

### 🎯 СТРАТЕГІЯ ВИКОРИСТАННЯ ДОВІДНИКІВ

**Для простих завдань (1-3 хвилини):**

```
1. Read: docs/guides/QUICK-REFERENCE.md (30 секунд)
2. Створи код
3. Перевір 5-секундний checklist
```

**Для середніх завдань (5-15 хвилин):**

```
1. Read: CODING-RULES.md (Master Index)
2. Read: Відповідний CODING-RULES файл (CORE/ADVANCED/WORDPRESS)
3. Створи код
4. Read: docs/guides/CHECKLIST.md (Pre-Commit section)
```

**Для складних завдань (15+ хвилин):**

```
1. Read: CODING-RULES.md (Master Index)
2. Read: 1-2 CODING-RULES файли згідно таблиці маршрутизації
3. Створи код
4. Read: docs/guides/CHECKLIST.md (повний Coding Checklist)
5. Read: docs/guides/CHECKLIST.md (Pre-Commit Checklist)
6. Debug якщо потрібно (Debug Checklist)
```

**Метадані (.meta.json файли):**

Для LLM доступні метадані файли:

- `docs/coding-rules/CODING-RULES-CORE.meta.json` - структура CORE файлу
- `docs/coding-rules/CODING-RULES-ADVANCED.meta.json` - структура ADVANCED файлу
- `docs/coding-rules/CODING-RULES-WORDPRESS.meta.json` - структура WORDPRESS файлу

Ці файли містять:

- Розмір файлу (lines)
- Карту секцій з номерами рядків
- Рівень критичності кожної секції
- Ключові слова
- Use cases
- Залежності

**Використання метаданих:**

```
LLM може прочитати .meta.json для:
- Швидкого визначення де знаходиться потрібна секція
- Оцінки складності завдання
- Визначення залежностей між файлами
- Пошуку по ключовим словам
```

---

### 📁 ПОЛІТИКА СТВОРЕННЯ ФАЙЛІВ

**ЗАГАЛЬНЕ ПРАВИЛО ДЛЯ ПРОЄКТУ "MEDICI":**

⚠️ **НЕ СТВОРЮВАТИ НОВІ ФАЙЛИ БЕЗ КРИТИЧНОЇ НЕОБХІДНОСТІ!**

🚫 **ЗАБОРОНЕНО ВИТРАЧАТИ ТОКЕНИ НА НЕПОТРІБНІ ФАЙЛИ!**

**Коли можна створювати нові файли:**

- ✅ Користувач **прямо та явно** попросив створити новий файл
- ✅ **Абсолютно неможливо** виконати завдання без створення нового файлу
- ✅ Створення окремого файлу **критично важливе** для безпеки або продуктивності

**КАТЕГОРИЧНО ЗАБОРОНЕНО:**

- ❌ **НЕ створювати "додаткові" або "допоміжні" файли** навіть якщо здається що це допоможе
- ❌ **НЕ створювати файли "для зручності"** користувача без прямого запиту
- ❌ **НЕ створювати документацію** (.md, .txt, README) без явного запиту
- ❌ **НЕ створювати приклади коду** в окремих файлах
- ❌ **НЕ створювати конфігураційні файли** якщо є існуючі
- ❌ **НЕ витрачати токени** на файли які користувач не просив

**В решті випадків:**

- ❌ НЕ створювати нові PHP модулі - використовувати існуючі файли в `inc/`
- ❌ НЕ створювати нові CSS файли - використовувати `style.css`
- ❌ НЕ створювати нові JS файли - використовувати `js/scripts.js`
- ❌ НЕ створювати нові шаблони - використовувати існуючі template files
- ❌ НЕ створювати нові GeneratePress Elements - використовувати існуючі в `gutenberg/`
- ❌ НЕ створювати markdown документацію (.md) без прямого запиту

**Що робити замість створення:**

- ✅ Додавати код до існуючих файлів (functions.php, style.css, тощо)
- ✅ Об'єднувати функціонал з існуючими модулями
- ✅ Розширювати існуючі класи та функції
- ✅ Використовувати WordPress hooks для додавання функціоналу
- ✅ Додавати CSS правила до style.css з коментарями-секціями
- ✅ Пояснювати код у відповіді замість створення файлів-прикладів

**Мета:**

- Підтримувати проєкт чистим
- Уникати роздування файлової структури
- **Економити токени відповіді**
- Полегшити підтримку коду
- Робити тільки те, що просить користувач

### 📏 ПОЛІТИКА ОНОВЛЕННЯ ДОКУМЕНТАЦІЇ

**🚨 КРИТИЧНО: ЛІМІТ ДОКУМЕНТАЦІЇ В CLAUDE.md**

⚠️ **ЗАБОРОНЕНО створювати 600+ рядків у CLAUDE.md!**

**Максимальний розмір нових секцій:**

- ✅ **40-80 рядків** для звичайних секцій
- ✅ **80-150 рядків** для критично важливих features
- ❌ **600+ рядків** - категорично заборонено!

**Що ОБОВ'ЯЗКОВО включати:**

- ✅ API references (функції з type hints)
- ✅ Function signatures
- ✅ Code examples (minimal, technical)
- ✅ Technical features list
- ✅ Файли змінені та версії

**Що ЗАБОРОНЕНО включати:**

- ❌ User guides (використання для користувачів)
- ❌ Детальні приклади з поясненнями
- ❌ Troubleshooting (debugging steps)
- ❌ Дублювання інформації з інших файлів
- ❌ Теоретичні пояснення та концепції

**Орієнтація документації:**

- 🤖 **Тільки для LLM** (AI assistants)
- 🎯 **Максимум технічної інформації**
- ⚡ **Мінімум слів, максимум коду**

**Мета:** Економити токени, підтримувати документацію компактною та технічною.

---

## 🚨 КРИТИЧНІ ПРАВИЛА ДЛЯ CSS ФАЙЛІВ

### ⚠️ ОБОВ'ЯЗКОВІ ВИМОГИ ПРИ РОБОТІ З CSS

**ЗАБОРОНЕНО - ЦЕ ЛАМАЄ ПАРСИНГ CSS!**

❌ **НЕ залишати незакриті фігурні дужки** - це критична помилка!
❌ **НЕ видаляти закриваючі `}` при редагуванні** - завжди перевіряй баланс
❌ **НЕ копіювати CSS без перевірки синтаксису** - перевір кожен блок
❌ **НЕ змішувати стилі з різних компонентів** без закриття попередніх блоків
❌ **НЕ видаляти media queries без закриття всіх вкладених правил**

**ОБОВ'ЯЗКОВО - ЦЕ ГАРАНТУЄ СТАБІЛЬНІСТЬ!**

✅ **ЗАВЖДИ перевіряй баланс `{` та `}` після редагування**
✅ **ЗАВЖДИ закривай media queries коректно**
✅ **ЗАВЖДИ закривай keyframe animations** (`@keyframes`)
✅ **ЗАВЖДИ закривай псевдокласи** (`:hover`, `:focus`, `:before`, `:after`)
✅ **ЗАВЖДИ закривай вкладені селектори** (`[data-theme="dark"]`, `:root`)
✅ **ЗАВЖДИ перевіряй CSS перед комітом** - використовуй валідатор

### 📋 CHECKLIST ПЕРЕД РЕДАГУВАННЯМ CSS

**1. Перед зміною:**

```bash
# Перевір баланс дужок (має бути однаково)
grep -c "{" css/file.css && grep -c "}" css/file.css
```

**2. Під час редагування:**

- ✅ Відкрив `{` → відразу додай закриваючу `}` (навіть якщо вона буде далі)
- ✅ Видаляєш блок → видали ВСЕ (і `{` і `}`)
- ✅ Копіюєш код → перевір чи всі дужки на місці
- ✅ Media query → перевір вкладеність селекторів

**3. Після редагування:**

```bash
# Перевір баланс знову
grep -c "{" css/file.css && grep -c "}" css/file.css

# Перевір CSS валідатором (опціонально)
npx stylelint css/file.css
```

### 🛡️ КРИТИЧНІ ФАЙЛИ CSS (ПІДВИЩЕНА УВАГА!)

**Файли, де помилки НАЙБІЛЬШ критичні:**

1. **`css/core/variables.css`** - CSS змінні (`:root`, `[data-theme="dark"]`)
   - ⚠️ Незакритий `:root` → весь сайт без змінних!
   - ⚠️ Незакритий `[data-theme="dark"]` → темна тема зламана!

2. **`css/components/navigation.css`** - Fixed header
   - ⚠️ Незакриті media queries → навігація зламана на mobile!
   - ⚠️ Незакриті keyframes → анімація не працює!

3. **`css/components/cards.css`** - Картки (7 типів)
   - ⚠️ Незакриті hover states → картки не реагують на hover!

4. **`css/layout/layout.css`** - Grid, Hero, Footer
   - ⚠️ Незакритий footer → весь footer зламаний!
   - ⚠️ Незакриті media queries → responsive не працює!

5. **`css/modules/blog/blog-single.css`** - Single post (832 рядки)
   - ⚠️ Найбільший файл → найбільше ризиків!
   - ⚠️ Багато вкладених блоків → уважно перевіряй!

### 🔍 ТИПОВІ ПОМИЛКИ (УНИКАЙ!)

**Помилка #1: Видалення закриваючої дужки при refactoring**

```css
/* ❌ НЕПРАВИЛЬНО */
.card {
    background: white;
    /* видалив декілька рядків і забув закрити блок */

.another-card {
    /* помилка парсингу! */
}

/* ✅ ПРАВИЛЬНО */
.card {
    background: white;
} /* ЗАВЖДИ закривай блок! */

.another-card {
    background: gray;
}
```

**Помилка #2: Незакритий media query**

```css
/* ❌ НЕПРАВИЛЬНО */
@media (max-width: 767px) {
    .element {
        padding: 1rem;
    }
/* забув закрити media query! */

/* ✅ ПРАВИЛЬНО */
@media (max-width: 767px) {
    .element {
        padding: 1rem;
    }
} /* ЗАВЖДИ закривай media query! */
```

**Помилка #3: Незакритий псевдоклас з вкладеністю**

```css
/* ❌ НЕПРАВИЛЬНО */
[data-theme="dark"] {
    --bg: #0f172a;
    .card {
        background: var(--bg);
    }
/* забув закрити [data-theme="dark"]! */

/* ✅ ПРАВИЛЬНО */
[data-theme="dark"] {
    --bg: #0f172a;
}

[data-theme="dark"] .card {
    background: var(--bg);
}
```

**Помилка #4: Незакритий keyframe animation**

```css
/* ❌ НЕПРАВИЛЬНО */
@keyframes slideDown {
    from {
        opacity: 0;
    }
    to {
        opacity: 1;
    }
/* забув закрити @keyframes! */

/* ✅ ПРАВИЛЬНО */
@keyframes slideDown {
    from {
        opacity: 0;
    }
    to {
        opacity: 1;
    }
} /* ЗАВЖДИ закривай @keyframes! */
```

### 🎯 ЗОЛОТЕ ПРАВИЛО CSS

**Кожна відкриваюча `{` МАЄ мати закриваючу `}`**

```
Відкриваючих { = Закриваючих }
```

**Перевірка балансу:**

```bash
# Має вивести однакові числа!
grep -c "{" css/file.css  # 42
grep -c "}" css/file.css  # 42
```

Якщо числа різні → є помилка!

### 📊 РЕКОМЕНДОВАНІ ІНСТРУМЕНТИ

**1. VS Code Extensions:**

- CSS Peek - швидкий перегляд CSS
- Stylelint - автоматична перевірка синтаксису
- Bracket Pair Colorizer - візуалізація парних дужок

**2. CLI Tools:**

```bash
# Stylelint (встановити глобально)
npm install -g stylelint stylelint-config-standard

# Перевірка файлу
stylelint css/**/*.css
```

**3. Git Pre-commit Hook (опціонально):**

```bash
#!/bin/bash
# Перевірка балансу дужок перед комітом
for file in css/**/*.css; do
    open=$(grep -c "{" "$file")
    close=$(grep -c "}" "$file")
    if [ "$open" != "$close" ]; then
        echo "❌ CSS Error in $file: { = $open, } = $close"
        exit 1
    fi
done
```

### ⚡ ЩО РОБИТИ ЯКЩО CSS ЗЛАМАВСЯ?

**Крок 1: Знайди файл з помилкою**

```bash
# Перевір баланс у всіх CSS файлах
for file in css/**/*.css; do
    echo "=== $file ==="
    grep -c "{" "$file"
    grep -c "}" "$file"
done
```

**Крок 2: Знайди незакриту дужку**

- Відкрий файл у VS Code
- Встанови курсор на `{` і подивись чи є парна `}`
- Використовуй "Go to Bracket" (Ctrl+Shift+\)

**Крок 3: Виправ помилку**

- Додай закриваючу `}` у потрібному місці
- Перевір баланс знову

**Крок 4: Протестуй**

- Перезавантаж сторінку (Ctrl+F5)
- Перевір чи стилі застосувались
- Перевір Dark Theme (якщо є)
- Перевір Responsive (mobile, tablet)

**ВАЖЛИВО:** Після виправлення CSS помилок завжди перевіряй:

- ✅ Головну сторінку
- ✅ Блог сторінку
- ✅ Single post
- ✅ Footer
- ✅ Navigation (mobile menu)
- ✅ Dark theme toggle
- ✅ Всі responsive breakpoints (767px, 1024px)

---

## Project Overview

**Medici Medical Marketing Theme** is a WordPress child theme built on GeneratePress Premium, specifically designed for medical marketing agencies. The theme emphasizes performance, security, and SEO optimization with custom Gutenberg blocks and advanced blog functionality.

### Key Information

- **Theme Type**: WordPress Child Theme
- **Parent Theme**: GeneratePress Premium (commercial version)
- **Block Editor**: GenerateBlocks Pro 2.0+ (commercial version)
- **Version**: 1.3.5
- **Text Domain**: medici.agency
- **Primary Language**: Ukrainian (with multilingual support)
- **Target Industry**: Medical Marketing
- **Site Structure**:
  - **Agency Home**: https://www.medici.agency (Головна сторінка Агенції)
  - **Blog Home**: https://www.medici.agency/blog (Головна сторінка Блогу)

### AI Assistant Language Requirements

**CRITICAL RULE - MANDATORY FOR ALL AI ASSISTANTS:**

🇺🇦 **Ukrainian Language Only** 🇺🇦

All communication, explanations, reasoning, and responses MUST be in **Ukrainian language exclusively**.

**Prohibited languages:**

- ❌ English
- ❌ German
- ❌ Polish
- ❌ Russian
- ❌ Any other language

**Required:**

- ✅ Ukrainian only (Українська мова)

**This applies to:**

- All responses to the user
- All explanations and reasoning
- All commit messages
- All code comments when adding new code
- All documentation updates
- All error messages and warnings
- All summaries and reports

**Exceptions:**

- Existing code comments in English may remain unchanged unless being refactored
- Technical terms and function names (e.g., `function_name`, `WordPress`, `PHP`)
- URLs and file paths

**Purpose:** This project is for a Ukrainian medical marketing agency. Maintaining Ukrainian language throughout ensures consistency and cultural alignment.

## Architecture & File Structure

```
medici/
├── functions.php          # Main theme functions and hooks (module loader) v2.0.0
├── style.css              # Theme metadata + base styles (v1.4.0)
├── theme.json             # WordPress Global Styles (colors, typography, spacing)
├── single-medici_blog.php # Single blog post template (TOC sidebar)
├── archive-medici_blog.php # Blog archive template
│
├── inc/                   # PHP modules (62 файли)
│   │
│   │ # === OOP Modules (v2.0.0) ===
│   │
│   ├── blog/              # Blog Module - Repository + Service Pattern
│   │   ├── BlogPostRepository.php   # Data access abstraction (416 рядків)
│   │   ├── ReadingTimeService.php   # Reading time calculation (206 рядків)
│   │   ├── PostViewsService.php     # Atomic view counting (324 рядки)
│   │   └── bootstrap.php            # Module initialization
│   │
│   ├── events/            # Events Module - Event Dispatcher + Observer
│   │   ├── EventInterface.php       # Event contract
│   │   ├── AbstractEvent.php        # Base event class
│   │   ├── EventDispatcher.php      # Central event bus (Singleton)
│   │   ├── ObserverInterface.php    # Observer contract
│   │   ├── ConsultationRequestEvent.php # Form submission event
│   │   ├── NewsletterSubscribeEvent.php # Newsletter event
│   │   ├── bootstrap.php            # Module initialization
│   │   └── observers/               # Event observers
│   │       ├── LoggingObserver.php        # Database logging
│   │       ├── LeadCreationObserver.php   # Lead CPT creation
│   │       ├── IntegrationObserver.php    # Email/Telegram/Sheets
│   │       └── WebhookObserver.php        # Webhook notifications
│   │
│   ├── lead/              # Lead Module - Adapter Pattern
│   │   ├── IntegrationInterface.php # Contract for integrations
│   │   ├── AbstractIntegration.php  # Base class with error handling
│   │   ├── EmailAdapter.php         # HTML email notifications
│   │   ├── TelegramAdapter.php      # Telegram Bot API
│   │   ├── GoogleSheetsAdapter.php  # Apps Script integration
│   │   ├── IntegrationManager.php   # Orchestrates adapters
│   │   └── bootstrap.php            # Module initialization
│   │
│   ├── exit-intent/       # Exit-Intent Module - WordPress Plugin Boilerplate
│   │   ├── class-exit-intent.php         # Main bootstrap class
│   │   ├── class-exit-intent-loader.php  # Hook registry
│   │   ├── class-exit-intent-assets.php  # Asset management
│   │   └── class-exit-intent-public.php  # Frontend functionality
│   │
│   │ # === Core Modules ===
│   │
│   ├── theme-setup.php              # Theme support & features
│   ├── generatepress.php            # GeneratePress customization
│   ├── assets.php                   # Asset management & enqueue
│   ├── performance.php              # Performance optimizations
│   ├── security.php                 # Security hardening (CSP, headers)
│   ├── class-events.php             # Legacy Events API (backwards compat)
│   ├── class-cache-manager.php      # Object caching with Transients
│   ├── database-optimization.php    # Database indexes
│   │
│   │ # === Lead Management ===
│   │
│   ├── lead-cpt.php                 # Lead Custom Post Type
│   ├── lead-integrations.php        # Legacy integrations (backwards compat)
│   ├── lead-scoring.php             # Lead scoring system
│   ├── lead-validation.php          # Lead validation
│   ├── lead-admin-settings.php      # Admin settings page
│   │
│   │ # === Blog System ===
│   │
│   ├── blog-cpt.php                 # Custom Post Type 'medici_blog'
│   ├── blog-meta-fields.php         # Meta fields (reading time, views)
│   ├── blog-cache.php               # Blog caching
│   ├── blog-category-color.php      # Category color picker
│   ├── blog-admin-settings.php      # Admin settings page
│   ├── blog-shortcodes.php          # [medici_warning], [medici_cta], [medici_takeaways]
│   ├── blog-sidebar-settings.php    # Sidebar configuration
│   ├── blog-relevant-services.php   # Services mapping
│   ├── blog-toc.php                 # Table of Contents
│   │
│   │ # === Analytics & Integrations ===
│   │
│   ├── analytics.php                # GA4 Events tracking
│   ├── dashboard-analytics.php      # Dashboard widgets, Lead Scoring widget
│   ├── rest-api.php                 # REST API endpoints
│   ├── zapier-integration.php       # Zapier webhooks
│   ├── class-webhook-sender.php     # Webhook sender
│   ├── webhook-settings.php         # Webhook settings
│   │
│   │ # === Other ===
│   │
│   ├── twemoji-local.php            # Local Twemoji (4009 SVG)
│   ├── schema-medical.php           # Schema.org (Organization, FAQ, HowTo, Video)
│   ├── sitemap-optimization.php     # XML Sitemap optimization
│   ├── transliteration.php          # Cyrillic → Latin slugs
│   ├── smtp-config.php              # SMTP email configuration
│   ├── theme-settings.php           # Theme settings page
│   │
│   └── widgets/                     # Custom widgets
│       ├── widgets-init.php         # Widgets registration
│       └── class-popular-posts-widget.php
│
├── css/                   # Модульна CSS архітектура (ITCSS)
│   ├── critical.css       # Critical CSS (inline in <head>)
│   ├── core/              # Базові стилі
│   │   ├── variables.css  # CSS змінні (Utopia Typography)
│   │   └── core.css       # Reset, fonts, base
│   ├── components/        # Компоненти
│   │   ├── navigation.css # Header, menu, mobile menu
│   │   ├── buttons.css    # Primary, Secondary, CTA buttons
│   │   ├── cards.css      # Service, Team, Value cards
│   │   ├── sections.css   # Section wrappers
│   │   ├── forms.css      # Form styling
│   │   └── faq.css        # FAQ accordion
│   ├── layout/            # Структура сторінки
│   │   └── layout.css     # Grid, hero, footer
│   ├── modules/blog/      # Blog модуль
│   │   ├── blog-new.css   # Blog archive (948 lines)
│   │   └── blog-single.css # Single post (915 lines)
│   └── admin/
│       └── admin.css      # Blog admin dashboard
│
├── js/                    # Frontend JavaScript (6 файлів)
│   ├── scripts.js         # Main frontend (ThemeModule, MobileMenu, Navigation)
│   ├── events.js          # Events API client
│   ├── forms-consultation.js # Consultation form
│   ├── forms-newsletter.js # Newsletter subscription
│   ├── faq-accordion.js   # FAQ toggle
│   ├── modules/blog/      # Blog JavaScript
│   │   ├── blog-single.js # TOC, scroll spy, progress bar
│   │   └── blog-new.js    # Filtering, sorting, AJAX load more
│   └── twemoji/
│       └── twemoji.min.js # Twemoji library 15.1.0 (18KB)
│
├── gutenberg/             # GeneratePress Elements HTML (10 файлів)
│   ├── HEADER.html        # Site header
│   ├── HERO.html          # Hero section
│   ├── SERVICES-1.html    # Services block 1
│   ├── SERVICES-2.html    # Services block 2
│   ├── TEAM.html          # Team section
│   ├── TEAM_FIXED.html    # Team (fixed version)
│   ├── FAQ.html           # FAQ section
│   ├── FEEDBACK.html      # Testimonials
│   ├── FOOTER.html        # Site footer
│   └── CONSULTATION-FORM.html # Consultation form (Hook Element)
│
├── templates/             # HTML Templates
│   ├── consultation-form.html
│   └── newsletter-form.html
│
├── assets/twemoji/svg/    # Twemoji SVG files (4009 emoji, 11MB)
├── fonts/                 # Local Montserrat fonts (WOFF/WOFF2)
├── img/                   # Theme images
└── scripts/               # Utility scripts
```

**⚠️ ВАЖЛИВІ ПРИМІТКИ:**

1. **PHP модулі завантажуються через functions.php (v2.0.0):**
   - Порядок завантаження визначений у `$priority_modules` масиві
   - OOP модулі завантажуються через `bootstrap.php` файли
   - Events API ініціалізується на `init` hook (priority 5)

2. **OOP Architecture (v2.0.0) - 3 модульні директорії:**
   - ✅ `inc/blog/` - Repository + Service Pattern (BlogPostRepository, ReadingTimeService, PostViewsService)
   - ✅ `inc/events/` - Event Dispatcher + Observer Pattern (EventDispatcher, 4 observers)
   - ✅ `inc/lead/` - Adapter Pattern (IntegrationManager + 3 adapters)
   - ✅ `inc/exit-intent/` - WordPress Plugin Boilerplate (4 класи)

3. **Blog система (Legacy + OOP):**
   - **Legacy модулі:** blog-cpt.php, blog-meta-fields.php, blog-category-color.php, blog-shortcodes.php
   - **OOP модулі:** BlogPostRepository, ReadingTimeService, PostViewsService
   - Backwards compatibility забезпечена

4. **Events система (v2.0.0):**
   - `EventDispatcher` - central event bus з priority support
   - `ConsultationRequestEvent`, `NewsletterSubscribeEvent` - concrete events
   - 4 observers: Logging, LeadCreation, Integration, Webhook

5. **Lead система (v2.0.0):**
   - `IntegrationManager` - orchestrates all adapters
   - `EmailAdapter`, `TelegramAdapter`, `GoogleSheetsAdapter` - notification channels
   - Legacy `lead-integrations.php` для backwards compatibility

6. **CSS архітектура (ITCSS):**
   - `critical.css` - inline в `<head>` для FCP
   - `core/` - змінні, reset, base
   - `components/` - buttons, cards, navigation, forms, faq
   - `layout/` - grid, hero, footer
   - `modules/blog/` - blog-new.css, blog-single.css

7. **gutenberg/** - GeneratePress Elements:
   - 10 HTML файлів з GenerateBlocks markup
   - Додаються на сторінки через **GeneratePress > Elements**

8. **JavaScript архітектура:**
   - `scripts.js` - головний файл (ThemeModule, MobileMenu, Navigation)
   - `js/modules/blog/` - blog-specific JS (TOC, filtering, AJAX)
   - `events.js` - Events API client
   - `forms-*.js` - форми консультації та newsletter
   - `analytics.js` - GA4 Events tracking

---

## 🏗️ GeneratePress Elements - Основна Архітектура Сайту

### ⚠️ КРИТИЧНО ВАЖЛИВА ІНФОРМАЦІЯ!

**Весь контент сайту створюється через GeneratePress Elements!**

### Як працює архітектура:

#### **GeneratePress Elements**

- 📁 Розташування: `gutenberg/` директорія
- 🎯 Призначення: Весь контент сайту (Header, Hero, Services, Footer, тощо)
- 🛠️ Інтерфейс: **GeneratePress > Elements** в WordPress адмін панелі
- ✅ Використовується для: Навігація, секції головної, footer, всі основні блоки

**Типи Elements:**

```
Header Elements  → Навігація, logo, mobile menu
Layout Elements  → Hero секції, Services, Testimonials, Footer
Hook Elements    → Додатковий контент в WordPress hooks
```

**Workflow створення контенту:**

1. Створи HTML/JSON код з GenerateBlocks markup в `gutenberg/`
2. Створи новий Element через **GeneratePress > Elements**
3. Обери тип (Header, Layout, Hook)
4. Вставити код з `gutenberg/` файлу
5. Налаштуй Display Rules (де показувати)
6. Опублікуй Element

**Всі типи контенту створюються в GeneratePress Elements:**

| Тип контенту         | Де створювати | Як додавати         |
| -------------------- | ------------- | ------------------- |
| Навігація, Header    | `gutenberg/`  | Header Element      |
| Hero, Services, Team | `gutenberg/`  | Layout Element      |
| Footer               | `gutenberg/`  | Layout Element      |
| Додатковий контент   | `gutenberg/`  | Hook Element        |
| Блог контент         | `gutenberg/`  | Layout/Hook Element |

### Переваги GeneratePress Elements:

✅ **Display Rules** - показувати на конкретних сторінках
✅ **Device Visibility** - приховати на mobile/tablet/desktop
✅ **User State** - показувати залогіненим/незалогіненим
✅ **Query String** - показувати за URL параметрами
✅ **Priority Control** - порядок виконання hooks
✅ **Disable Elements** - вимкнути без видалення
✅ **Export/Import** - легко переносити між сайтами

### Важливі примітки для AI асистентів:

1. Вся навігація реалізована через **Header Element** (код в `gutenberg/`)
2. Якщо треба змінити навігацію - редагуй Header Element через **GeneratePress > Elements**
3. Весь контент (Header, Hero, Services, Footer, Blog) створюється як Elements
4. НЕ створюй нові HTML файли без необхідності - використовуй існуючі Elements

---

## Core Features

**PHP модулі теми (22 файлів в inc/):**

| #   | Модуль                  | Файл                       | Опис                                               |
| --- | ----------------------- | -------------------------- | -------------------------------------------------- |
| 1   | Theme Setup             | theme-setup.php            | Theme support, image sizes                         |
| 2   | GeneratePress           | generatepress.php          | GP customization, body classes                     |
| 3   | Assets                  | assets.php                 | CSS/JS enqueue, critical CSS                       |
| 4   | Performance             | performance.php            | Lazy loading, emoji disable                        |
| 5   | Security                | security.php               | XML-RPC disable, headers                           |
| 6   | Events API              | class-events.php           | Newsletter, consultation events                    |
| 7   | Lead CPT                | lead-cpt.php               | Custom Post Type 'medici_lead'                     |
| 8   | Lead Integrations       | lead-integrations.php      | Email, Telegram, Google Sheets                     |
| 9   | Lead Admin              | lead-admin-settings.php    | Integration settings page                          |
| 10  | **Dashboard Analytics** | dashboard-analytics.php    | Dashboard widgets, Lead/Blog analytics, SEO Audit  |
| 11  | **REST API**            | rest-api.php               | REST API endpoints для лідів та блогу              |
| 12  | Blog CPT                | blog-cpt.php               | Custom Post Type 'medici_blog'                     |
| 13  | Blog Meta               | blog-meta-fields.php       | Reading time, views, featured                      |
| 14  | Blog Category           | blog-category-color.php    | Color picker для категорій                         |
| 15  | Blog Settings           | blog-admin-settings.php    | Admin settings page                                |
| 16  | Blog Shortcodes         | blog-shortcodes.php        | [medici_warning], [medici_cta], [medici_takeaways] |
| 17  | Blog Sidebar            | blog-sidebar-settings.php  | Sidebar widgets configuration                      |
| 18  | Blog Services           | blog-relevant-services.php | Services mapping                                   |
| 19  | Twemoji Local           | twemoji-local.php          | 4009 SVG emoji локально                            |
| 20  | Schema                  | schema-medical.php         | Organization, FAQ, HowTo, Video schema             |
| 21  | Sitemap                 | sitemap-optimization.php   | XML Sitemap optimization                           |
| 22  | Transliteration         | transliteration.php        | Cyrillic → Latin slugs                             |

**JavaScript модулі (6 файлів в js/):**

| Файл                  | Опис                                              |
| --------------------- | ------------------------------------------------- |
| scripts.js            | ThemeModule, MobileMenu, Navigation, SmoothScroll |
| events.js             | Events API client                                 |
| forms-consultation.js | Consultation form validation                      |
| forms-newsletter.js   | Newsletter subscription                           |
| faq-accordion.js      | FAQ toggle                                        |
| modules/blog/\*.js    | Blog TOC, filtering, AJAX                         |

**Ключові функції:**

- `medici_calculate_reading_time()` - розрахунок часу читання
- `medici_get_blog_post_views()` - кількість переглядів
- `medici_get_category_color()` - колір категорії
- `medici_get_related_blog_posts()` - схожі статті
- `medici_build_organization_schema()` - Organization schema
- `medici_transliterate()` - транслітерація тексту
- `Lead_CPT::create_lead()` - створення ліда в CPT
- `Lead_Integrations::send_all()` - відправка всіх інтеграцій
- `Dashboard_Analytics::init()` - Dashboard widgets, analytics pages
- `REST_API::init()` - REST API endpoints

## Lead Management System

**Повноцінна CRM система для обробки лідів з форми консультації.**

### Архітектура Lead System

**1. Lead CPT (Custom Post Type):**

- Post Type: `medici_lead`
- Meta fields: name, email, phone, service, message, page*url, utm*\*, status
- Statuses: new, contacted, qualified, closed, lost
- Admin columns: Name, Email, Phone, Service, Status, Date
- Sortable columns: Name, Email, Service, Status
- Meta boxes: Lead details, UTM parameters, Status selector

**2. Lead Integrations:**

- **Email:** Відправка HTML email адміністратору з деталями ліда
- **Telegram:** Markdown повідомлення в Telegram через Bot API
- **Google Sheets:** Додавання рядка в Google Sheets через Apps Script Web App

**3. Admin Settings:**

- Settings page: **Ліди > Інтеграції**
- Fields: Admin Email, Telegram Bot Token, Telegram Chat ID, Google Sheets URL
- Setup instructions для Telegram та Google Sheets
- Automatic integration triggering при новому ліді

### Workflow Lead Processing

```
1. User submits consultation form
   ↓
2. JavaScript (forms-consultation.js) validates fields
   ↓
3. Events API (mediciEvents.requestConsultation) sends AJAX
   ↓
4. Events::handle_ajax() validates + sanitizes data
   ↓
5. Events::log_event() logs to wp_medici_events table
   ↓
6. Lead_CPT::create_lead() creates post in medici_lead CPT
   ↓
7. Lead_Integrations::send_all() triggers:
   - Email notification to admin
   - Telegram message to chat
   - Google Sheets row append
   ↓
8. User receives success message
```

### API Methods

**Lead_CPT class:**

```php
Lead_CPT::create_lead( array $data ): int
// Creates lead post, returns post ID

Lead_CPT::init(): void
// Initializes CPT hooks
```

**Lead_Integrations class:**

```php
Lead_Integrations::send_all( array $data, int $lead_id ): void
// Sends all integrations (Email, Telegram, Sheets)

Lead_Integrations::send_email_notification( array $data, int $lead_id ): bool
// Sends HTML email to admin

Lead_Integrations::send_to_telegram( array $data, int $lead_id ): bool
// Sends Markdown message to Telegram

Lead_Integrations::send_to_google_sheets( array $data, int $lead_id ): bool
// Appends row to Google Sheets via Apps Script

// Getters/Setters
Lead_Integrations::get_admin_email(): string
Lead_Integrations::get_telegram_bot_token(): string
Lead_Integrations::get_telegram_chat_id(): string
Lead_Integrations::get_google_sheet_url(): string
Lead_Integrations::update_admin_email( string $email ): bool
Lead_Integrations::update_telegram_bot_token( string $token ): bool
Lead_Integrations::update_telegram_chat_id( string $chat_id ): bool
Lead_Integrations::update_google_sheet_url( string $url ): bool
```

**Lead_Admin_Settings class:**

```php
Lead_Admin_Settings::init(): void
// Initializes admin settings page and fields
```

### Admin Interface

**Lead Post Type:**

- Menu: **Ліди** (dashicons-email)
- Submenu: **Всі ліди**, **Інтеграції**
- Cannot create manually (only via form submission)
- Export enabled

**Lead Edit Screen:**

- Meta box: **Деталі ліда** (name, email, phone, service, message, page_url)
- Meta box: **UTM параметри** (source, medium, campaign, term, content)
- Meta box: **Статус ліда** (dropdown: new/contacted/qualified/closed/lost)
- Auto-generated title: "Лід #{ID} - {Name}"

**Settings Page:**

- Path: **Ліди > Інтеграції**
- Sections:
  1. Email сповіщення (admin email field)
  2. Telegram інтеграція (bot token, chat ID)
  3. Google Sheets інтеграція (Apps Script URL)
- Setup instructions with code examples

### Form Integration

**Frontend Form:**

- File: `gutenberg/CONSULTATION-FORM.html`
- Type: GeneratePress Hook Element
- Fields: name*, email*, phone*, service, message, consent*
- JavaScript: `js/forms-consultation.js`
- CSS: `css/components/forms.css`
- Events API: `mediciEvents.requestConsultation()`

**Required Fields:**

- Name (autocomplete: name)
- Email (autocomplete: email, validation: email)
- Phone (autocomplete: tel, validation: phone)
- Consent checkbox (required)

**Optional Fields:**

- Service (select: smm, seo, advertising, branding, consultation, other)
- Message (textarea, autogrowing)

### Security & Validation

**Client-side (JavaScript):**

- Email regex validation
- Phone regex validation (10+ digits)
- Consent checkbox required
- Field focus on error

**Server-side (PHP):**

- Nonce verification (`check_ajax_referer`)
- Email validation (`is_email`)
- Sanitization: `sanitize_text_field`, `sanitize_email`, `sanitize_textarea_field`
- Escaping: `esc_html`, `esc_url`, `esc_attr`

### Integration Setup

**Telegram Bot:**

1. Create bot via @BotFather
2. Get Bot Token
3. Add bot to group or send private message
4. Get Chat ID via @getidsbot
5. Save in **Ліди > Інтеграції**

**Google Sheets:**

1. Create new Google Sheet
2. Add columns: lead_id, date, name, email, phone, service, message, page_url, utm_source, utm_medium, utm_campaign, status
3. Create Apps Script (Tools → Script editor)
4. Deploy as Web App (Execute as: Me, Access: Anyone)
5. Copy Web App URL to **Ліди > Інтеграції**

**Apps Script Code:**

```javascript
function doPost(e) {
	try {
		const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
		const data = JSON.parse(e.postData.contents);

		sheet.appendRow([
			data.lead_id || '',
			data.date || new Date().toISOString(),
			data.name || '',
			data.email || '',
			data.phone || '',
			data.service || '',
			data.message || '',
			data.page_url || '',
			data.utm_source || '',
			data.utm_medium || '',
			data.utm_campaign || '',
			data.status || 'new',
		]);

		return ContentService.createTextOutput(JSON.stringify({ success: true })).setMimeType(
			ContentService.MimeType.JSON
		);
	} catch (error) {
		return ContentService.createTextOutput(
			JSON.stringify({ success: false, error: error.toString() })
		).setMimeType(ContentService.MimeType.JSON);
	}
}
```

## WordPress Admin Interface

**Post Editor:**

- Meta boxes: Featured Post, Reading Time, Post Statistics
- Columns: Views, Reading Time, Featured, Categories
- Quick Edit: Featured checkbox, Reading time input

**Category Management:**

- Icon selector (11 emoji icons)
- Color picker integration

**Dashboard Widgets:**

- Blog Performance (total posts, views, recent activity)

## Development Guidelines

**File Management:**

- Не створювати нові файли без необхідності
- Додавати код до існуючих модулів
- Використовувати WordPress hooks

**Code Standards:**

- PHP: strict_types, type hints, WordPress Coding Standards
- Security: esc*\*(), sanitize*\*(), nonce verification
- Text domain: 'medici.agency'

**Version Management:**

- MEDICI_VERSION constant (functions.php)
- Version field (style.css header)
- CHANGELOG.md updates

---

## 🔴 ОБОВ'ЯЗКОВІ ПРАВИЛА РЕФАКТОРИНГУ

**⚠️ КРИТИЧНО ВАЖЛИВО:** При будь-якому рефакторингу коду теми Medici **ОБОВ'ЯЗКОВО** застосовувати ВСІ правила та рекомендації з цього документа.

### Перед рефакторингом — Checklist обов'язковий!

**1. Прочитай релевантну документацію:**

- ✅ Якщо рефакториш **PHP код** → прочитай:
  - **ADVANCED ARCHITECTURE PATTERNS** (DI Container, PSR-4, Repository Pattern)
  - **WORDPRESS SECURITY (CVE 2024-2025)** (Input validation, SQL injection, REST API security)
  - `docs/coding-rules/CODING-RULES-WORDPRESS.md` (WordPress Coding Standards, типізація)

- ✅ Якщо рефакториш **JavaScript** → прочитай:
  - **MODERN JAVASCRIPT & CSS ARCHITECTURE** (ES6+ modules, async/await)
  - Gutenberg Block Development (якщо плануєш блоки)

- ✅ Якщо рефакториш **CSS** → прочитай:
  - **КРИТИЧНІ ПРАВИЛА ДЛЯ CSS ФАЙЛІВ** (баланс дужок!)
  - **CSS Architecture — BEM + ITCSS** (naming conventions)

**2. Застосуй Security Best Practices (обов'язково!):**

```php
// ✅ ЗАВЖДИ при рефакторингу перевіряй:
- check_ajax_referer() для AJAX handlers
- current_user_can() для REST API
- $wpdb->prepare() для SQL queries
- sanitize_*() для input
- esc_*() для output
```

**3. Застосуй Modern PHP Patterns (якщо можливо):**

```php
// ✅ При рефакторингу PHP модулів:
- Додай strict_types=1
- Використай type hints (int, string, array, ?WP_Post)
- Розглянь Repository Pattern для database queries
- Додай Object Caching (Transients API) для важких запитів
```

**4. Застосуй Performance Best Practices:**

```php
// ✅ Оптимізація при рефакторингу:
- Lazy loading для важких компонентів
- Database indexes для meta queries
- REST API caching (Redis/Transients)
- Event delegation замість множинних listeners
```

**5. Перевір CSS баланс (якщо торкаєшся CSS):**

```bash
# ОБОВ'ЯЗКОВО після редагування CSS файлів:
grep -c "{" css/file.css && grep -c "}" css/file.css
# Числа мають бути однакові!
```

**6. 🔴 ОБОВ'ЯЗКОВО: Prettier форматування ПЕРЕД комітом!**

```bash
# ⚠️ КРИТИЧНО: Форматуй ВСІNОВІ файли через Prettier перед комітом!
# CI/CD перевіряє форматування і заблокує PR при помилках

# Перевірити форматування:
npm run format:check

# Виправити форматування:
npx prettier --write <file>

# Або всі файли:
npm run format
```

**Типи файлів що форматуються Prettier:**

- ✅ `*.json` (theme.json, package.json, etc.)
- ✅ `*.css` (всі стилі)
- ✅ `*.js` (JavaScript)
- ✅ `*.md` (Markdown документація)
- ⚠️ `*.php` (потребує @prettier/plugin-php)

**Помилки CI/CD при ігноруванні:**

```
[warn] theme.json
[warn] Code style issues found. Run Prettier with --write to fix.
Error: Process completed with exit code 1.
```

### Заборонені практики при рефакторингу

**КАТЕГОРИЧНО ЗАБОРОНЕНО:**

❌ **НЕ використовувати застарілі патерни:**

- `eval()` з user input
- `unserialize()` з $_POST/$\_GET
- SQL конкатенація замість `$wpdb->prepare()`
- `__return_true` як `permission_callback` у REST API

❌ **НЕ ігнорувати Security Checklist:**

- Кожен рефакторинг AJAX handler → перевір nonce
- Кожен рефакторинг REST endpoint → перевір permissions
- Кожен рефакторинг SQL query → перевір sanitization

❌ **НЕ порушувати CSS баланс дужок:**

- Завжди закривай `{` з `}`
- Перевіряй баланс після КОЖНОГО редагування

### Workflow рефакторингу (обов'язковий)

```
КРОК 1: Визнач тип файлу (PHP/JS/CSS)
        ↓
КРОК 2: Прочитай релевантну секцію CLAUDE.md
        ↓
КРОК 3: Застосуй Security Best Practices
        ↓
КРОК 4: Застосуй Modern Architecture Patterns (якщо можливо)
        ↓
КРОК 5: Перевір Performance implications
        ↓
КРОК 6: Запусти тести/валідацію (CSS баланс, PHPStan, тощо)
        ↓
КРОК 7: Коміт з детальним описом змін
```

### Приклад правильного рефакторингу

**Завдання:** Рефакторинг `inc/blog-meta-fields.php` (views tracking)

**Правильний підхід:**

1. ✅ Прочитав WORDPRESS SECURITY → знайшов SQL Injection patterns
2. ✅ Прочитав ADVANCED ARCHITECTURE PATTERNS → знайшов Repository Pattern
3. ✅ Застосував `$wpdb->prepare()` замість конкатенації
4. ✅ Додав atomic increment через SQL (ON DUPLICATE KEY UPDATE)
5. ✅ Додав Object Caching (Transients) для top viewed posts
6. ✅ Додав strict_types=1 та type hints
7. ✅ Перевірив Security Checklist (sanitization, escaping)

**Результат:** Безпечний, швидкий, сучасний код згідно з best practices 2025.

### Посилання на документацію

**Основні секції для рефакторингу:**

- **Advanced Architecture Patterns** — DI, PSR-4, Repository, Performance
- **Modern JavaScript & CSS** — ES6+, Gutenberg, BEM+ITCSS
- **WordPress Security** — CVE patterns, Security Audit Checklist
- **CSS Правила** — Баланс дужок, типові помилки

**Зовнішні файли:**

- `CODING-RULES.md` — Master Index
- `docs/coding-rules/CODING-RULES-WORDPRESS.md` — WordPress standards
- `docs/guides/CHECKLIST.md` — Pre-Commit Checklist

---

## 🚀 ADVANCED ARCHITECTURE PATTERNS

**⚠️ Примітка:** Ці патерни — майбутні покращення для масштабування теми. Поточна реалізація використовує модульний підхід (17 PHP модулів в `inc/`).

### 1. Dependency Injection Container (Future Enhancement)

**Концепція:** PSR-11 контейнер для управління залежностями та lazy loading сервісів.

**Приклад використання:**

```php
// inc/core/Container.php
namespace Medici\Core;
class Container implements \ArrayAccess
{
	public function service(callable $callable): callable
	{
		return function ($c) use ($callable) {
			static $object;
			return $object ?? ($object = $callable($c));
		};
	}
}

// functions.php
$container['blog.reading_time'] = $container->service(function ($c) {
	return new ReadingTimeCalculator(200); // WPM
});
```

**Переваги:** Централізоване управління залежностями, легше тестування, lazy loading.

### 2. PSR-4 Autoloading

**Структура для WordPress теми з Composer:**

```
medici/
├── composer.json      # PSR-4 autoload config
├── src/               # Namespace: Medici\
│   ├── Blog/
│   ├── Performance/
│   └── Schema/
└── functions.php      # Bootstrap
```

**composer.json:**

```json
{
	"autoload": {
		"psr-4": { "Medici\\": "src/" }
	}
}
```

**Переваги:** Автоматичне завантаження класів без `require_once`, краща IDE підтримка.

### 3. Repository Pattern для WordPress

**Приклад абстракції доступу до даних:**

```php
namespace Medici\Blog;
class PostRepository
{
	public function __construct(private readonly \wpdb $wpdb) {}

	public function findFeaturedPosts(int $limit = 6): array
	{
		return (new \WP_Query([
			'post_type' => 'medici_blog',
			'posts_per_page' => $limit,
			'meta_key' => 'medici_featured',
			'meta_value' => '1',
		]))->posts;
	}

	public function incrementViews(int $post_id): bool
	{
		// Атомарний інкремент через SQL
		return false !==
			$this->wpdb->query(
				$this->wpdb->prepare(
					"INSERT INTO {$this->wpdb->postmeta} (post_id, meta_key, meta_value)
                VALUES (%d, 'medici_views', 1)
                ON DUPLICATE KEY UPDATE meta_value = meta_value + 1",
					$post_id
				)
			);
	}
}
```

**Використання в темі:** Замінити прямі виклики WP_Query в модулях (`inc/blog-meta-fields.php`) на Repository methods.

**Переваги:** Легше тестувати (mock repository), бізнес-логіка відокремлена від WordPress API.

### 4. Advanced Performance Patterns

**A. Object Caching з Transients API**

```php
namespace Medici\Cache;
class CacheManager
{
	private const PREFIX = 'medici_';

	public function remember(string $key, callable $callback, int $ttl = HOUR_IN_SECONDS): mixed
	{
		$value = get_transient(self::PREFIX . $key);
		if (false !== $value) {
			return $value;
		}

		$value = $callback();
		set_transient(self::PREFIX . $key, $value, $ttl);
		return $value;
	}
}
```

**Використання:**

```php
$cache = new CacheManager();
$top_posts = $cache->remember(
	'top_posts_10',
	fn() => $repository->getTopViewedPosts(10),
	HOUR_IN_SECONDS
);
```

**B. Custom Database Indexes**

```php
// Для швидкого пошуку за meta_key + meta_value
global $wpdb;
$wpdb->query("CREATE INDEX idx_medici_views ON {$wpdb->postmeta} (meta_key, meta_value(10))");
```

**Інтеграція в тему:** Додати хук `after_switch_theme` в `functions.php`.

### 5. REST API Endpoints для Headless

**Приклад endpoint для featured posts:**

```php
namespace Medici\Api;
class BlogRestController
{
	public function register(): void
	{
		register_rest_route('medici/v1', '/posts/featured', [
			'methods' => 'GET',
			'callback' => [$this, 'getFeaturedPosts'],
			'permission_callback' => '__return_true',
			'args' => [
				'per_page' => [
					'default' => 6,
					'sanitize_callback' => 'absint',
					'validate_callback' => fn($v) => $v > 0 && $v <= 50,
				],
			],
		]);
	}
}
```

**Використання в темі:** Створити `inc/rest-api.php` модуль, реєструвати на `rest_api_init` hook.

### 6. Static Analysis & Code Quality

**PHPStan конфігурація (phpstan.neon):**

```yaml
parameters:
  level: 8
  paths: [inc, src]
  bootstrapFiles: [wordpress-stubs.php]
```

**Composer scripts:**

```json
"scripts": {
    "phpstan": "phpstan analyse",
    "phpcs": "phpcs --standard=WordPress inc/",
    "test": "phpunit"
}
```

**GitHub Actions (.github/workflows/code-quality.yml):**

```yaml
name: Code Quality
on: [push, pull_request]
jobs:
  phpstan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: composer install
      - run: composer phpstan
```

**Інтеграція в тему:** Додати `composer.json`, `phpstan.neon`, GitHub Actions workflow.

### 7. Production Performance & Core Web Vitals

**Метрики:**

- **LCP < 2.0s** — Preload критичних шрифтів (`css/core/core.css`), defer non-critical CSS
- **INP < 100ms** — Delay JS execution (`inc/assets.php`), minimal event listeners
- **CLS < 0.05** — Fixed dimensions для images (вже реалізовано в `blog-single.css`)

**Рекомендовані інструменти:**

- **Perfmatters** — Script management, delay JavaScript
- **Query Monitor** — Debug WordPress queries (95 queries на homepage → optimize!)
- **PHPStan Level 8** — Static analysis для виявлення помилок до production

**Моніторинг:**

```php
// inc/performance.php
add_action('shutdown', function () {
	if (defined('SAVEQUERIES') && SAVEQUERIES) {
		global $wpdb;
		error_log(
			sprintf(
				'Queries: %d, Time: %.4fs',
				count($wpdb->queries),
				array_sum(wp_list_pluck($wpdb->queries, 1))
			)
		);
	}
});
```

**Поточний стан теми:**

- ✅ Lazy loading images (`inc/performance.php`)
- ✅ Critical CSS inline (`inc/assets.php`)
- ✅ Local fonts (WOFF2, `fonts/`)
- ⚠️ Потрібно: Query optimization, object caching, REST API

---

## 🎨 MODERN JAVASCRIPT & CSS ARCHITECTURE

**⚠️ Примітка:** Поточна тема використовує vanilla JavaScript. Ці патерни — для майбутнього розвитку з Gutenberg блоками та сучасним frontend.

### 1. Modern ES6+ JavaScript для WordPress

**ES6 Modules замість глобальних змінних:**

```javascript
// src/utils/api.js
export const fetchPosts = async (perPage = 6) => {
	const response = await fetch(`/wp-json/wp/v2/posts?per_page=${perPage}`);
	if (!response.ok) throw new Error('Failed to fetch posts');
	return response.json();
};

// src/main.js
import { fetchPosts } from './utils/api.js';

document.addEventListener('DOMContentLoaded', async () => {
	const posts = await fetchPosts(10);
	console.log(posts);
});
```

**Destructuring та Spread Operator:**

```javascript
// Immutable state updates
const [posts, setPosts] = useState([]);
setPosts([...posts, newPost]); // ✅ Не мутує масив

// Object destructuring
const { title, content, author } = post;
```

**Інтеграція в тему:** Використати `@wordpress/scripts` для Webpack збірки, модульна структура `js/src/`.

### 2. Gutenberg Block Development

**block.json — стандарт WordPress 5.8+:**

```json
{
	"apiVersion": 3,
	"name": "medici/featured-post",
	"title": "Featured Post Card",
	"category": "medici-blocks",
	"attributes": {
		"postId": { "type": "number", "default": 0 },
		"showExcerpt": { "type": "boolean", "default": true }
	},
	"supports": {
		"align": ["wide", "full"],
		"color": { "background": true, "text": true }
	},
	"editorScript": "file:./index.js",
	"render": "file:./render.php"
}
```

**Dynamic Block з Server-Side Rendering:**

```php
// render.php
$post = get_post($attributes['postId']);
?>
<article <?php echo get_block_wrapper_attributes(); ?>>
  <h3><?php echo esc_html($post->post_title); ?></h3>
</article>
```

**React Hooks у Gutenberg:**

```javascript
import { useSelect } from '@wordpress/data';

const PostSelector = () => {
	const posts = useSelect((select) => select('core').getEntityRecords('postType', 'medici_blog'));

	return (
		<select>
			{posts?.map((p) => (
				<option>{p.title.rendered}</option>
			))}
		</select>
	);
};
```

**Інтеграція в тему:** Створити `blocks/` директорію, використати `register_block_type(__DIR__ . '/build/featured-post')`.

### 3. CSS Architecture — BEM + ITCSS

**ITCSS (Inverted Triangle CSS) — 7 шарів:**

```
01-settings/    # Змінні ($color-primary, $spacing)
02-tools/       # Mixins, functions (без CSS output)
03-generic/     # Reset, normalize
04-elements/    # HTML tags (body, h1, a)
05-objects/     # Layout patterns (.o-container, .o-grid)
06-components/  # UI компоненти (.c-card, .c-button) - BEM
07-utilities/   # Helpers (.u-mt-2, .u-hidden) з !important
```

**BEM Naming Convention:**

```css
.blog-card {
} /* Block */
.blog-card__title {
} /* Element */
.blog-card--featured {
} /* Modifier */

/* Приклад */
.blog-card {
	background: white;
	border-radius: 12px;

	&__image {
		width: 100%;
		aspect-ratio: 16/9;
	}

	&__title {
		font-size: 1.5rem;
	}

	&--featured {
		border: 3px solid var(--accent);
	}
}
```

**Інтеграція в тему:** Рефакторинг `css/` директорії з ITCSS структурою, BEM класи для компонентів.

### 4. Performance Optimization

**A. REST API Caching (Redis):**

```php
class REST_API_Cache
{
	private $redis;
	private $ttl = 300; // 5 хвилин

	public function get_cached_response($result, $server, $request)
	{
		if ($request->get_method() !== 'GET') {
			return $result;
		}

		$cache_key = 'rest_api:' . md5($request->get_route());
		$cached = $this->redis->get($cache_key);

		return $cached ? json_decode($cached) : $result;
	}
}
add_filter('rest_pre_dispatch', [new REST_API_Cache(), 'get_cached_response'], 10, 3);
```

**B. Core Web Vitals Optimization:**

```php
// LCP — Preload критичних ресурсів
add_action(
	'wp_head',
	function () {
		echo '<link rel="preload" as="image" href="hero.jpg" fetchpriority="high">';
	},
	1
);
```

```css
/* CLS — Fixed dimensions */
.hero-image {
	width: 100%;
	aspect-ratio: 16/9; /* Запобігає layout shift */
	object-fit: cover;
}
```

```javascript
// INP — Event delegation
document.querySelector('.cards-container').addEventListener('click', (e) => {
	const card = e.target.closest('.card');
	if (card) handleClick(card);
});
```

**C. Code Splitting та Lazy Loading:**

```javascript
// Динамічний імпорт
const loadComments = async () => {
	const { initComments } = await import('./comments.js');
	initComments();
};

// Intersection Observer
const observer = new IntersectionObserver((entries) => {
	if (entries[0].isIntersecting) {
		loadComments();
		observer.disconnect();
	}
});
observer.observe(document.querySelector('#comments-trigger'));
```

**Поточний стан теми:**

- ✅ Vanilla JS модулі (`js/scripts.js`, `js/events.js`)
- ✅ ITCSS-подібна структура (`css/core/`, `css/components/`, `css/layout/`)
- ⚠️ Потрібно: Gutenberg блоки, BEM naming, REST API caching, Web Workers

**Цільові метрики Core Web Vitals:**

- **LCP < 2.0s** — Largest Contentful Paint
- **INP < 100ms** — Interaction to Next Paint
- **CLS < 0.05** — Cumulative Layout Shift

---

## 🔐 WORDPRESS SECURITY (CVE 2024-2025)

**⚠️ Критична інформація:** Аналіз ландшафту загроз WordPress за 2024-2025 для захисту теми Medici.

### 1. Ландшафт загроз 2024-2025

**Статистика вразливостей:**

- **Плагіни:** 96% всіх CVE (зростання на 34% порівняно з 2023)
- **Теми:** 3.5% вразливостей
- **WordPress Core:** 0.5% (переважно Authenticated XSS, низький імпакт)

**Критичні тренди:**

- **Supply Chain Attacks** — компрометація розробників, бекдори в легітимних оновленнях
- **Mass Exploitation** — автоматизовані атаки протягом 24-48 годин після публікації PoC
- **Unauthenticated RCE** — найнебезпечніший вектор (CVSS 9.0-10.0)

### 2. Критичні патерни вразливостей

**A. Unprotected File Upload (CVSS 10.0):**

```php
// ❌ ВРАЗЛИВИЙ КОД (CVE-2025-6327 pattern)
function ajax_upload()
{
	move_uploaded_file($_FILES['file']['tmp_name'], $target);
}
add_action('wp_ajax_nopriv_upload', 'ajax_upload'); // Неавтентифікований!

// ✅ БЕЗПЕЧНИЙ КОД
function ajax_upload()
{
	check_ajax_referer('upload_nonce');
	if (!current_user_can('upload_files')) {
		wp_die('Недостатньо прав');
	}

	$allowed = ['jpg', 'jpeg', 'png', 'gif'];
	$type = wp_check_filetype($_FILES['file']['name']);
	if (!in_array($type['ext'], $allowed)) {
		wp_die('Недозволений тип');
	}

	wp_handle_upload($_FILES['file'], ['test_form' => false]);
}
add_action('wp_ajax_upload', 'ajax_upload'); // Тільки залогінені
```

**B. SQL Injection через Partial Sanitization:**

```php
// ❌ ВРАЗЛИВИЙ КОД (CVE-2025-12197 pattern)
$orderby = esc_sql($_GET['orderby']);
$where = 'AND ' . $_GET['where']; // Не санітизовано!
$query = "SELECT * FROM posts WHERE status = 1 $where ORDER BY $orderby";

// ✅ БЕЗПЕЧНИЙ КОД
$allowed_orderby = ['date', 'title', 'id'];
$orderby = in_array($_GET['orderby'], $allowed_orderby) ? $_GET['orderby'] : 'date';
$meta_key = sanitize_text_field($_GET['meta_key']);
$query = $wpdb->prepare("SELECT * FROM posts WHERE meta_key = %s ORDER BY $orderby", $meta_key);
```

**C. Insecure REST API (CVE-2024-25600 pattern):**

```php
// ❌ ВРАЗЛИВИЙ КОД
register_rest_route('medici/v1', '/admin', [
	'callback' => 'do_admin_action',
	'permission_callback' => '__return_true', // КРИТИЧНА ПОМИЛКА!
]);

// ✅ БЕЗПЕЧНИЙ КОД
register_rest_route('medici/v1', '/admin', [
	'callback' => 'do_admin_action',
	'permission_callback' => fn() => current_user_can('manage_options'),
	'args' => [
		'id' => [
			'validate_callback' => fn($p) => is_numeric($p),
			'sanitize_callback' => 'absint',
		],
	],
]);
```

### 3. Security Audit Checklist

**Перед кожним коммітом перевір:**

✅ **Input Validation:**

- `$_GET`, `$_POST`, `$_REQUEST` санітизовані (`sanitize_text_field`, `absint`, `esc_url`)
- REST API має `validate_callback` та `sanitize_callback`

✅ **Authentication:**

- AJAX без `nopriv` має `check_ajax_referer()`
- REST API має `permission_callback` з `current_user_can()`

✅ **Database:**

- Завжди `$wpdb->prepare()` для динамічних запитів
- Whitelist для `ORDER BY`, `WHERE`

✅ **Output Escaping:**

- `esc_html()`, `esc_url()`, `esc_attr()`, `wp_kses_post()`

### 4. Hardening для теми Medici

**Поточний стан (v1.5.2):**

- ✅ Security module: `inc/security.php` (XML-RPC disable, CSP, headers)
- ✅ Nonce verification: `inc/class-events.php`
- ✅ Input sanitization: `inc/blog-*.php`
- ✅ **CSP Policy** з підтримкою Google Analytics, GTM, Fonts, Cloudflare Zaraz
- ✅ **Cross-Origin headers** (CORP, COOP, COEP)
- ✅ **CSP Report Endpoint** (Cloudflare Worker)

**CSP директиви (inc/security.php v1.5.2):**

```
script-src 'self' 'unsafe-inline' 'unsafe-eval' *.googletagmanager.com *.google-analytics.com *.cloudflare.com
style-src 'self' 'unsafe-inline' fonts.googleapis.com
font-src 'self' data: fonts.gstatic.com
connect-src 'self' *.google-analytics.com stats.g.doubleclick.net *.cloudflare.com
frame-src 'self' youtube.com youtube-nocookie.com player.vimeo.com
object-src 'none'
report-uri https://csp-report-endpoint.moto08405.workers.dev
```

**Cross-Origin заголовки:**

```
Cross-Origin-Resource-Policy: cross-origin
Cross-Origin-Opener-Policy: same-origin-allow-popups
Cross-Origin-Embedder-Policy: unsafe-none
```

**Посилання:**

- Security: `inc/security.php:153-255`
- Events API: `inc/class-events.php` (nonce examples)
- Blog modules: `inc/blog-*.php` (sanitization)

---

## Shortcodes Reference

The theme provides **3 shortcodes** for blog single post functionality, all registered in `inc/blog-shortcodes.php` (v1.0.16).

**⚠️ ВАЖЛИВО:** Тільки ці 3 shortcodes реально існують в темі. Решта функціоналу (author box, TOC, newsletter) реалізовані через інші методи.

---

### 1. Warning Box Shortcode

Display a warning box with custom title and icon for highlighting important information or common mistakes.

**Usage:**

```php
[medici_warning title="УВАГА: Найпоширеніша помилка" icon="⚠️"]
Текст попередження тут. Це може бути кілька абзаців з важливою інформацією для читача.
[/medici_warning]
```

**Parameters:**

- `title` (string, default: "УВАГА: Найпоширеніша помилка") - Warning heading
- `icon` (string, default: "⚠️") - Emoji icon to display

**Features:**

- Supports HTML content inside shortcode
- Processes nested shortcodes
- Sanitized output (XSS protection)
- CSS class: `.warning-box`

**Examples:**

```php
// Default warning
[medici_warning]
Це важливе попередження!
[/medici_warning]

// Custom title and icon
[medici_warning title="КРИТИЧНО" icon="🚨"]
Це критична інформація яку потрібно прочитати!
[/medici_warning]

// With HTML content
[medici_warning title="Юридичні аспекти" icon="⚖️"]
<strong>Важливо:</strong> Перед публікацією реклами лікарських засобів обов'язково отримайте дозвіл Держлікслужби.
[/medici_warning]
```

**File:** `inc/blog-shortcodes.php:33-64`

---

### 2. Inline CTA Shortcode

Display a call-to-action box inside blog post content to encourage user engagement.

**Usage:**

```php
[medici_cta
    title="Потрібна консультація?"
    text="Наші експерти допоможуть вам вирішити будь-яке питання"
    button_text="Зв'язатися з нами"
    button_url="#contact"
    button_icon="→"]
```

**Parameters:**

- `title` (string, default: "Потрібна консультація?") - CTA heading
- `text` (string, default: "Наші експерти допоможуть вам вирішити будь-яке питання") - CTA description
- `button_text` (string, default: "Зв'язатися з нами") - Button label
- `button_url` (string, default: "#contact") - Button link URL
- `button_icon` (string, default: "→") - Button icon (special handling for arrow "→")

**Features:**

- Responsive design
- Sanitized URLs and text
- SVG arrow icon for "→" (automatic)
- CSS class: `.inline-cta`
- Button class: `.btn-white`

**Examples:**

```php
// Default CTA
[medici_cta]

// Custom legal consultation CTA
[medici_cta
    title="Потрібна юридична перевірка реклами?"
    text="Наші юристи з 10-річним досвідом перевірять ваші рекламні матеріали"
    button_text="Замовити перевірку"
    button_url="/contacts/"
    button_icon="→"]

// Custom icon
[medici_cta
    title="Завантажити чек-лист"
    text="Безкоштовний чек-лист для перевірки реклами ЛЗ"
    button_text="Завантажити PDF"
    button_url="/downloads/checklist.pdf"
    button_icon="📥"]
```

**File:** `inc/blog-shortcodes.php:79-119`

---

### 3. Key Takeaways Shortcode

Display a highlighted box with key points or takeaways from the article.

**Usage:**

```php
[medici_takeaways title="Що ви дізнаєтесь з цієї статті:"]
Перший важливий пункт
Другий важливий пункт
Третій важливий пункт
[/medici_takeaways]
```

**Parameters:**

- `title` (string, default: "Що ви дізнаєтесь з цієї статті:") - Takeaways heading

**Features:**

- Each line becomes a separate list item
- Empty lines ignored
- Supports HTML in items
- CSS class: `.takeaways`
- Bullet list format

**Examples:**

```php
// Default takeaways
[medici_takeaways]
Як отримати дозвіл на рекламу ЛЗ
Які документи потрібні для Держлікслужби
Найчастіші помилки при подачі заявки
[/medici_takeaways]

// Custom title
[medici_takeaways title="Ключові висновки:"]
SMM для медичних клінік має свої особливості
Важлива прозорість та етика в комунікації
ROI можна виміряти через записи на прийом
[/medici_takeaways]

// With HTML formatting
[medici_takeaways title="💡 Головне:"]
<strong>Юридична перевірка</strong> - обов'язковий етап
<em>Термін розгляду</em> - від 30 до 90 днів
<strong>Вартість дозволу</strong> - від 5000 до 15000 грн
[/medici_takeaways]
```

**File:** `inc/blog-shortcodes.php:135-169`

---

### Technical Details

**All shortcodes:**

- ✅ Full PHP type hints (strict_types=1)
- ✅ PHPDoc documentation
- ✅ Input sanitization (security)
- ✅ Output escaping (XSS protection)
- ✅ WordPress Coding Standards compliant

**Security features:**

- `sanitize_text_field()` for text inputs
- `esc_url()` for URLs
- `wp_kses_post()` for HTML content
- `esc_html()` for output

**CSS Styling:**
All shortcodes have predefined CSS classes in `style.css`. Customize appearance by editing:

- `.warning-box` - Warning box styles
- `.inline-cta` - CTA box styles
- `.takeaways` - Takeaways box styles

---

### Not Available as Shortcodes

The following functionality is **NOT** available as shortcodes but implemented differently:

- **Author Box** - Available via `Medici_Blog_Module::render_author_box()` method (see API Reference)
- **Share Buttons** - Available via `Medici_Blog_Module::render_share_buttons()` method
- **Breadcrumbs** - Built into `single-medici_blog.php` template
- **Related Posts** - Built into `single-medici_blog.php` template (uses `medici_get_related_blog_posts()`)
- **Newsletter Form** - Available in sidebar (single-medici_blog.php template)
- **Table of Contents** - ✅ Implemented! Auto-generated from H2/H3 headings via JavaScript (js/modules/blog/blog-single.js)
- **Blog Posts Grid** - Use GenerateBlocks patterns instead (see Blog Patterns Module)

**Why not shortcodes?**
These features require complex logic, database queries, and state management that's better handled through PHP methods and widgets rather than shortcodes.

**How to use them?**
See **API Reference** section below for method signatures and usage examples.

## Twemoji Local Integration

**🎯 Локальна система відображення емоджі**

Тема використовує **Twemoji** для відображення всіх емоджі як SVG картинок.

- **4009 емоджі** - Повне покриття Unicode emoji стандарту
- **SVG формат** - Векторна графіка, масштабується без втрати якості
- **Локальні файли** - Всі assets на власному сервері (CSP compliant)

**Файли:**

- `inc/twemoji-local.php` - Локальний Twemoji модуль (v1.0.1)
- `js/twemoji/twemoji.min.js` - Twemoji JavaScript library 15.1.0 (18KB, локальний)
- `assets/twemoji/svg/` - 4009 SVG емоджі файлів (11MB, локальні)

**Використання:**

**1. Автоматична ініціалізація:**
Twemoji автоматично парсить `document.body` після завантаження сторінки та конвертує всі емоджі в SVG картинки.

**2. PHP функції:**

```php
// Отримати Twemoji base URL
$base_url = medici_get_twemoji_base_url();
// Результат: https://www.medici.agency/wp-content/themes/medici/assets/twemoji/

// Отримати Twemoji base path (серверний шлях)
$base_path = medici_get_twemoji_base_path();
// Результат: /home/user/medici/assets/twemoji/

// Перевірити чи існують Twemoji assets
if (medici_twemoji_assets_exist()) {
	// SVG файли знайдені
}

// Debug інформація
$info = medici_get_twemoji_info();
// Результат: ['base_url' => '...', 'svg_count' => 4009, ...]
```

**3. JavaScript конфігурація:**

```javascript
// Twemoji автоматично налаштовується через inline script:
twemoji.parse(document.body, {
	folder: 'svg',
	ext: '.svg',
	base: 'https://www.medici.agency/wp-content/themes/medici/assets/twemoji/',
});
```

**Особливості:**

✅ **Локальні файли** - Twemoji hosted на власному сервері (no CDN, no external requests)
✅ **4009 емоджі** - Повне покриття Unicode emoji стандарту
✅ **SVG формат** - Векторна графіка, масштабується без втрати якості
✅ **CSP compliant** - script-src 'self', no inline scripts
✅ **Admin notice** - Попередження якщо SVG assets не знайдені
✅ **Debug helper** - `medici_get_twemoji_info()` для діагностики
✅ **Типізація** - Всі функції з PHP type hints (strict_types=1)
✅ **Priority 10** - Стандартний WordPress priority

**Як працює:**

1. **Twemoji JavaScript** - Автоматично парсить document.body після завантаження
2. **Конвертація** - Всі емоджі замінюються на SVG картинки з локального сервера
3. **WordPress emoji disabled** - Стандартні WordPress emoji скрипти вимкнені для уникнення конфліктів

**Приклад:**

```
Подія відбудеться 📅 15 грудня о 10:00 ⏰
Привіт! 👋 Як справи? 😊

↓ Twemoji конвертує в SVG:

Подія відбудеться <img src=".../svg/1f4c5.svg" class="emoji"> 15 грудня о 10:00 <img src=".../svg/23f0.svg" class="emoji">
Привіт! <img src=".../svg/1f44b.svg" class="emoji"> Як справи? <img src=".../svg/1f60a.svg" class="emoji">
```

**Performance:**

- ✅ **11MB SVG assets** - Cached браузером (один раз завантажується)
- ✅ **18KB JavaScript** - Twemoji library мінімізована
- ✅ **Lazy parsing** - Емоджі парсяться після DOMContentLoaded
- ✅ **No CDN latency** - Всі файли на власному сервері
- ✅ **HTTP/2 multiplexing** - Браузер може завантажувати багато SVG паралельно

**Джерело:** [Twemoji by Twitter](https://github.com/jdecked/twemoji) (локальна копія 15.1.0)

## API Reference

**Blog Functions (inc/blog-\*.php):**

```php
// Reading Time & Views
medici_calculate_reading_time( int $post_id ): int
medici_get_blog_reading_time( int $post_id ): int
medici_get_blog_post_views( int $post_id ): int
medici_increment_blog_post_views( int $post_id ): void
medici_track_blog_post_views(): void

// Featured & Related Posts
medici_is_blog_post_featured( int $post_id ): bool
medici_get_featured_blog_post(): ?WP_Post
medici_get_related_blog_posts( int $post_id, int $limit = 3 ): array

// Category
medici_get_category_color( int $term_id ): string
medici_get_category_style( int $term_id ): array

// Meta
medici_get_blog_meta_values( WP_Post $post ): array
medici_get_blog_author_name( int $post_id ): string
medici_get_blog_publication_date( int $post_id ): string
```

**Twemoji Functions (inc/twemoji-local.php):**

```php
medici_get_twemoji_base_url(): string
medici_get_twemoji_base_path(): string
medici_twemoji_assets_exist(): bool
medici_get_twemoji_info(): array
```

**Schema Functions (inc/schema-medical.php):**

```php
medici_build_organization_schema(): array
medici_build_faq_schema( string $content ): ?array
medici_build_howto_schema( string $content ): ?array
medici_build_video_schema( string $content ): ?array
```

**Transliteration (inc/transliteration.php):**

```php
medici_transliterate( string $text ): string
medici_transliterate_sanitize_title( string $title ): string
medici_transliterate_sanitize_file_name( string $filename ): string
```

**Events API (inc/class-events.php):**

```php
Medici\Events::init(): void
Medici\Events::send( string $event_type, array $payload ): bool
```

**WordPress Hooks Used:**

- Actions: after_setup_theme, init, wp_enqueue_scripts, wp_head, save_post
- Filters: sanitize_title, sanitize_file_name, excerpt_length, body_class
- Filters: excerpt_length, body_class, wp_nav_menu_items, the_content

## 🛠️ DevOps & Code Quality

### Composer & PHPStan

**Встановлення залежностей:**

```bash
composer install
```

**Доступні команди:**

```bash
composer phpstan          # Запустити PHPStan аналіз
composer phpstan:baseline # Згенерувати baseline для існуючих помилок
composer phpcs            # Запустити PHP CodeSniffer (WordPress standards)
composer phpcbf           # Автоматично виправити PHPCS помилки
composer lint             # Запустити всі linting інструменти
composer test             # Запустити всі тести та linting
```

**PHPStan конфігурація:**

- **Рівень:** 5 (рекомендовано для WordPress тем)
- **WordPress stubs:** szepeviktor/phpstan-wordpress
- **Baseline:** 23 помилки в існуючому коді (для поступового виправлення)

**Файли конфігурації:**
| Файл | Призначення |
|------|-------------|
| `composer.json` | Залежності та scripts |
| `phpstan.neon` | PHPStan конфігурація |
| `phpstan-baseline.neon` | Ігноровані помилки (existing code) |
| `.gitignore` | Git виключення (vendor/, node_modules/) |

**PHPStan Baseline помилки (23):**
Типові помилки для поступового виправлення:

- `esc_html()` / `esc_attr()` отримують int замість string (12 помилок)
- Unreachable code / always true conditions (4 помилки)
- Type mismatches у WordPress functions (7 помилок)

**Виправлення baseline помилок:**

```php
// ❌ До (помилка: int замість string)
echo esc_html($post_id);

// ✅ Після (правильно: cast до string)
echo esc_html((string) $post_id);
```

**Після виправлення помилок:**

```bash
# Перегенерувати baseline
composer phpstan:baseline
```

### PHP CodeSniffer

**Стандарт:** WordPress Coding Standards (WPCS 3.3.0)

**Перевірка:**

```bash
composer phpcs
```

**Автоматичне виправлення:**

```bash
composer phpcbf
```

### GitHub Actions CI/CD

**Автоматичні перевірки при push/PR:**

- **PHPStan** - Статичний аналіз (level 5)
- **PHPCS** - WordPress Coding Standards
- **PHP Compatibility** - Сумісність з PHP 8.1+
- **CSS Check** - Баланс фігурних дужок

**Файл:** `.github/workflows/ci.yml`

**Статус перевірок:**

```
✅ PHPStan (Level 5)      - Обов'язково
✅ PHPCS (WordPress)      - Warning (continue-on-error)
✅ PHP Compatibility      - Warning (continue-on-error)
✅ CSS Balance Check      - Обов'язково
```

### Pre-commit Hooks

**Встановлення:**

```bash
./scripts/install-hooks.sh
```

**Або вручну:**

```bash
chmod +x scripts/pre-commit
ln -sf ../../scripts/pre-commit .git/hooks/pre-commit
```

**Що перевіряється:**

1. PHPStan аналіз
2. CSS баланс дужок
3. Debug statements (var_dump, print_r, die, dd)

**Пропустити hook:**

```bash
git commit --no-verify
```

### Рекомендований Workflow

**Перед кожним комітом (автоматично з hook):**

```bash
# Hook автоматично запустить:
# 1. PHPStan
# 2. CSS balance check
# 3. Debug statement check
```

**При додаванні нового коду:**

1. Переконайся що PHPStan проходить без нових помилок
2. Запусти `composer phpcs` для перевірки стилю
3. Не додавай debug statements (var_dump, print_r, die)

**Ручна перевірка:**

```bash
composer lint  # PHPStan + PHPCS
```

---

## Change Log (Documentation)

### 6.2 - 2025-12-18

- 🐛 **КРИТИЧНЕ ВИПРАВЛЕННЯ: Sitemap Fatal Error** (v2.0.2)
  - **Проблема:** PHP Fatal Error `TypeError` у функції `medici_disable_user_sitemap()`
    - Функція очікувала `array $providers`, але отримувала об'єкт `WP_Sitemaps_Provider`
    - Filter `wp_sitemaps_add_provider` передає `($provider, $name)`, а не масив
    - Fatal Error на кожному запиті до сайту → WordPress sitemap не працював
  - **Виправлення:**
    - Змінено signature: `array $providers` → `WP_Sitemaps_Provider $provider, string $name`
    - Логіка: повертає `false` для 'users' provider, інакше `$provider`
    - Додано 2 параметри до filter: `add_filter(..., 10, 2)`
    - Оновлено PHPDoc з правильними типами
  - **Файли:**
    - `inc/sitemap-optimization.php:305-314` — FIXED
  - **Результат:**
    - ✅ Fatal Error більше не виникає
    - ✅ WordPress sitemap працює штатно
    - ✅ User sitemap коректно виключається
    - ✅ PHPStan Level 5 compliant
  - **Commits:** `8b116e1`
  - **Branch:** `claude/fix-sitemap-error-1ibJv`
  - **Документація оновлена:**
    - CHANGELOG.md — детальний запис з code examples
    - TODO.md — позначено виконання (якщо було завдання)
    - CLAUDE.md — Change Log 6.2

### 6.1 - 2025-12-18

- ♻️ **PHP OOP Refactoring Phase 2** - Strategy, Chain of Responsibility, Builder Patterns
  - **Lead Scoring Module** (Strategy Pattern) - 8 файлів:
    - `ScoringStrategyInterface.php`, `ScoringConfig.php`, `ScoringService.php`, `ScoringAdmin.php`
    - `scoring/SourceStrategy.php`, `MediumStrategy.php`, `ServiceStrategy.php`, `BonusStrategy.php`
  - **Lead Validation Module** (Chain of Responsibility) - 10 файлів:
    - `ValidatorInterface.php`, `ValidationResult.php`, `ValidationService.php`
    - `validators/EmailValidator.php`, `PhoneValidator.php`, `NameValidator.php`, `MessageValidator.php`, `UtmValidator.php`, `SpamValidator.php`, `ServiceValidator.php`
  - **Schema Module** (Builder Pattern) - 9 файлів:
    - `SchemaBuilderInterface.php`, `AbstractSchemaBuilder.php`, `SchemaConfig.php`, `SchemaRenderer.php`
    - `builders/OrganizationBuilder.php`, `FaqBuilder.php`, `HowToBuilder.php`, `VideoBuilder.php`
- 🎯 **Helper Functions:**
  - `medici_scoring()->calculate($data)`, `medici_scoring()->getLabel($score)`
  - `medici_validation()->validate($data)`, `medici_validation()->process($data)`
  - `medici_schema()->render()`, `medici_schema_build_*($content)`
- 🐛 **PHPStan Fixes:** `getInstance()` to SchemaConfig, `setContent()` to AbstractSchemaBuilder
- 🔧 **Commits:** `ee74410`, `68e0784`
- 📦 **Statistics:** +5067 рядків, 27 нових файлів

### 6.0 - 2025-12-18

- ♻️ **PHP Modern Patterns - OOP Refactoring v2.0.0**
  - **Blog Module (`inc/blog/`)** - Repository + Service Pattern:
    - `BlogPostRepository.php` (416 рядків) - Data access abstraction
    - `ReadingTimeService.php` (206 рядків) - Reading time calculation
    - `PostViewsService.php` (324 рядки) - Atomic view counting
    - `bootstrap.php` (186 рядків) - Module initialization
  - **Lead Module (`inc/lead/`)** - Adapter Pattern:
    - `IntegrationInterface.php` - Contract for all integrations
    - `AbstractIntegration.php` - Base class with error handling
    - `EmailAdapter.php` (378 рядків) - HTML email notifications
    - `TelegramAdapter.php` (279 рядків) - Telegram Bot API
    - `GoogleSheetsAdapter.php` (204 рядки) - Apps Script integration
    - `IntegrationManager.php` (250 рядків) - Orchestrates adapters
    - `bootstrap.php` - Module initialization
  - **Events Module (`inc/events/`)** - Event Dispatcher + Observer Pattern:
    - `EventInterface.php` - Event contract з getEventId/setEventId
    - `AbstractEvent.php` (151 рядок) - Base event class
    - `EventDispatcher.php` (181 рядок) - Central event bus (Singleton)
    - `ObserverInterface.php` - Observer contract
    - `ConsultationRequestEvent.php` (173 рядки) - Form submission
    - `NewsletterSubscribeEvent.php` (97 рядків) - Newsletter
    - `bootstrap.php` (396 рядків) - Module initialization + helper functions
    - **Observers:**
      - `LoggingObserver.php` (162 рядки) - Database logging
      - `LeadCreationObserver.php` (117 рядків) - Lead CPT creation
      - `IntegrationObserver.php` (134 рядки) - Email/Telegram/Sheets
      - `WebhookObserver.php` (176 рядків) - Webhook notifications
- 🐛 **PHPStan EventInterface Fix**
  - Додано `getEventId(): ?int` та `setEventId(int $id): void` до EventInterface
  - Виправлено PHPStan помилки в AbstractEvent та всіх observers
- 📝 **Документація оновлена:**
  - CHANGELOG.md - додано OOP Refactoring запис
  - CLAUDE.md - оновлено Change Log, Architecture, API Reference
  - TODO.md - оновлено Фаза 5 (OOP Refactoring)
- 🔧 **Commits:**
  - `8e5180d` - ♻️ REFACTOR: PHP Modern Patterns - Repository, Adapter, Event Dispatcher
  - `a83361d` - 🐛 Fix: PHPStan - додано getEventId/setEventId до EventInterface
  - `4e0a5ae` - 📝 Docs: додано секцію Interface Design та PHPStan Compliance
- 📦 **Statistics:**
  - +4211 рядків нового коду
  - 23 нових файли
  - 3 нових модульні директорії

### 6.1 - 2025-12-18

- ♻️ **PHP OOP Refactoring Phase 2** - Strategy, Chain of Responsibility, Builder Patterns
  - **Lead Scoring Module** (Strategy Pattern) - 8 файлів:
    - `ScoringStrategyInterface.php`, `ScoringConfig.php`, `ScoringService.php`, `ScoringAdmin.php`
    - `scoring/SourceStrategy.php`, `MediumStrategy.php`, `ServiceStrategy.php`, `BonusStrategy.php`
  - **Lead Validation Module** (Chain of Responsibility) - 10 файлів:
    - `ValidatorInterface.php`, `ValidationResult.php`, `ValidationService.php`
    - `validators/EmailValidator.php`, `PhoneValidator.php`, `NameValidator.php`, `MessageValidator.php`, `UtmValidator.php`, `SpamValidator.php`, `ServiceValidator.php`
  - **Schema Module** (Builder Pattern) - 9 файлів:
    - `SchemaBuilderInterface.php`, `AbstractSchemaBuilder.php`, `SchemaConfig.php`, `SchemaRenderer.php`
    - `builders/OrganizationBuilder.php`, `FaqBuilder.php`, `HowToBuilder.php`, `VideoBuilder.php`
- 🎯 **Helper Functions:**
  - `medici_scoring()->calculate($data)`, `medici_scoring()->getLabel($score)`
  - `medici_validation()->validate($data)`, `medici_validation()->process($data)`
  - `medici_schema()->render()`, `medici_schema_build_*($content)`
- 🐛 **PHPStan Fixes:** `getInstance()` to SchemaConfig, `setContent()` to AbstractSchemaBuilder
- 🔧 **Commits:** `ee74410`, `68e0784`
- 📦 **Statistics:** +5067 рядків, 27 нових файлів

### 5.9 - 2025-12-17

- ✨ **GA4 Analytics + Lead Scoring Dashboard + WordPress Global Styles**
  - **GA4 Events** (`inc/analytics.php` + `js/analytics.js`) - вже існували:
    - Scroll depth (25%, 50%, 75%, 100%)
    - Time on page (30s, 60s, 2min, 5min)
    - CTA clicks, Form interactions
    - UTM tracking з first/last touch attribution
    - Microsoft Clarity інтеграція
  - **Lead Scoring** (`inc/lead-scoring.php`) - вже існував:
    - SOURCE_SCORES, MEDIUM_SCORES, SERVICE_SCORES
    - Пороги: hot (70+), warm (40-69), cold (0-39)
- 🎨 **WordPress Global Styles** (`theme.json`) - **СТВОРЕНО**:
  - 14 кольорів palette (primary, hot-lead, warm-lead, cold-lead, etc.)
  - 8 fluid font sizes (Utopia scale)
  - 9 spacing sizes (3xs → 3xl)
  - 6 shadow presets
  - Lead Scoring thresholds у custom settings
  - Typography, border radius, transitions
- 📊 **Dashboard Lead Scoring Widget** (`inc/dashboard-analytics.php`) - **ОНОВЛЕНО**:
  - Hot/Warm/Cold leads статистика з візуалізацією
  - Середній Score з progress bar (gradient)
  - Топ-5 гарячих лідів з посиланнями
  - `get_lead_scoring_stats()` + `get_hot_leads()` methods
- 🔧 **Commits:**
  - `956ab7a` - ✨ GA4 Analytics: Lead Scoring Dashboard + theme.json Global Styles
- 📦 **Файли:**
  - `theme.json` (460 рядків) - NEW
  - `inc/dashboard-analytics.php` (+150 рядків) - UPDATED

### 5.8 - 2025-12-17

- ♻️ **Exit-Intent OOP Refactoring** - WordPress Plugin Boilerplate Architecture
  - **Створено 4 класи** згідно з Single Responsibility Principle:
    - `inc/exit-intent/class-exit-intent.php` (148 рядків) - Main bootstrap class
    - `inc/exit-intent/class-exit-intent-loader.php` (132 рядки) - Hook registry (Loader pattern)
    - `inc/exit-intent/class-exit-intent-assets.php` (153 рядки) - Asset management
    - `inc/exit-intent/class-exit-intent-public.php` (115 рядків) - Frontend functionality
  - **Видалено 53 рядки** з `inc/assets.php` (procedural enqueue → OOP)
  - **Оновлено `functions.php`** - додано `medici_init_exit_intent()` на `after_setup_theme` hook
- 🏗️ **Architecture Patterns:**
  - Loader Pattern - централізований реєстр WordPress hooks
  - Dependency Injection - Exit_Intent instantiates all dependencies
  - Separation of Concerns - кожен клас має одну відповідальність
  - Type Safety - strict_types=1, type hints для всіх методів
- ✨ **Features Preserved:**
  - bioEp library (exit-intent detection + 30-day cookies)
  - GenerateBlocks Overlay Panel trigger
  - Events API form handling
  - Desktop-only loading (>1024px)
  - Debug mode (WP_DEBUG support)
- 🎯 **Benefits:**
  - Легше тестувати (mock dependencies)
  - Краща організація коду (4 малі класи vs 1 великий файл)
  - WordPress standards compliance (Plugin Boilerplate pattern)
  - PHPStan Level 5 compatible
  - Maintainable (clear separation of concerns)
- 🔧 **Commits:**
  - `8355861` - ♻️ REFACTOR: Exit-Intent → OOP Architecture (WordPress Plugin Boilerplate)
- 📖 **References:**
  - WordPress Plugin Boilerplate: https://github.com/DevinVinson/WordPress-Plugin-Boilerplate
  - beeker1121 library: https://github.com/beeker1121/exit-intent-popup

### 5.7 - 2025-12-16

- 🚀 **Exit-Intent Popup - ПОВНЕ ВИПРАВЛЕННЯ** (7 комітів, 3 дні troubleshooting)
  - **Створено `public-form-handler.php`** - Standalone endpoint БЕЗ WordPress nonce
  - **Оновлено `js/exit-intent.js`** (v1.0.0 → v1.1.0) - POST до public endpoint
  - **Оновлено `inc/class-events.php`** (v1.2.1 → v1.2.3) - Lead CPT integration + lenient nonce
  - **Створено `inc/smtp-config.php`** - PHPMailer SMTP для info@medici.agency
  - **Оновлено `inc/assets.php`** - Видалено wp_is_mobile() + defer attribute fixes
- 🔒 **Security Architecture (NO nonce for public forms):**
  - IP Rate Limiting - 10 requests / 5 min (Transients API)
  - Honeypot fields - website, url, company, address
  - User-Agent validation - minimum 10 chars
  - HTTP Referer validation - medici.agency domain only
- 📧 **SMTP Integration:**
  - Server: mail.adm.tools:465 (SSL)
  - From: info@medici.agency
  - Test endpoint: wp-admin/?test_smtp=1
- 🐛 **Виправлені помилки (7 issues):**
  1. Script not loading - `! wp_is_mobile()` blocked loading
  2. window.MediciExitIntent undefined - defer race condition
  3. trackEvent not a function - should be send()
  4. Leads not created - missing Lead_CPT integration
  5. HTTP 403 strict nonce - check_ajax_referer() too strict
  6. HTTP 403 persistent - WordPress nonce broken for public users
  7. **FINAL FIX** - public-form-handler.php bypasses nonce entirely
- 🔧 **Commits:**
  - `a527c58` - 🐛 Fix exit-intent: remove wp_is_mobile() + event_type
  - `b42faea` - 🐛 Fix exit-intent script loading - remove defer attribute
  - `ecd6626` - 🐛 Fix exit-intent: trackEvent → send() + better error handling
  - `ab0f6ce` - 🔧 Fix nonce verification + Lead CPT integration
  - `04a0900` - 📧 Add SMTP configuration for email sending
  - `34137e7` - 🔓 Lenient nonce verification for public forms (exit-intent)
  - `899fae6` - 🚀 NEW: Public Form Handler - NO WordPress nonce required!
- 📝 **Документація рішення (47 рядків troubleshooting notes):**
  - Створено `docs/EXIT-INTENT-POPUP.md` - повна документація 7 commits

### 5.7.1 - 2025-12-17

- ♻️ **Exit-Intent Refactor → GenerateBlocks Overlay Panels**
  - **Видалено custom solution** (647 рядків коду):
    - `js/exit-intent.js` (307 рядків)
    - `public-form-handler.php` (340 рядків)
  - **Створено GenerateBlocks рішення:**
    - `gutenberg/EXIT-INTENT-POPUP.html` (158 рядків) - Overlay Panel content
    - `css/components/exit-intent-overlay.css` (332 рядки) - Styling
    - `js/exit-intent-overlay.js` (133 рядки) - Form handler з Events API
  - **Оновлено `inc/assets.php`** (v2.0.0) - Conditional loading
- 🎯 **Переваги рішення:**
  - Використання GenerateBlocks Pro built-in Exit Intent trigger
  - Видалено 647 рядків custom коду
  - Кращий UX (Scale In анімація, backdrop blur)
  - Session-only tracking (localStorage)
- 🔧 **Commits:**
  - `58ff25b` - ♻️ REFACTOR: Exit-Intent → GenerateBlocks Overlay Panel

### 5.7.2 - 2025-12-17

- ✨ **Exit-Intent HYBRID Solution** - beeker1121 + GenerateBlocks
  - **Додано beeker1121 library:**
    - `js/vendor/bioep.min.js` - Exit-intent detection + cookie tracking
  - **Створено adapter:**
    - `js/exit-intent-hybrid.js` - Connects bioEp → GenerateBlocks Overlay Panel
  - **Оновлено `inc/assets.php`** (v2.1.0) - Enqueue 3 JS files
  - **Оновлено `gutenberg/EXIT-INTENT-POPUP.html`** - Hybrid instructions
- 🎯 **Гібридна архітектура:**
  1. bioEp детектить exit-intent (mouseout до верху екрану)
  2. Перевіряє cookie (30 днів)
  3. Тригерить GenerateBlocks Overlay Panel через `[data-gb-trigger-panel]`
  4. Форма відправляється через Events API
  5. bioEp зберігає cookie на 30 днів
- 🔧 **Commits:**
  - `c647c1e` - ✨ HYBRID: beeker1121 exit-intent + GenerateBlocks Overlay Panel

### 5.6 - 2025-12-15

- ♻️ **BEM CSS Refactoring** - Повний рефакторинг CSS до BEM naming convention
  - `css/components/forms.css` (v2.0.0) - `.consultation-form__*`, `.newsletter-form__*`
  - `css/components/navigation.css` (v2.0.0) - `.gbp-navigation__*` елементи + модифікатори
  - `css/components/cards.css` (v2.0.0) - 7 типів карток з BEM елементами
  - `css/layout/layout.css` (v2.0.0) - Footer `.gbp-footer__*` + `.scroll-to-top--visible`
  - Backwards compatibility секції для legacy class names
- ✨ **JavaScript js-\* Hooks** - Розділення styling від behavior
  - `js/forms-consultation.js` (v1.4.0) - `.js-consultation-form`, `.js-consultation-message`
  - `js/forms-newsletter.js` (v1.4.0) - `.js-newsletter-form`, `.js-newsletter-message`
  - `js/scripts.js` (v1.3.0):
    - ThemeModule: `.js-theme-toggle`
    - MobileMenuModule: `.js-mobile-toggle`, `.js-nav-menu`, `.js-nav-link`
    - NavigationModule: `--scrolled` BEM modifier
    - ActiveLinksModule: `--active` BEM modifier
    - ScrollToTopModule: `.js-scroll-to-top`, `--visible` modifier
    - AccessibilityModule: js-\* hooks для всіх інтерактивних елементів
- 📝 **Frontend Conventions документація:**
  - Додано `docs/FRONTEND-CONVENTIONS.md` (BEM + JS hooks guide)
  - Додано `docs/LEAD-TRACKING-RULES.md` (Lead validation module)
- 🔧 **Commits:**
  - `c25427e` - 📝 Add Frontend Conventions (BEM + JS hooks)
  - `3a7db80` - ♻️ Refactor forms.css to BEM naming convention
  - `001bf0c` - ♻️ Refactor navigation, cards, layout CSS to BEM naming convention
  - `e4e4419` - ✨ Add js-\* hooks to JavaScript for BEM separation

### 5.5 - 2025-12-15

- ✨ **Modern Solutions Analysis** - Дослідження та впровадження
  - Створено `docs/DECISIONS-2025-12-15.md` - Повний звіт рішень
  - Створено `inc/database-optimization.php` - Database indexes
  - Створено `inc/class-cache-manager.php` - Object caching з Transients
- 🗑️ **Видалено як overkill:**
  - CSS/JS Bundling (Webpack, PostCSS, Autoprefixer) - GeneratePress вже оптимізує
  - Unit Testing (PHPUnit) - Тема без складної бізнес-логіки
  - Web Workers - Не потрібні для маркетингового сайту
- ❌ **Оцінено та відхилено:**
  - Airbnb CSS Guide (`border: 0`) - мікро-оптимізація
  - GenerateBlocks Icon Sets - вже є Twemoji + вбудовані іконки
  - GeneratePress Child (Addison Hall) - Medici має кращу архітектуру
- 📋 **На майбутнє (заплановано):**
  - Fade-in анімації (scroll-triggered)
  - ~~BEM naming консистентність~~ → ✅ Виконано в 5.6
  - ~~JavaScript hooks (`js-*` класи)~~ → ✅ Виконано в 5.6
  - Lead Scoring система
  - Microsoft Clarity (heatmaps)
  - GA4 Events tracking
- 📊 **Lead Tracking рекомендації:**
  - UTM стратегія для соцмереж (Instagram, Facebook, LinkedIn)
  - Microsoft Clarity для heatmaps (безкоштовно)
  - GA4 Events для scroll depth, time on page, CTA clicks

### 5.4 - 2025-12-15

- ✅ **PHPStan Baseline → 0 помилок** - Всі 23 помилки виправлено!
  - Cast int→string для esc_html/esc_attr (12 файлів)
  - Видалено unreachable code (dashboard-analytics, rest-api)
  - Виправлено type mismatches (post_author, wp_insert_post)
- 🔄 **GitHub Actions CI/CD** - Автоматичні перевірки
  - PHPStan (Level 5) - Обов'язково
  - PHPCS (WordPress) - Warning
  - PHP Compatibility (8.1+) - Warning
  - CSS Balance Check - Обов'язково
  - Файл: `.github/workflows/ci.yml`
- 🪝 **Pre-commit Hooks** - Локальна перевірка перед комітом
  - PHPStan аналіз
  - CSS баланс дужок
  - Debug statements check (var_dump, print_r, die)
  - Встановлення: `./scripts/install-hooks.sh`
- 📝 **CLAUDE.md:** Оновлено секцію DevOps (CI + Hooks)

### 5.3 - 2025-12-15

- 🛠️ **Composer + PHPStan Integration** - DevOps foundation для code quality
  - Створено `composer.json` - Залежності та scripts
  - Створено `phpstan.neon` - PHPStan конфігурація (level 5)
  - Створено `phpstan-baseline.neon` - Baseline (тепер порожній)
  - Оновлено `.gitignore` - vendor/, node_modules/, IDE files
- 📦 **Composer Dependencies:**
  - phpstan/phpstan ^1.12 - Static analysis
  - szepeviktor/phpstan-wordpress ^1.3 - WordPress stubs
  - squizlabs/php_codesniffer ^3.11 - Code style
  - wp-coding-standards/wpcs ^3.1 - WordPress standards
  - phpcompatibility/phpcompatibility-wp ^2.1 - PHP compatibility
- 📝 **CLAUDE.md:** Додано секцію "DevOps & Code Quality"
- 🎯 **Composer Scripts:**
  - `composer phpstan` - Run PHPStan
  - `composer phpcs` - Run PHPCS
  - `composer lint` - Run all linting
  - `composer test` - Run all tests

### 5.2 - 2025-12-14

- ⚡ **CSS/JS Coverage Optimization** - Зменшення unused CSS/JS на ~48KB
  - Оновлено `inc/assets.php` (v1.5.0) - Conditional CSS loading
  - Оновлено `inc/widgets/widgets-init.php` (v1.0.2) - Видалено дублікат CSS enqueue
- 📦 **Conditional CSS Loading:**
  - `forms.css` - тільки на contact/consultation/single blog pages
  - `faq.css` - тільки на homepage та FAQ pages
  - `cards.css` - тільки на homepage/services/single posts
  - `widget-styles.css` - тільки коли sidebars активні
- 🚀 **GP Premium Smooth-Scroll вимкнено:**
  - Видалено `smooth-scroll.min.js` (-6.9KB)
  - Використовується native CSS `scroll-behavior: smooth`
- 🧹 **Cleanup:**
  - Видалено dead reference на `team-section-override.css`
  - Видалено дублікат widget CSS enqueue
- 📊 **Performance Impact:**
  - Homepage: -24KB, Blog archive: -27KB, Inner pages: -30KB

### 5.1 - 2025-12-14

- ✨ **Dashboard Analytics Features** (v1.5.0) - Комплексна система аналітики
  - Створено `inc/dashboard-analytics.php` (890 рядків) - Dashboard widgets, Lead Analytics, SEO Audit
  - Створено `inc/rest-api.php` (650 рядків) - REST API endpoints для лідів та блогу
  - Оновлено `functions.php` - Module loading + init hooks
- 📊 **Dashboard Widgets:**
  - Статистика лідів (всього, нові, кваліфіковані, закриті)
  - Статистика блогу (статті, перегляди, SEO бали)
  - Таблиці останніх лідів та популярних статей
- 🎯 **Lead Analytics (admin page):**
  - Воронка конверсії з візуалізацією етапів
  - Розподіл за послугами та UTM джерелами
  - Таймлайн лідів за 30 днів
  - Метрики: конверсія, всього, нові, qualified, closed
- 🔍 **SEO Audit Tool:**
  - 7-критеріальна перевірка: title, meta description, зображення, контент (800+ слів), H2 заголовки, internal links, alt text
  - Бали: good (80-100%), warning (50-79%), bad (0-49%)
  - SEO колонка в списку статей блогу
- ⚡ **Bulk Actions для лідів:**
  - Швидка зміна статусу: contacted, qualified, closed, lost
  - Експорт в CSV з BOM для Excel (українські символи)
- 🔗 **REST API Endpoints (medici/v1/):**
  - `GET /leads` - Список з фільтрами (status, service, search, orderby)
  - `GET /leads/{id}` - Деталі ліда
  - `PUT /leads/{id}/status` - Зміна статусу
  - `GET /leads/stats` - Статистика лідів
  - `GET /leads/export` - Експорт лідів
  - `GET /blog/stats` - Статистика блогу
  - `GET /blog/posts` - Список статей з views
  - `GET /blog/{id}/seo` - SEO аудит поста
  - `GET /blog/popular` - Популярні статті (public)
- 📝 **CLAUDE.md оновлено:**
  - Оновлено Core Features table (20 → 22 модулі)
  - Додано Dashboard_Analytics та REST_API до ключових функцій
- 🐛 **Bugfix:** `get_score_level()` TypeError - cast round() to int

### 4.9 - 2025-12-14

- ♻️ **CSS Рефакторинг: видалення дублювань та узгодження архітектури**
  - Видалено `@font-face` дублювання з `css/core/core.css` (тепер тільки в `critical.css`)
  - Видалено `body` styles дублювання з `css/core/variables.css`
  - Видалено застарілі коментарі про `overflow-x` з `core.css` та `critical.css`
  - Видалено `.gbp-grid-3` дублювання з `critical.css` (є в `layout.css`)
  - Оновлено документацію CSS архітектури в header `critical.css` (v1.2.0)
- 🔧 **Нормалізація line endings (CRLF → LF)**
  - Всі CSS файли нормалізовані до LF (Unix style)
  - Відповідність `.gitattributes` конфігурації (`text=auto eol=lf`)
- 📝 **Оновлена документація `critical.css`:**
  - Додано коментарі про зв'язок з іншими CSS файлами
  - Пояснено архітектуру Critical CSS для FCP/LCP оптимізації
  - Видалено застарілу секцію "Grid System" (стилі в `layout.css`)
- 📦 **Змінені файли:**
  - `css/core/core.css` - видалено @font-face дублювання (~30 рядків)
  - `css/core/variables.css` - видалено body styles (~10 рядків)
  - `css/critical.css` - оновлена документація, видалено grid (~50 рядків)
- 🐛 **Вирішено конфлікти rebase з main branch:**
  - Конфлікт у `variables.css` - збережено рефакторинг
  - Конфлікт у `critical.css` - збережено оновлену документацію
  - Видалено `team-section-override.css` (вже видалений в main)

### 4.8 - 2025-12-13

- 🎯 **Lead Management System** (v1.4.0) - Повноцінна CRM для лідів
  - Створено `inc/lead-cpt.php` - Custom Post Type `medici_lead`
  - Створено `inc/lead-integrations.php` - Email, Telegram, Google Sheets integrations
  - Створено `inc/lead-admin-settings.php` - Admin settings page
  - Оновлено `inc/class-events.php` - Integration з Lead CPT та інтеграціями
  - Оновлено `functions.php` - Module loading (3 нові модулі)
  - Створено `gutenberg/CONSULTATION-FORM.html` - GeneratePress Hook Element
- ✨ **Lead CPT Features:**
  - Meta fields: name, email, phone, service, message, page*url, utm*\*, status
  - Statuses: new 🆕, contacted 📞, qualified ✅, closed 🎉, lost ❌
  - Admin columns: Name, Email, Phone, Service, Status, Date (all sortable)
  - Meta boxes: Lead details, UTM parameters, Status selector
  - Auto-title: "Лід #{ID} - {Name}"
  - Cannot create manually (only via form)
- 📧 **Email Integration:**
  - HTML email template з деталями ліда
  - Reply-To header (user email)
  - Admin email configurable (fallback to WordPress admin_email)
- 📱 **Telegram Integration:**
  - Markdown message format
  - Bot Token + Chat ID configuration
  - @BotFather + @getidsbot setup instructions
  - Supports groups (negative chat IDs)
- 📊 **Google Sheets Integration:**
  - Apps Script Web App endpoint
  - JSON POST with lead data
  - Auto-append row to sheet
  - Setup instructions з Apps Script code
- 🔧 **Admin Settings Page:**
  - Path: **Ліди > Інтеграції**
  - 3 sections: Email, Telegram, Google Sheets
  - Inline setup instructions (Telegram: 6 steps, Google Sheets: 11 steps)
  - Apps Script code included
- 📝 **CLAUDE.md оновлено:**
  - Додано секцію "Lead Management System" (215 рядків)
  - Оновлено файлову структуру (20 модулів)
  - Оновлено Core Features table
  - Оновлено API Reference (Lead_CPT, Lead_Integrations, Lead_Admin_Settings)
  - Workflow діаграма lead processing
- 📦 **Файли:**
  - inc/lead-cpt.php (487 рядків) - NEW
  - inc/lead-integrations.php (535 рядків) - NEW
  - inc/lead-admin-settings.php (345 рядків) - NEW
  - gutenberg/CONSULTATION-FORM.html (118 рядків) - NEW
  - inc/class-events.php (+13 рядків) - UPDATED
  - functions.php (+54 рядки) - UPDATED
  - CLAUDE.md (+250 рядків) - UPDATED

### 5.0 - 2025-12-14

- 🧹 **Актуалізація документації та очищення проекту**
  - ✅ Видалено застарілі звіти аудиту (дата: 2025-12-12) → архів `docs/archive/`
  - ✅ Перевірено та підтверджено: всі 5 критичних проблем Phase 1 вже виправлені
  - ✅ Оновлено CLAUDE.md - видалено згадки про неіснуючі `patterns/` директорію
  - ✅ Оновлено секцію "GeneratePress Elements" - правильна архітектура без patterns
  - ✅ Виправлено політику створення файлів - використовувати `gutenberg/` замість `patterns/`
- ✅ **Підтверджено стан коду (актуально на 2025-12-14):**
  - emoji disable дублікат - ❌ не існує (виправлено раніше)
  - JS файли в css/modules/blog/ - ❌ не існують (видалено раніше)
  - HTML синтаксис blog-meta-fields.php:182 - ✅ правильний (`<code>`)
  - CSS залежність twemoji-local.php:97 - ✅ правильна (`medici-child-theme`)
  - Версія inc/assets.php - ✅ оновлена до 1.3.5
- 📁 **Архівовано 9 файлів звітів:**
  - AUDIT-REPORT.md, COMPONENT-MAP.md, INCONSISTENCIES-REPORT.md
  - CSS-AUDIT-REPORT.md, CSP-SECURITY-REPORT.md, VALIDATION-REPORT.md
  - TEMPLATES-AUDIT-\*.md (3 файли)
- 📝 **Документація актуалізована:**
  - Видалено Change Log 4.7 (застарілі звіти)
  - Оновлено примітки для AI асистентів
  - Виправлено всі згадки про patterns/ → gutenberg/
- 🗂️ **Реструктуризація документації (19 → 5 файлів в корні, -74%):**
  - Створено `docs/coding-rules/` - 3 файли CODING-RULES-\*.md
  - Створено `docs/style-rules/` - 4 файли STYLE-RULES-\*.md
  - Створено `docs/guides/` - QUICK-REFERENCE, CHECKLIST, BEST-PRACTICES
  - Створено `docs/api/` - EVENTS-API.md
  - Створено `docs/bot/` - README_BOT, QUICKSTART_BOT
  - Переміщено DOCS-INDEX.md → docs/
  - Оновлено всі посилання в CLAUDE.md (14 місць)
  - В корні залишилось тільки: CLAUDE.md, CODING-RULES.md, STYLE-RULES.md, TODO.md, CHANGELOG.md

### 4.6 - 2025-12-09

- ✨ **Schema Markup розширено** (v1.3.5)
  - Додано FAQPage schema - автоматичне визначення FAQ з details/summary блоків або заголовків з питаннями
  - Додано HowTo schema - автоматичне визначення інструкцій з ordered lists або заголовків "Крок X"
  - Додано VideoObject schema - підтримка YouTube, Vimeo та self-hosted відео
  - Всі schema functions з повною типізацією (strict_types=1)
  - Автоматичне визначення контенту - не потрібно вручну додавати schema markup
- 🗺️ **XML Sitemap оптимізація** (v1.3.5)
  - Створено inc/sitemap-optimization.php - новий модуль для WordPress Core sitemap
  - Priority та changefreq для всіх типів контенту (homepage: 1.0, blog: 0.9, posts: 0.7, categories: 0.6)
  - Exclude admin/system pages (wp-admin, wp-login, cart, checkout, privacy-policy)
  - Exclude system post types (attachments, revisions, nav_menu_item)
  - Підтримка medici_blog CPT та medici_blog_category taxonomy
  - Вимкнено user sitemap (author pages)
  - Max 1000 entries per sitemap (замість default 2000)
- 📝 **Документація оновлена:**
  - CLAUDE.md - оновлено Core Features з новими модулями
  - functions.php - додано sitemap-optimization.php до priority modules
  - style.css - version bump до 1.3.5
  - Додано schema functions до ключових функцій
- 📦 **Файли:**
  - inc/schema-medical.php - +403 рядки (FAQPage, HowTo, VideoObject)
  - inc/sitemap-optimization.php - новий файл (260 рядків)
  - functions.php - оновлено module loading

### 4.5 - 2025-12-08

- 📝 **Blog Admin модулі - аудит та оновлення документації**
  - Перевірено стан всіх admin модулів (Meta boxes, Category picker, Settings page - АКТИВНІ)
  - Dashboard widgets НЕ реалізовані
  - Видалено застарілу інформацію про blog-admin.php та blog-admin-controller.php
  - Оновлено CLAUDE.md Architecture notes (примітка #2)
  - Файли: CLAUDE.md, TODO.md, CHANGELOG.md

### 4.5 - 2025-12-11

- ✅ **Table of Contents (TOC) реалізовано для блогу!**
  - Створено `single-medici_blog.php` - новий template для окремої статті блогу
  - Template включає двоколонковий layout (content + sidebar)
  - Sidebar містить:
    - **Table of Contents** - автогенерація з H2/H3 заголовків через JavaScript
    - **Newsletter форма** - підписка на розсилку
    - **Back to Blog** - кнопка повернення до списку статей
  - TOC Features:
    - Автоматична генерація з заголовків статті (H2, H3)
    - Smooth scroll до секції при кліку
    - Scroll spy - підсвічування активного пункту при прокрутці
    - Активний пункт змінює колір та фон
    - Mobile-friendly responsive design
  - Breadcrumb навігація (Головна / Блог / Категорія / Стаття)
  - Article meta (дата, час читання, перегляди)
  - Prev/Next навігація між статтями (в межах категорії)
  - Related articles section (3 схожі статті)
  - Автоматичний підрахунок переглядів статті
- 📝 **Документація оновлена:**
  - CLAUDE.md - оновлено файлову структуру (додано single-medici_blog.php)
  - CLAUDE.md - оновлено "Not Available as Shortcodes" секцію (TOC тепер implemented!)
  - Всі необхідні helper функції вже існують:
    - `medici_get_category_style()` - стилізація категорій
    - `medici_get_related_blog_posts()` - схожі статті
- 🎯 **JavaScript та CSS:**
  - js/modules/blog/blog-single.js - TOC generation, smooth scroll, scroll spy (вже існував)
  - css/modules/blog/blog-single.css - стилі sidebar, TOC, newsletter (вже існував)
  - Підключення через inc/assets.php (conditional на is_singular('medici_blog'))
- 📦 **Файли:**
  - single-medici_blog.php - новий файл (245 рядків)
  - CLAUDE.md - оновлено

### 4.4 - 2025-12-08

- 🐛 **КРИТИЧНЕ ВИПРАВЛЕННЯ: CSS Parsing Errors** - Виправлено незакриті фігурні дужки
  - **Проблема**: Незакриті `}` у 6 основних CSS файлах ламали парсинг стилів
  - **Вплив**: Могло впливати на відображення всього сайту, включно зі "Змістом статті"

- 🔧 **Виправлені файли (6 файлів):**
  1. **`css/core/core.css`** - Відновлено повну структуру reset-стилів, fonts, CLS-фіксів
  2. **`css/core/variables.css`** - Закрито `:root` та `[data-theme="dark"]` блоки
  3. **`css/components/navigation.css`** - Виправлено media queries та keyframe animations
  4. **`css/components/sections.css`** - Закрито всі wrapper та typography блоки
  5. **`css/components/cards.css`** - Закрито 7 типів карток (global, service, team, value, testimonial, approach, event)
  6. **`css/layout/layout.css`** - Закрито Grid, Hero, Footer, Utilities блоки

- ✅ **Ключові виправлення (20 пунктів):**
  1. У `core.css` відновлено повну структуру reset-стилів, підключення локальних шрифтів Montserrat та CLS-фіксів
  2. У `variables.css` коректно закрито блок `:root` і `[data-theme="dark"]`, збережено Utopia-типографію
  3. Винесено тільки змінні у `:root`, а стилі для `body` та карток залишено окремими правилами
  4. У `navigation.css` виправлено стилі для fixed-хедера, лого, центрального меню, правого блоку, перемикача теми
  5. Мобільне меню тепер відкривається через клас `menu--open` із анімацією `slideDown`
  6. У `sections.css` відновлено базовий wrapper `.gbp-section`, внутрішній контейнер та типографіку
  7. `cards.css` приведено до стабільної структури: 7 типів карток з узгодженими бордерами та градієнтами
  8. Для карток збережено семантику hover-ефектів (підйом, зміна тіні, анімація заголовка) без `!important`
  9. У `layout.css` впорядковано грід-системи на 3 і 4 колонки з окремими медіа-правилами
  10. Hero-секція отримала чітку структуру: флекс-центрування, обмеження ширини, clamp-типографіка
  11. Футер розділено на компанію, навігацію, контакти, бейджі з грід-layout'ом
  12. Додано адаптивні стилі футера для планшетів і мобільних (одна колонка, центровані блоки)
  13. В `core.css` включено універсальні стилі для SVG-емоджі (коректні розміри, вирівнювання)
  14. Встановлено `contain: layout style` для основних компонентів (продуктивність)
  15. Для темної теми посилено тіні, контраст тексту та фонів
  16. Кнопка `scroll-to-top` отримала чіткі стани: прихована за замовчуванням, плавна поява
  17. Для навігації та футера збережено стилі SVG-іконок (телефон, емоджі)
  18. Всі адаптивні блоки тепер дотримуються єдиних брейкпоінтів `767px` і `1024px`
  19. Збережено темну тему для карток з градієнтним фоном та зеленим бордером testimonial-карток
  20. Сукупно ці виправлення зняли помилки парсингу CSS, покращили структуру та підготовили базу для стабільної роботи

- 📊 **Валідація CSS:**
  - Перевірено баланс `{` та `}` у всіх 11 CSS файлах
  - Всі файли мають ідеальний баланс дужок (рівна кількість відкриваючих і закриваючих)
  - Результати перевірки: core.css (17/17), variables.css (8/8), navigation.css (41/41), cards.css (30/30), layout.css (63/63), blog-single.css (119/119), blog-new.css (87/87), тощо

- 📝 **Додано нову секцію в CLAUDE.md:**
  - **"🚨 КРИТИЧНІ ПРАВИЛА ДЛЯ CSS ФАЙЛІВ"** (234 рядки)
  - Жорсткі вимоги при роботі з CSS (заборонені та обов'язкові практики)
  - Checklist перед/під час/після редагування CSS
  - Критичні файли CSS з підвищеною увагою
  - 4 типові помилки з прикладами (НЕПРАВИЛЬНО/ПРАВИЛЬНО)
  - Золоте правило CSS: `Відкриваючих { = Закриваючих }`
  - Рекомендовані інструменти (VS Code Extensions, CLI Tools, Git Pre-commit Hook)
  - Troubleshooting guide: Що робити якщо CSS зламався? (4 кроки)
  - Checklist перевірки після виправлення (7 пунктів: головна, блог, single post, footer, navigation, dark theme, responsive)

- 🛡️ **Превентивні заходи:**
  - Додано скрипт перевірки балансу дужок для всіх CSS файлів
  - Рекомендовано встановити Stylelint та Bracket Pair Colorizer
  - Додано Git Pre-commit Hook (опціонально) для автоматичної перевірки

### 4.3 - 2025-12-07

- 🗑️ **Видалено Font Awesome систему** - перехід на Twemoji-only
  - Видалено inc/emoji-to-fontawesome.php (60+ емоджі mapping)
  - Видалено css/components/fontawesome-emoji.css (стилі іконок)
  - Видалено css/fontawesome/ (all.min.css + 3 webfonts, 370KB total)
  - Оновлено inc/assets.php - видалено Font Awesome enqueue
  - Оновлено functions.php - видалено emoji-to-fontawesome.php з modules
  - **Причина**: Twemoji покриває всі 4009 емоджі, Font Awesome надлишковий
- ✨ **Twemoji Local - єдина система** (v1.0.1)
  - Оновлено inc/twemoji-local.php - видалено згадки про гібридну систему
  - Priority змінено з 20 на 10 (стандартний WordPress priority)
  - Twemoji тепер ЄДИНА система відображення емоджі
- 📝 CLAUDE.md оновлено:
  - Видалено секцію "Emoji to Font Awesome Auto-Replacement System" (206 рядків)
  - Оновлено "Twemoji Local Integration" - прибрано згадки про Font Awesome
  - Оновлено Core Features (пункт 14 - тільки Twemoji)
  - Оновлено API Reference - видалено Font Awesome функції
  - Оновлено файлову структуру
- 📁 Видалені файли:
  - inc/emoji-to-fontawesome.php
  - css/components/fontawesome-emoji.css
  - css/fontawesome/all.min.css (101KB)
  - css/fontawesome/webfonts/fa-solid-900.woff2 (153KB)
  - css/fontawesome/webfonts/fa-regular-400.woff2 (25KB)
  - css/fontawesome/webfonts/fa-brands-400.woff2 (115KB)

### 4.2 - 2025-12-07

- ✨ **Twemoji Local Integration** (v1.0.0) - Початкова реалізація
  - Завантажено 4009 SVG емоджі локально (11MB) з GitHub
  - Створено inc/twemoji-local.php - модуль локального Twemoji
  - Завантажено twemoji.min.js 15.1.0 (18KB) локально
  - ✅ **CSP compliant** - всі файли на власному сервері
  - PHP функції: medici_get_twemoji_base_url(), medici_twemoji_assets_exist(), medici_get_twemoji_info()
  - Admin notice якщо SVG assets не знайдені
- 📝 CLAUDE.md оновлено:
  - Додано секцію "Twemoji Local Integration"
  - Оновлено Core Features (пункт 15)
  - Оновлено API Reference (Twemoji функції)
  - Оновлено файлову структуру (js/twemoji/, assets/twemoji/)
- 📁 Файлова структура:
  - inc/twemoji-local.php (новий модуль)
  - js/twemoji/twemoji.min.js (18KB)
  - assets/twemoji/svg/ (4009 файлів, 11MB)
- 🔧 functions.php оновлено - додано twemoji-local.php в priority_modules

### 4.1 - 2025-12-07

- 🗑️ **Видалено SVG Icons систему** (тимчасово замінено на Font Awesome)
  - Видалено inc/svg-icons.php
  - Видалено css/components/svg-icons.css
  - Видалено з inc/assets.php та functions.php
  - Видалено секцію "SVG Icons System" з документації
  - Причина: Font Awesome покриває всі use cases та краще підтримується

### 4.0 - 2025-12-07

- ✨ **Emoji to Font Awesome Auto-Replacement System** (v1.0.0)
  - Автоматична заміна 60+ емоджі на Font Awesome іконки
  - WordPress фільтри: the_content, the_title, the_excerpt, widget_text
  - PHP functions: medici_render_fa_from_emoji(), medici_get_fa_class_for_emoji()
  - CSS стилі: fontawesome-emoji.css (responsive, dark theme, accessibility)
  - **Font Awesome 6.5.1 локальні файли** (css/fontawesome/all.min.css + webfonts/)
  - ✅ **CSP compliant** - no external requests, no CDN warnings
  - Static cache для performance
  - Повна типізація (strict_types=1)
- 📝 CLAUDE.md оновлено з повною документацією нової системи
- 📁 Файлова структура оновлена:
  - inc/emoji-to-fontawesome.php
  - css/components/fontawesome-emoji.css
  - css/fontawesome/all.min.css (101KB)
  - css/fontawesome/webfonts/ (fa-solid-900, fa-regular-400, fa-brands-400)

### 3.3 - 2025-12-07

- Типізація assets.php (100% функцій)
- Shortcodes Reference оновлено (3 real shortcodes)

### 3.2 - 2025-12-05

- Bulk Update скрипти (post_type fix)
- Blog background fix

### 3.1 - 2025-12-05

- Dark theme fixes
- PHP Parse error fix (типографічні лапки)

### 3.0 - 2025-12-02

- Common Pitfalls додано (CSS refactoring lessons)
- CHANGELOG.md створено

**Last Updated:** 2025-12-18
**Theme Version:** 2.0.0
**Documentation Version:** 6.1
