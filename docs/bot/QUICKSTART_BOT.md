# ⚡ Швидкий Старт - Telegram Бот Медічі

> 5 хвилин до запуску покращеного бота!

## 🚀 Швидкі Кроки

### 1. Встановлення залежностей

```bash
pip install python-telegram-bot==21.0.1
```

### 2. Отримання Bot Token

1. Відкрий [@BotFather](https://t.me/BotFather)
2. Надішли: `/newbot`
3. Ім'я: `Medici Bot`
4. Username: `medici_bot` (має бути унікальний)
5. Скопіюй токен

### 3. Налаштування змінних

```bash
export TELEGRAM_BOT_TOKEN="твій_токен_тут"
export MANAGER_CHAT_ID="твій_chat_id"  # опціонально
```

### 4. Додавання PDF файлів

Додай файли в директорію `files/`:

```bash
# Створи символічні посилання або скопіюй файли
cp /path/to/checklist_google_ads.pdf files/
cp /path/to/checklist_doctor_facebook.pdf files/
cp /path/to/guide_cpl_roas.pdf files/
cp /path/to/guide_10_mistakes.pdf files/
cp /path/to/guide_landing_page.pdf files/
```

### 5. Запуск

```bash
python3 medici_bot_enhanced.py
```

## ✅ Перевірка

Відкрий свого бота в Telegram і надішли `/start`

Має з'явитись повідомлення:

```
Привіт, [твоє ім'я]! 👋

Я бот агенції медичного маркетингу «Медічі».

🎯 Допомагаю лікарям та медичним клінікам...

[Кнопки меню]
```

## 🎯 Тестування Функцій

### Тест 1: Матеріали

1. Натисни **"📚 Матеріали"**
2. Обери будь-який чеклист
3. Має завантажитись PDF файл

### Тест 2: Калькулятор

1. Натисни **"🧮 Калькулятор"**
2. Обери **"💰 Розрахувати CPL"**
3. Введи бюджет: `5000`
4. Введи ліди: `10`
5. Має показатись результат з оцінкою

### Тест 3: Квіз

1. Натисни **"🎮 Квіз"**
2. Натисни **"▶️ Почати квіз"**
3. Відповідай на питання
4. В кінці отримай результат

### Тест 4: Статистика

1. Натисни **"📊 Моя статистика"**
2. Має показати твої дії та бейджі

### Тест 5: Аналіз файлу

1. Натисни **"📎 Аналіз файлу"**
2. Надішли будь-яке зображення
3. Обери тип матеріалу
4. Дочекайся прогрес-бару (0% → 100%)
5. Отримай аналіз з оцінкою

## 🔧 Налаштування для Production

### 1. Systemd Service

Створи `/etc/systemd/system/medici-bot.service`:

```ini
[Unit]
Description=Medici Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/home/your_user/medici
Environment="TELEGRAM_BOT_TOKEN=your_token_here"
ExecStart=/usr/bin/python3 medici_bot_enhanced.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 2. Активація

```bash
sudo systemctl daemon-reload
sudo systemctl enable medici-bot
sudo systemctl start medici-bot

# Перевірка
sudo systemctl status medici-bot
```

### 3. Логи

```bash
# Реальний час
sudo journalctl -u medici-bot -f

# Останні 100 рядків
sudo journalctl -u medici-bot -n 100
```

## 🐛 Швидке Виправлення Проблем

### Бот не відповідає?

```bash
# Перевір токен
echo $TELEGRAM_BOT_TOKEN

# Перевір процес
ps aux | grep medici_bot

# Перезапусти
pkill -f medici_bot_enhanced
python3 medici_bot_enhanced.py
```

### PDF не відправляються?

```bash
# Перевір файли
ls -lah files/

# Права доступу
chmod 644 files/*.pdf
```

### База даних заблокована?

```bash
# Закрий бота
pkill -f medici_bot

# Видали lock
rm medici_bot.db-journal

# Запусти знову
python3 medici_bot_enhanced.py
```

## 📊 Моніторинг

### Статистика з БД

```bash
# Встанови sqlite3
sudo apt-get install sqlite3

# Підключись до БД
sqlite3 medici_bot.db

# Запити
sqlite> SELECT COUNT(DISTINCT user_id) FROM user_profiles;
sqlite> SELECT COUNT(*) FROM events;
sqlite> SELECT action, COUNT(*) FROM events GROUP BY action;
```

### Топ користувачі

```sql
SELECT
    u.name,
    u.files_uploaded,
    u.materials_downloaded,
    u.quizzes_completed
FROM user_profiles u
ORDER BY u.files_uploaded DESC
LIMIT 10;
```

## 🎯 Наступні Кроки

1. ✅ Додай реальні PDF матеріали в `files/`
2. ✅ Налаштуй `MANAGER_CHAT_ID` для отримання заявок
3. ✅ Протестуй всі функції
4. ✅ Налаштуй systemd для автозапуску
5. ✅ Налаштуй моніторинг та логування
6. ✅ Додай свої питання до квізу
7. ✅ Кастомізуй тексти під свій бренд

## 📚 Детальна Документація

Дивись [README_BOT.md](README_BOT.md) для повної документації.

## 🆘 Підтримка

- 📧 Email: info@medici.agency
- 🌐 Сайт: medici.agency
- 📱 Telegram: [@medici_agency](https://t.me/medici_agency)

---

**Успішного запуску! 🚀**
