# Git Sparse Checkout - Оптимізація Dev Workflow

## 🎯 Мета

Прискорення розробки через виключення з Git checkout непотрібних директорій та файлів, які:
- Містять велику кількість файлів (4,349 з 4,616 = **94%**)
- Рідко змінюються
- Займають багато місця (19.7MB з 20.5MB)

## 📊 Статистика до оптимізації

| Директорія | Розмір | Файлів в Git | Опис |
|------------|--------|--------------|------|
| `assets/` | 11MB | ~2000 | Fonts, images, twemoji |
| `skills/` | 8.7MB | ~1500 | AI skills для Claude Code |
| `docs/` | 446KB | ~150 | Документація (крім coding-rules) |
| `fonts/` | 191KB | ~50 | Web fonts |
| `bot/` | 110KB | ~30 | Bot scripts |
| `scripts/` | 58KB | ~20 | Build scripts |
| **ВСЬОГО** | **~20MB** | **~4,349** | **94% всіх файлів!** |

## 🚀 Рішення: Git Sparse Checkout v2

### Що це?

Git Sparse Checkout дозволяє клонувати та синхронізувати тільки потрібні файли/директорії, залишаючи непотрібні виключеними з робочої директорії.

**Переваги:**
- ✅ Швидший `git clone` (в 10-20 разів!)
- ✅ Швидший `git pull` (оновлюються тільки потрібні файли)
- ✅ Менше місця на диску
- ✅ Швидше індексування IDE (VS Code, PHPStorm)
- ✅ Локальна конфігурація (не впливає на інших розробників)

**Недоліки:**
- ⚠️ Директорії фізично відсутні в робочій директорії
- ⚠️ Треба вручну увімкнути/вимкнути при потребі

## 📁 Структура файлів

```
.git/info/
├── sparse-checkout              # Конфігурація (що виключити/включити)
├── sparse-checkout-toggle.sh    # Скрипт перемикання режимів
└── exclude                      # Локальне ігнорування (build artifacts)
```

## 🔧 Швидкий старт

### 1️⃣ Активувати Dev режим (Sparse Checkout)

```bash
# Метод 1: Використати скрипт (РЕКОМЕНДОВАНО)
.git/info/sparse-checkout-toggle.sh enable

# Метод 2: Вручну
git config core.sparseCheckout true
git sparse-checkout init --no-cone
git sparse-checkout set --stdin < .git/info/sparse-checkout
git checkout HEAD -- .
```

### 2️⃣ Перевірити статус

```bash
.git/info/sparse-checkout-toggle.sh status
```

### 3️⃣ Деактивувати (повернутись до Full режиму)

```bash
.git/info/sparse-checkout-toggle.sh disable
```

## 📋 Що виключається / залишається?

### ❌ ВИКЛЮЧЕНО (Dev режим)

```
assets/          # Fonts, images, twemoji (11MB)
bot/             # Bot scripts (110KB)
docs/            # Документація (крім coding-rules!)
fonts/           # Web fonts (191KB)
scripts/         # Build scripts (58KB)
skills/          # AI skills для Claude Code (8.7MB)
```

### ✅ ЗАЛИШЕНО (критичні файли)

```
# WordPress Core
functions.php
style.css
theme.json
inc/
css/
js/
templates/
gutenberg/
plugins/

# Config files
package.json
package-lock.json
composer.json
composer.lock
.eslintrc.json
.prettierrc.json
.stylelintrc.json

# Documentation (мінімум для LLM)
README.md
CHANGELOG.md
CLAUDE.md
CODING-RULES.md
TODO.md
docs/coding-rules/    # КРИТИЧНО для LLM!

# CI/CD
.github/
```

## 🎓 Детальні інструкції

### Для нового клону (швидкий старт)

```bash
# 1. Клонувати репозиторій
git clone <repo-url> medici

# 2. Перейти в директорію
cd medici

# 3. Активувати Dev режим
.git/info/sparse-checkout-toggle.sh enable

# Результат: Тільки 267 файлів замість 4,616!
```

### Для існуючого репозиторію

```bash
# 1. Переконатись що немає незакомічених змін
git status

# 2. Активувати Dev режим
.git/info/sparse-checkout-toggle.sh enable

# 3. Якщо щось пішло не так - деактивувати
.git/info/sparse-checkout-toggle.sh disable
```

### Тимчасово отримати доступ до виключених файлів

```bash
# Метод 1: Деактивувати Sparse Checkout
.git/info/sparse-checkout-toggle.sh disable

# Метод 2: Отримати конкретний файл (не рекомендовано)
git checkout HEAD -- assets/fonts/some-font.woff2

# Метод 3: Переглянути файл без checkout
git show HEAD:assets/fonts/some-font.woff2 > /tmp/some-font.woff2
```

## ⚙️ Додаткові оптимізації

### 1. Git Shallow Clone (зменшення історії)

```bash
# Клонувати тільки останні 10 комітів
git clone --depth 10 <repo-url>

# Або конвертувати існуючий репозиторій
git fetch --depth 10
git gc --prune=all
```

**Економія:** ~50% розміру `.git/` директорії

### 2. Git LFS (для великих бінарних файлів)

```bash
# Встановити Git LFS
git lfs install

# Налаштувати для fonts та images
git lfs track "assets/fonts/**/*.woff2"
git lfs track "assets/images/**/*.{png,jpg,svg}"
```

**Економія:** Бінарні файли зберігаються зовні, в `.git/` тільки посилання

### 3. Локальне ігнорування (.git/info/exclude)

Вже налаштовано! Ігнорує:
- `node_modules/` (після `npm install`)
- `vendor/` (після `composer install`)
- IDE файли (`.vscode/`, `.idea/`)
- Build artifacts (`*.log`, `*.tmp`, `dist/`)
- OS файли (`.DS_Store`, `Thumbs.db`)

## 📈 Очікувані результати

| Метрика | До оптимізації | Після оптимізації | Прискорення |
|---------|----------------|-------------------|-------------|
| Файлів в checkout | 4,616 | ~267 | **17x менше** |
| Розмір робочої директорії | 20.5MB | ~1MB | **20x менше** |
| `git clone` час | ~30 сек | ~3 сек | **10x швидше** |
| `git pull` час | ~5 сек | <1 сек | **5x швидше** |
| VS Code індексування | ~10 сек | ~1 сек | **10x швидше** |

## 🐛 Troubleshooting

### Проблема: Директорія все ще присутня після enable

**Рішення:**
```bash
# Перезавантажити робочу директорію
git checkout HEAD -- .

# Або форсовано видалити
rm -rf assets bot docs fonts scripts skills
git checkout HEAD -- .
```

### Проблема: Потрібен файл з виключеної директорії

**Рішення:**
```bash
# Тимчасово деактивувати
.git/info/sparse-checkout-toggle.sh disable

# Зробити роботу
# ...

# Активувати знову
.git/info/sparse-checkout-toggle.sh enable
```

### Проблема: `npm install` або `composer install` не працює

**Відповідь:** Це нормально! `node_modules/` та `vendor/` ігноруються через `.git/info/exclude`, але працюють локально.

### Проблема: CI/CD ламається

**Відповідь:** Sparse checkout — це **локальна** конфігурація. CI/CD завжди робить повний checkout і не залежить від вашої локальної конфігурації.

## 🔄 Інтеграція з workflow

### Коли використовувати Dev режим?

✅ **ТАК:**
- Робота з PHP кодом (`inc/`, `functions.php`)
- Редагування CSS/JS (`css/`, `js/`)
- Робота з GenerateBlocks (`gutenberg/`)
- Щоденна розробка

❌ **НІ:**
- Оновлення документації в `docs/`
- Робота з AI skills (`skills/`)
- Додавання нових assets (`assets/`)
- Робота з bot scripts (`bot/`)

### Git команди (не змінюються!)

```bash
# Всі звичні команди працюють як раніше
git status
git add .
git commit -m "message"
git push
git pull

# Sparse checkout ПРОЗОРИЙ для Git операцій!
```

## 📚 Додаткові ресурси

- [Git Sparse Checkout Documentation](https://git-scm.com/docs/git-sparse-checkout)
- [Git LFS Documentation](https://git-lfs.github.com/)
- [Git Shallow Clone](https://git-scm.com/docs/git-clone#Documentation/git-clone.txt---depthltdepthgt)

## 🚨 ВАЖЛИВО!

1. ⚠️ **Backup перед активацією!** (або переконайтесь що все закомічено)
2. ⚠️ **Не редагуйте `.git/info/sparse-checkout` вручну** (використовуйте скрипт)
3. ⚠️ **CI/CD не залежить** від вашого локального sparse checkout
4. ⚠️ **Інші розробники не побачать** ваших локальних налаштувань

## 📞 Підтримка

При проблемах:
1. Перевірити статус: `.git/info/sparse-checkout-toggle.sh status`
2. Деактивувати: `.git/info/sparse-checkout-toggle.sh disable`
3. Перевірити `git status` та `git diff`
4. Активувати знову: `.git/info/sparse-checkout-toggle.sh enable`

---

**Версія:** 1.0.0
**Дата:** 2025-12-19
**Автор:** Claude Code (Anthropic)
