# Git Templates - Dev Mode Configuration

## 📁 Що це?

Шаблони конфігурації для налаштування **Git Sparse Checkout** (Dev режим).

**Мета:** Прискорити розробку через виключення непотрібних директорій (assets, bot, docs, fonts, scripts, skills).

## 🚀 Швидке налаштування

```bash
# 1. Запустити setup скрипт
.git-templates/setup-dev-mode.sh

# 2. Активувати Dev режим
./git-dev-mode enable

# 3. Перевірити статус
./git-dev-mode status
```

## 📋 Файли

| Файл | Опис |
|------|------|
| `sparse-checkout.template` | Шаблон конфігурації Git Sparse Checkout |
| `exclude.template` | Шаблон локального ігнорування (build artifacts) |
| `sparse-checkout-toggle.sh` | Скрипт перемикання режимів |
| `setup-dev-mode.sh` | Автоматичне налаштування |
| `README.md` | Ця документація |

## 🔧 Ручне налаштування

Якщо setup скрипт не працює:

```bash
# 1. Копіювати конфігурації
cp .git-templates/sparse-checkout.template .git/info/sparse-checkout
cp .git-templates/exclude.template .git/info/exclude
cp .git-templates/sparse-checkout-toggle.sh .git/info/

# 2. Зробити скрипт executable
chmod +x .git/info/sparse-checkout-toggle.sh

# 3. Створити symlink
ln -sf .git/info/sparse-checkout-toggle.sh git-dev-mode

# 4. Активувати
./git-dev-mode enable
```

## 📖 Повна документація

**docs/devops/GIT-SPARSE-CHECKOUT.md** - Детальний гайд з:
- Troubleshooting
- Додатковими оптимізаціями (Git LFS, Shallow Clone)
- Best practices
- FAQ

## 🎯 Результати

| Метрика | До | Після | Прискорення |
|---------|-----|-------|-------------|
| Файлів | 4,616 | ~267 | **17x менше** |
| Розмір | 20.5MB | ~1MB | **20x менше** |
| `git clone` | ~30 сек | ~3 сек | **10x швидше** |
| `git pull` | ~5 сек | <1 сек | **5x швидше** |

## ⚠️ Важливо

1. **Локальна конфігурація** - не впливає на інших розробників
2. **CI/CD не залежить** - завжди робить повний checkout
3. **Деактивуйте** при роботі з виключеними директоріями
4. **Файли `.git/info/*`** не синхронізуються з Git (це правильно!)

## 🐛 Troubleshooting

**Проблема:** Скрипт не працює

```bash
# Перевірити executable права
ls -la .git/info/sparse-checkout-toggle.sh

# Встановити права
chmod +x .git/info/sparse-checkout-toggle.sh
```

**Проблема:** Symlink не створюється

```bash
# Створити вручну
ln -sf .git/info/sparse-checkout-toggle.sh git-dev-mode

# Або використовувати повний шлях
.git/info/sparse-checkout-toggle.sh enable
```

## 📞 Підтримка

При проблемах дивіться:
- **docs/devops/GIT-SPARSE-CHECKOUT.md** - Повний гайд
- **docs/devops/DEVOPS-GUIDE.md** - Секція "Git Sparse Checkout"

---

**Версія:** 1.0.0
**Дата:** 2025-12-19
